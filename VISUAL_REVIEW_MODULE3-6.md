# Visual review of Modules 3–6 (screenshots of the live site)

One reviewer per chapter examined full-page screenshots of agdataanalytics.com taken with headless Chrome on 2026-08-22 (commit `ddfb1ed`): desktop at 1280px and mobile at 390px, sliced into tiles. Tile filenames in the findings refer to the screenshot set kept with this session; the section names locate the same spot on the page. Embedded SharePoint workbooks render blank in headless Chrome and were excluded from judgement. The site ignores the OS dark-mode preference (dark and light captures were identical), which is itself worth a decision.

## Must-fix items

- **module03a** [mobile] (7.5.2 Check what R guessed, the tickets <- read_csv(... col_types = cols(...)) block; module03a_mobile_09.png (compare module03a_desktop_06.png)): The deeply space-aligned col_types arguments are indented ~40 characters, so on a 390px screen the block renders as a grey box with two visible fragments and four blank lines; the actual column specification (the point of the example) is entirely off-screen and there is no visible horizontal scrollbar to hint it exists. *Fix:* Reformat the block with a line break after read_csv( and cols( and a 2-space indent for each argument so lines stay under ~45 characters, or enable code wrapping (code-overflow: wrap) for this chapter. Apply the same to the grain_deliveries_messy and na = c(...) blocks just below.
- **module03a** [mobile] (Every code and output block from 7.3 onward (JSON response, Power Query M code, glimpse() output, httr2 request, README); module03a_mobile_04.png, mobile_08.png, mobile_09.png, mobile_11.png): Long lines are cut off at the right edge of the viewport with no scrollbar visible, e.g. the JSON time array, the M code Web.Contents lines, the chr (4): column spec line and the tibble output in 7.5.4. A reader on a phone cannot see that content continues. *Fix:* Set code-overflow: wrap in the chapter YAML (or site-wide), or style pre blocks with a visible overflow-x scrollbar on small screens. Where wrapping would hurt (the M code, tibble printouts), shorten lines: drop trailing columns from the 62 x 6 tibble print and truncate the JSON arrays with ellipses as the prose already says it is cut down.
- **module03b** [mobile] (8.1 One-to-one, many-to-one and many-to-many, Figure 8.1 (module03b_mobile_01.png)): The join diagram is one wide raster/SVG scaled to the 340px column, so cell text, key labels and the row-count captions are about 5px tall and unreadable. The connecting lines are barely visible. *Fix:* Split the figure into three stacked panels (one per relationship) or rebuild it as three separate images so each panel can fill the mobile width; alternatively wrap it in an overflow-x: auto container at its native width so the reader can pan.
- **module03b** [mobile] (8.3 Merging in R, all code blocks (module03b_mobile_04.png, module03b_mobile_05.png, module03b_mobile_06.png)): Every code block is clipped at the right edge with no visible scrollbar: `by = c("rm" = "rm_nu`, `relationship = "many-to-o`, `select(year, rm, rm_name, canola, ... precip_may_`. The clipped parts are precisely the join keys the chapter teaches. Comments are cut off mid-sentence too. *Fix:* Enable horizontal scrolling on pre blocks (overflow-x: auto, with a visible scroll affordance) or set code-overflow: wrap in _quarto.yml; also shorten the long comments and break `by = c(...)` arguments onto their own lines so the important tokens fit in 40 characters.
- **module03c** [mobile] (All R code blocks in 9.3 (e.g. opening read_csv block, 9.3.3 filter/str_detect, 9.3.5 filter, 9.3.8 summarise(across(...))); module03c_mobile_00.png, module03c_mobile_04.png, module03c_mobile_06.png, module03c_mobile_07.png, module03c_mobile_08.png, module03c_mobile_09.png, module03c_mobile_10.png, module03c_mobile_11.png): Code blocks are cut off at the right edge of the viewport. Comments ("# Load the packages used in this chap"), arguments ("filter(weight_tonnes <= 0 | moistur"), and whole lines ("summarise(across(everything(), ~ su") are invisible; the grey code background itself runs past the screen edge, so it is not obviously a scrollable box. *Fix:* Give pre/code blocks overflow-x: auto and max-width: 100% in the book CSS (or Quarto's code-overflow: scroll), and shorten the longest lines in the source: break comments to under ~45 characters and put one argument per line in the filter()/select()/summarise() calls so the code fits a 390px column without scrolling.
- **module04a** [both] (Label lines directly, barley-share area chart pair (desktop_03, mobile_05)): This is the only figure in the chapter with no number and no Figure caption; its subcaptions ('A. Labels placed beside the lines.') are plain green text instead of the italic '(a) A. ...' style used everywhere else. The chart itself has ~20 variety labels and a 22-item legend at about 5px, unreadable on desktop and a smear on mobile. It also undercuts the section's point: the 'direct labels' panel is as hard to read as the legend panel. *Fix:* Give the block a fig-cap and label so it renders as Figure 10.x with the standard subcaption style. Replace the 20-variety example with 4-6 series (e.g. the four crops already used in 10.6) so the direct labels are actually legible, or render at larger base font size and full column width.
- **module04a** [both] (Figure 10.11 'Average yield by crop, 2021-2025' (desktop_04, mobile_07)): In-chart title says average yield by crop (Oats, Barley, Spring wheat, Canola) but the Quarto caption reads 'Average spring wheat yield by risk zone.' The reader sees two contradictory descriptions of the same chart. *Fix:* Change the caption to match the chart (average yield by crop, 2021-2025) or swap in the intended risk-zone chart.
- **module04b** [both] (Sections 11.2 Create a Chart through 11.5 Moving a Chart Into a Report (module04b_desktop_01.png, module04b_desktop_02.png, module04b_mobile_02.png, module04b_mobile_03.png)): The chapter has no static figure anywhere. After the embed, four sections of prose describe chart types, Chart Design > Add Chart Element, Format Axis, legends, data labels, 3D effects and Paste as Picture without a single screenshot or rendered chart. For a chapter about making graphs this reads as visually monotonous and makes the instructions hard to follow for someone who has not seen the Excel ribbon. *Fix:* Add three small PNG figures exported from the workbook (bar, line, scatter) with the same style and width as the text column, placed in 11.1 or 11.2, and one or two cropped screenshots of the Insert > Charts group and the Format Axis pane in 11.2 and 11.3. Give each a short caption.
- **module04c** [both] (12.2 A First Plot, Figure 12.1 and all later scatter plots (module04c_desktop_01.png, desktop_03.png, desktop_04.png; mobile_01.png)): The rendered plot images are cropped on the right: the final x-axis tick label '500' is cut to '50C' and the panel's right margin is missing on Figures 12.1, 12.2, 12.8, 12.9 and 12.10. It looks like a rendering bug rather than a design choice. *Fix:* Regenerate the figures with a slightly wider device or add plot.margin in the theme; or set fig-width/fig-asp in the chunk options so ggplot has room for the last tick label. Check the saved PNGs directly rather than the page.
- **module04c** [both] (geom_line(): change over time, Figure 12.3 (module04c_desktop_01.png bottom / desktop_02.png top; mobile_02.png)): A line joining two points, with x ticks at 2024.00, 2024.25 ... 2025.00 and a y-axis spanning 45.80 to 45.95, looks broken and teaches a bad habit. The chart is taller than its content warrants and makes a 0.15 bu difference look dramatic. *Fix:* Either use more years in the synthetic data, or add scale_x_continuous(breaks = c(2024, 2025)) and an explicit y range (or expand_limits(y = 0)) and state in one sentence that the line is steep because the y range is narrow. A short, wide aspect (fig-asp 0.4) would also suit two points better.
- **module05a** [both] (13.4 Common Failure Modes, Figure 13.2 (module05a_desktop_01.png bottom, module05a_mobile_03.png)): The timeline figure uses very small label text (about 9px on desktop); on mobile the whole figure is about 300px wide and the five labels are unreadable. It is also rendered in a different sans font and style from the ggplot-style bar charts around it. The tile boundary also cuts it across desktop_01/02, but the figure itself is just very short and wide. *Fix:* Regenerate at a larger font size (labels at least 11-12px after scaling) and a taller aspect ratio, or replace with a simple two-column list (R stops you / nothing stops you). Match the font and green/rust palette of Figures 13.1 and 13.3.
- **module05a** [mobile] (13.5 A Silent Failure, all three R code blocks (module05a_mobile_03.png, module05a_mobile_04.png, module05a_mobile_05.png)): Long lines (read_csv paths, mutate(Revenue = ...), weighted.mean(...)) are clipped at the right edge of the code block with no visible scrollbar, so the reader cannot tell the line continues. *Fix:* Either shorten the long lines in the source (break the read_csv calls and weighted.mean arguments over multiple lines) or enable code-overflow: wrap in _quarto.yml for this site, so mobile shows the complete code.
- **module05b** [both] (14.1 Getting Access, Figure 14.1 (module05b_desktop_00.png, module05b_mobile_01.png)): The figure image fails to load: a tiny broken-image icon appears centred above the caption 'Figure 14.1: Applying for GitHub Education student benefits.' The chapter's only figure is therefore missing, and the orphaned caption looks unfinished. *Fix:* Fix the image path in module05b.qmd (check the file exists in the images folder and is committed; filenames are case-sensitive on GitHub Pages). If no screenshot exists yet, remove the figure block rather than ship a broken one. Once it loads, constrain width to the text column (e.g. fig-width / width="100%").
- **module05c** [mobile] (15.3 A Useful Prompt Structure, example prompt block (module05c_mobile_03.png; desktop module05c_desktop_01.png)): The example prompt is set as a preformatted/code block with hard line breaks. On a 390px screen it is clipped at the right edge ('Crop, Yea', 'recorded Y', 'Show t') and there is no visible scrollbar, so the reader cannot see the full prompt. On desktop it sits with ragged hard-wrapped lines that look like code rather than prose a student would type. *Fix:* This is natural-language text, not code. Put it in a blockquote or a callout (e.g. ::: {.callout-note title="Example prompt"}) so it wraps normally on every width. If it must stay monospace, drop the hard line breaks and add `white-space: pre-wrap` for that block.
- **module06a** [both] (16.3 Three Workbooks, Three Audiences -- the three collapsed callouts (module06a_desktop_01.png, module06a_mobile_02.png)): The only concrete examples in the chapter (Manitoba elevator capacity, Crop Planning Guide, RM yields) are collapsed one-line callouts with a lightbulb icon. A reader sees three identical thin bars and no indication that an embedded workbook or screenshot is behind them. The prose describes bold headers, borders and layout that the reader cannot see without clicking. *Fix:* Open the three callouts by default (collapse="false") or replace them with a static screenshot of each workbook's first sheet above the embed, so the visual contrast between the three files is visible on the page.
- **module06a** [mobile] (16.6 Hyperlinks -- =HYPERLINK code block (module06a_mobile_04.png)): The two-line HYPERLINK example is cut off at the right edge on mobile ("Go to sum", "saskatc", "Dash") with no visible scrollbar, so the URL and second argument are unreadable. *Fix:* Break the long URL call across lines (one argument per line) so it fits in ~40 characters, or enable code-overflow: wrap for this block. Consider dropping the URL example to a shorter placeholder URL.

Totals: 16 must-fix, 32 should-fix, 44 consider.

## module03a -- Getting Data

On desktop the chapter reads cleanly: comfortable line length, consistent green heading styling, well-spaced code blocks, and no broken elements, though it is a long chapter with no figures at all, so the eye has nothing to rest on except code. On mobile the prose holds up but almost every code block and console transcript is clipped on the right with no visible scrollbar, and the four-column "Four ways to get it" table is squeezed into columns three or four words wide. The single worst problem is the col_types code block in 7.5.2, which on mobile shows as a mostly empty grey box because the space-aligned arguments push all content past the right edge (module03a_mobile_09.png). Note: the mobile capture restarts from the chapter top midway through tile 11 and never reaches 7.5.5 onward, so sections 7.5.6 to 7.7 were reviewed on desktop only.

- **[must-fix, mobile] 7.5.2 Check what R guessed, the tickets <- read_csv(... col_types = cols(...)) block; module03a_mobile_09.png (compare module03a_desktop_06.png)** -- The deeply space-aligned col_types arguments are indented ~40 characters, so on a 390px screen the block renders as a grey box with two visible fragments and four blank lines; the actual column specification (the point of the example) is entirely off-screen and there is no visible horizontal scrollbar to hint it exists.
  *Fix:* Reformat the block with a line break after read_csv( and cols( and a 2-space indent for each argument so lines stay under ~45 characters, or enable code wrapping (code-overflow: wrap) for this chapter. Apply the same to the grain_deliveries_messy and na = c(...) blocks just below.
- **[must-fix, mobile] Every code and output block from 7.3 onward (JSON response, Power Query M code, glimpse() output, httr2 request, README); module03a_mobile_04.png, mobile_08.png, mobile_09.png, mobile_11.png** -- Long lines are cut off at the right edge of the viewport with no scrollbar visible, e.g. the JSON time array, the M code Web.Contents lines, the chr (4): column spec line and the tibble output in 7.5.4. A reader on a phone cannot see that content continues.
  *Fix:* Set code-overflow: wrap in the chapter YAML (or site-wide), or style pre blocks with a visible overflow-x scrollbar on small screens. Where wrapping would hurt (the M code, tibble printouts), shorten lines: drop trailing columns from the 62 x 6 tibble print and truncate the JSON arrays with ellipses as the prose already says it is cut down.
- **[should-fix, mobile] Four ways to get it table in 7.2; module03a_mobile_02.png, mobile_03.png, mobile_15.png, mobile_16.png** -- Four columns of prose in a 390px viewport gives cells three or four words wide ("Give Excel / or R the / address of / a / published / file") and the Good for column is clipped at the right edge. The table runs over two full screens and is hard to scan.
  *Fix:* Reduce to three columns by merging What you get into What you do, or replace the table with a short bulleted list of the four routes (route in bold, one sentence each). If the table stays, wrap it in a scrolling container with a visible edge so the clipped column is discoverable.
- **[should-fix, both] Unnumbered subheadings: Four ways to get it (7.2), File formats (7.4), Text to Columns is for repairs (7.4); module03a_desktop_01.png, desktop_02.png, desktop_04.png** -- These three headings render at the same size and colour as the numbered 7.4.1 and 7.4.2 subsections but carry no number, so they look like a different level and do not appear in the right-hand table of contents. Within 7.4 the reader sees File formats, 7.4.1, 7.4.2, then Text to Columns, which reads as inconsistent.
  *Fix:* Either number them (make them ordinary ### headings so Quarto numbers them 7.2.1, 7.4.1 and so on) or demote all three to a smaller #### level so the hierarchy is visibly different.
- **[should-fix, both] Whole chapter, especially 7.4.2 Power Query; module03a_desktop_03.png, desktop_04.png** -- The chapter has no figures or screenshots. The Power Query section describes a visual tool (the preview, the type icons in column headers, the Advanced Editor, the record view for JSON) in five dense paragraphs of prose studded with bold menu names. This is the one place a picture would carry most of the explanation, and its absence makes the desktop page a long run of grey code boxes and text.
  *Fix:* Add one or two screenshots: the Power Query preview showing the column-type icons for elevator_tickets.csv, and the record view after pasting the Open-Meteo URL. Size them to the text column width and give them short captions.
- **[consider, both] 7.4.2 Power Query, second paragraph; module03a_desktop_03.png** -- One paragraph contains nine bolded UI labels (Get Data > From Text/CSV, Get Data (Power Query), Transform Data, Promoted Headers, Changed Type, Close & Load ...) plus five inline code spans, so the bold stops signalling anything and the paragraph looks speckled.
  *Fix:* Break the procedure into a short numbered list (open, preview, Transform Data, set types, Close & Load) so each bold label heads its own step, or keep bold only for the menu paths and leave step names in plain text.
- **[consider, both] Console output blocks versus code blocks, e.g. 7.5.2, 7.5.5, 7.5.7; module03a_desktop_05.png, desktop_07.png, desktop_08.png** -- Code input has a grey background while output (Rows: 20 Columns: 7, the tibble prints, [1] 200) sits on white with the same monospace font. The convention is sensible but nothing tells the reader which is which; the glimpse(rm_yields) output in particular is long and its bare trailing "..." line looks like a missing element.
  *Fix:* Add a light left border or a faint tint to output blocks (Quarto's .cell-output styling) so input and output are visibly distinct, and replace the standalone "..." with a comment such as # 13 more columns.
- **[consider, desktop] End of chapter, Other resources callout and page-nav footer; module03a_desktop_09.png** -- The collapsed Other resources callout is followed by roughly 150px of empty space before the previous/next navigation, which makes the chapter end look unfinished, and a collapsed box with no visible content is easy to skip entirely.
  *Fix:* Either expand the callout by default (it is the last item, so there is nothing to push down) or tighten the bottom margin between the last element and the page navigation.

## module03b -- Merging Data

On desktop this is a clean, well-proportioned chapter: short prose blocks alternate with code, the heading hierarchy is clear, and the join diagram sits comfortably in the column. On mobile the prose and lists still read well, but Figure 8.1 collapses to an unreadable thumbnail and every R code block clips its right edge, so the reader cannot see the `by =` arguments that the chapter is about. The worst problem is the mobile figure: the three-table diagram shrinks to roughly 340px wide with 5-6px text, making the core concept of the chapter illegible on a phone.

- **[must-fix, mobile] 8.1 One-to-one, many-to-one and many-to-many, Figure 8.1 (module03b_mobile_01.png)** -- The join diagram is one wide raster/SVG scaled to the 340px column, so cell text, key labels and the row-count captions are about 5px tall and unreadable. The connecting lines are barely visible.
  *Fix:* Split the figure into three stacked panels (one per relationship) or rebuild it as three separate images so each panel can fill the mobile width; alternatively wrap it in an overflow-x: auto container at its native width so the reader can pan.
- **[must-fix, mobile] 8.3 Merging in R, all code blocks (module03b_mobile_04.png, module03b_mobile_05.png, module03b_mobile_06.png)** -- Every code block is clipped at the right edge with no visible scrollbar: `by = c("rm" = "rm_nu`, `relationship = "many-to-o`, `select(year, rm, rm_name, canola, ... precip_may_`. The clipped parts are precisely the join keys the chapter teaches. Comments are cut off mid-sentence too.
  *Fix:* Enable horizontal scrolling on pre blocks (overflow-x: auto, with a visible scroll affordance) or set code-overflow: wrap in _quarto.yml; also shorten the long comments and break `by = c(...)` arguments onto their own lines so the important tokens fit in 40 characters.
- **[should-fix, desktop] Figure 8.1 top, One-to-one panel (module03b_desktop_00.png / module03b_desktop_01.png)** -- The figure has a wide gap above it and the panel title 'One-to-one (1:1)' is separated from its tables by the tile boundary, but more notably the figure is narrower than the text column (about 700px of 780px) and left-indented, while the 'key:' labels sit at the far right; it reads as slightly off-centre and unlike the full-width code blocks around it.
  *Fix:* Size the figure to the column width (fig-width matching the text column, or width=100%) and centre it; keep consistent margins with the code blocks.
- **[should-fix, both] 8.2 Merging in Excel, embedded workbook (module03b_desktop_01.png, module03b_desktop_02.png; module03b_mobile_02.png, module03b_mobile_03.png)** -- The embed box is very tall for what it shows: roughly 720px on desktop (almost a full viewport) and about 500px of empty white on mobile, for a sheet described as thirty rows plus two lookup columns. It breaks the flow between the two paragraphs that explain the sheet and the paragraph that interprets it.
  *Fix:* Reduce the iframe height to fit the thirty-row sheet (around 450-500px desktop) and give it a smaller fixed height on mobile via a media query; consider placing the 'Download this workbook' line directly under the heading so the reader can open it before scrolling past the box.
- **[should-fix, both] 8.3 Merging in R, 'Show console session' collapsed blocks (module03b_desktop_02.png through module03b_desktop_04.png)** -- Six consecutive code chunks each end in a collapsed 'Show console session' bar, all closed. The row-count argument of the chapter (2,950 stays 2,950, then becomes 29,500) is only in those hidden outputs, so the prose repeatedly asserts results the reader never sees without clicking. The small-caps amber label also looks visually unrelated to the grey code block it belongs to.
  *Fix:* Open by default the two sessions that carry the point (First join nrow and The row-count trap), or print the key numbers inline as a short output block; style the toggle to sit flush against the code block so it reads as its output.
- **[consider, both] 8.1 Learning Objectives callout (module03b_desktop_00.png, module03b_mobile_00.png)** -- The callout uses numbered list markers plus a green checkmark on each item, doubling the marker and pushing the item text into a narrow column on mobile where every objective wraps to two or three lines.
  *Fix:* Drop either the number or the checkmark (keep the number), which also recovers about 30px of width on mobile.
- **[consider, both] 8.3 First join, inline code in prose (module03b_desktop_03.png, module03b_mobile_05.png)** -- Long inline code spans such as `relationship = "many-to-one"` and `by = c("left_name" = "right_name")` break across lines on mobile and produce ragged grey boxes mid-sentence; on desktop the spacing around inline code is wider than the body word spacing, so lines with several spans look gappy.
  *Fix:* Keep inline code to single tokens and put multi-argument examples in a display code block; reduce padding on inline code in the theme CSS (for example 0.1em 0.3em).
- **[consider, both] 8.4 What the Merge Gives You, chapter end (module03b_desktop_04.png, module03b_mobile_06.png)** -- The chapter ends on a paragraph of prose about limits with a large empty band before the prev/next navigation; after the build-up about yield versus rain there is no chart of the annual means the last code block computes, so the section title promises a result the page does not show.
  *Fix:* Add a small figure (annual mean canola yield and rainfall) or the printed summary table directly after the code, sized to the text column like the rest of the chapter.

## module03c -- Cleaning Data

On desktop the chapter is clean and consistent: comfortable line length, clear green heading hierarchy, code blocks sized to the text column, and a tidy checklist at the end. It is also visually monotonous, because section 9.3 is roughly five screens of the same prose / code block / collapsed "Show console session" sandwich with every result hidden and no figure or table anywhere. On mobile the code blocks are clipped at the right edge in almost every tile (comments, filter() arguments and string literals run off-screen), the 9.3.1 heading with two inline code spans wraps to three lines, and the 390px-wide blank workbook embed is a tall empty box. The single worst problem is the mobile code overflow, which hides parts of the actual code the student is meant to copy.

- **[must-fix, mobile] All R code blocks in 9.3 (e.g. opening read_csv block, 9.3.3 filter/str_detect, 9.3.5 filter, 9.3.8 summarise(across(...))); module03c_mobile_00.png, module03c_mobile_04.png, module03c_mobile_06.png, module03c_mobile_07.png, module03c_mobile_08.png, module03c_mobile_09.png, module03c_mobile_10.png, module03c_mobile_11.png** -- Code blocks are cut off at the right edge of the viewport. Comments ("# Load the packages used in this chap"), arguments ("filter(weight_tonnes <= 0 | moistur"), and whole lines ("summarise(across(everything(), ~ su") are invisible; the grey code background itself runs past the screen edge, so it is not obviously a scrollable box.
  *Fix:* Give pre/code blocks overflow-x: auto and max-width: 100% in the book CSS (or Quarto's code-overflow: scroll), and shorten the longest lines in the source: break comments to under ~45 characters and put one argument per line in the filter()/select()/summarise() calls so the code fits a 390px column without scrolling.
- **[should-fix, both] Every "Show console session" callout, most importantly the first one after glimpse(deliveries) where the prose says "The output already shows two problems"; module03c_desktop_00.png, module03c_desktop_03.png through module03c_desktop_07.png** -- Seventeen identical collapsed "Show console session" bars, all closed by default. Several paragraphs describe output the reader cannot see without clicking (the glimpse() types, the T0030 duplicate, the count(crop) spellings). The chapter's argument is about noticing problems in output, and the output is hidden.
  *Fix:* Open the key transcripts by default (the first glimpse(), the count(crop) before/after, the duplicates count, the summary() ranges) and keep the rest collapsed. Alternatively render short outputs as small inline tables instead of collapsed callouts.
- **[should-fix, both] 9.2 Cleaning with formulas, embedded workbook; module03c_desktop_01.png, module03c_desktop_02.png, module03c_mobile_02.png, module03c_mobile_03.png** -- The workbook embed is very tall (about 1,200px on desktop, spanning a tile and a half; about 450px tall but only ~340px wide on mobile). On desktop it pushes the rest of 9.2 a full screen away; on mobile a spreadsheet in a 340px frame is unusable even when it does load.
  *Fix:* Reduce the desktop embed height to roughly 600-700px so the table and the summary checks fit without dead space. On mobile hide the iframe with a media query and show only the "Download this workbook / Open online" links, or replace it with a static screenshot of the formula columns.
- **[should-fix, both] 9.2 heading structure: "9.2.1 Reshaping with Power Query" followed by unnumbered "Cleaning with formulas" and "Cleaning with Power Query"; module03c_desktop_01.png, module03c_desktop_02.png** -- 9.2 promises three approaches (formulas, Power Query, Find & Replace), but only 9.2.1 is numbered; the two cleaning subsections are unnumbered H4s that visually read as children of 'Reshaping with Power Query', and Find & Replace has no section at all. The sidebar TOC shows only 9.2, so none of this is navigable.
  *Fix:* Promote "Cleaning with formulas" and "Cleaning with Power Query" to numbered ### headings (9.2.2, 9.2.3) so they sit beside 9.2.1 and appear in the right-hand TOC. Either add a short numbered Find & Replace note or drop it from the 9.2 intro.
- **[consider, both] 9.3.1 heading "Reshaping with pivot_longer() and pivot_wider()"; module03c_desktop_02.png, module03c_mobile_04.png** -- Inline code in a heading renders as two grey monospace boxes inside the green heading; on mobile the heading wraps to three lines with the code boxes dominating. No other heading in the chapter uses code, so it looks inconsistent.
  *Fix:* Rename the heading to plain text, e.g. "Reshaping Between Wide and Long", and name the functions in the first sentence of the section.
- **[consider, both] Section 9.3 as a whole (9.3.2 through 9.3.9); module03c_desktop_03.png through module03c_desktop_07.png** -- About five desktop screens of prose / code block / collapsed callout repeated with no figure, table, or callout variation. The chapter has no image at all. The rhythm makes the subsections hard to tell apart when scrolling.
  *Fix:* Break the run with one or two visible outputs rendered as tables (the count(crop) before/after is ideal), or a small before/after figure of the wide and long yield tables in 9.1 where the wide-versus-long idea is introduced.
- **[consider, both] Unnumbered sub-headings: "Recognizing untidy data" (9.1) and "Cleaning with formulas" (9.2) versus "Missing values in disguise", "Working with missing values", "When is dropping safe?" (9.3.8); module03c_desktop_01.png, module03c_desktop_07.png, module03c_desktop_08.png** -- Two different unnumbered heading styles appear: a larger green H4 in 9.1/9.2 and a smaller dark H5 in 9.3.8. Both serve the same role (a topic inside a section), so the size and colour change reads as inconsistent rather than hierarchical.
  *Fix:* Use one level for all unnumbered subsections. Either number 9.3.8's three sub-parts' parent consistently or convert "Recognizing untidy data" and the two 9.2 cleaning headings to the same level as the 9.3.8 ones.
- **[consider, desktop] Learning Objectives box and spacing before code blocks; module03c_desktop_00.png, module03c_desktop_04.png** -- Vertical spacing is uneven: a large gap (~70px) sits between the objectives box and 9.1, and each code block has noticeably more space above it than below it (the console callout hugs the block), so prose-to-code gaps look bigger than code-to-prose gaps.
  *Fix:* Tighten the bottom margin of the callout box and equalise the code block's top and bottom margins in CSS (e.g. margin: 1.25rem 0 for both the pre and the following console callout).
- **[consider, mobile] End of page on mobile: navigation header and chapter title repeat after checklist item 6; module03c_mobile_11.png** -- The bottom mobile tile shows the green header, breadcrumb and "9 Cleaning Data" opening paragraph again directly below checklist item 6, and items 7-8 plus the prev/next links are missing. This is most likely a screenshot stitching artefact, but if it is real the sticky header is overlapping content at the page end.
  *Fix:* Verify in a real phone browser that the checklist runs to item 8 and the prev/next footer is reachable; if the sticky header overlaps, add scroll-padding / bottom padding to the main column.

## module04a -- Graphing Best Practices

On desktop the chapter reads well: short prose blocks alternate with figures, headings are clearly tiered, and the two-panel before/after comparisons are the best-looking element in the book so far. The weakness is figure sizing: single-panel charts arrive at four or five different widths (roughly 55% to 85% of the text column), several carry tiny axis text, and the Tufte-style barley-share chart under "Label lines directly" is illegible even at 1280px. On mobile the stacked two-panel figures hold up, but the single figures (10.11, 10.14, 10.15 and especially the small multiples in 10.17) shrink to unreadable thumbnails. The single worst problem is the unnumbered, uncaptioned barley-acreage pair under "Label lines directly", which has micro-text labels, a different subcaption style from every other figure, and no figure number. (Mobile tiles 11-12 show the page restarting from the top; that is a capture-stitching artefact, not a page defect.)

- **[must-fix, both] Label lines directly, barley-share area chart pair (desktop_03, mobile_05)** -- This is the only figure in the chapter with no number and no Figure caption; its subcaptions ('A. Labels placed beside the lines.') are plain green text instead of the italic '(a) A. ...' style used everywhere else. The chart itself has ~20 variety labels and a 22-item legend at about 5px, unreadable on desktop and a smear on mobile. It also undercuts the section's point: the 'direct labels' panel is as hard to read as the legend panel.
  *Fix:* Give the block a fig-cap and label so it renders as Figure 10.x with the standard subcaption style. Replace the 20-variety example with 4-6 series (e.g. the four crops already used in 10.6) so the direct labels are actually legible, or render at larger base font size and full column width.
- **[must-fix, both] Figure 10.11 'Average yield by crop, 2021-2025' (desktop_04, mobile_07)** -- In-chart title says average yield by crop (Oats, Barley, Spring wheat, Canola) but the Quarto caption reads 'Average spring wheat yield by risk zone.' The reader sees two contradictory descriptions of the same chart.
  *Fix:* Change the caption to match the chart (average yield by crop, 2021-2025) or swap in the intended risk-zone chart.
- **[should-fix, desktop] Single-panel figures 10.3, 10.4, 10.9, 10.11, 10.13, 10.14, 10.15 (desktop_01, 03, 04, 05)** -- Single charts are rendered at inconsistent widths: 10.3/10.4 about 60% of the column and left-indented, 10.9 about 55%, 10.11 about 75%, 10.13/10.14 about 85%. Axis text in 10.3/10.4 is noticeably smaller than in 10.13. The chapter is about consistent, deliberate chart formatting and its own figures vary from one to the next.
  *Fix:* Set one fig-width/fig-height and out-width (e.g. 100% of column, 7x4 in) for all single-panel ggplots and one theme base_size (14-16) so text renders at the same physical size across figures.
- **[should-fix, mobile] Figure 10.17 small multiples and Figures 10.11, 10.14, 10.15 (mobile_07, mobile_08, mobile_09)** -- At 390px the four-panel small-multiples figure is about 330px wide with axis labels of roughly 3px; 10.11, 10.14 and 10.15 are likewise thumbnails with unreadable axis text and legends, while the stacked two-panel figures nearby are perfectly legible because each panel gets the full width.
  *Fix:* Render small multiples as a 2x2 facet grid instead of 1x4, and increase base font size / reduce fig-width on the single-panel charts so the text scales up on narrow screens. Alternatively wrap them in lightbox so a tap enlarges them.
- **[should-fix, both] Two-panel subcaptions throughout, e.g. '(a) A. Cluttered', '(b) B. Reduced' (desktop_02, desktop_03, mobile_04)** -- Every subpanel is labelled twice: Quarto adds '(a)/(b)' and the in-chart titles add 'A./B.' again, so captions read '(a) A. Cluttered'. In 10.6 the in-chart title says 'B. Decluttered' while the subcaption says 'B. Reduced'.
  *Fix:* Drop the 'A.'/'B.' prefixes from the subcaptions (keep them only in the chart titles) or drop the in-chart titles, and make the wording match (Decluttered vs Reduced).
- **[should-fix, both] In-chart titles in 10.11, 10.13, 10.14, 10.15, 10.16 vs none in 10.3-10.10 (desktop_04, 05, 06)** -- The 'Types of Charts' figures carry bold in-chart titles ('Average yield by year', 'Zones that grow good wheat also grow good barley (2025)') while the 'General Principles' figures have none and rely on the caption. The text in 'Use descriptive labels' tells the reader not to repeat a title inside a chart that has a document caption.
  *Fix:* Remove the in-chart titles from the Section 10.2 figures (or move the wording into the captions) so the chapter follows its own rule and the figures look like one set.
- **[should-fix, both] Figure 10.20 panel A reference line (desktop_07, mobile_11)** -- The dashed-line label 'five-year average 38 bu/ac' sits on top of the 2022 bar and is partly hidden behind it; the value is unreadable in both views.
  *Fix:* Place the label at the right end of the line, above it, with a white background (annotate with label/geom_label, or put the value in the caption) so it does not collide with bars.
- **[consider, mobile] Figure 10.2 Georgia COVID chart (mobile_00)** -- The screenshot is 340px wide on mobile and its date labels, the thing the text asks the reader to inspect, are unreadable. The caption's 'click to view full size' is the only route to the point of the example.
  *Fix:* Add a cropped detail image of the x-axis dates (or list the out-of-order dates in the prose) so a phone reader can see the problem without opening the full image.
- **[consider, desktop] Figure 10.6 panel A 'Cluttered' and Figure 10.5 (desktop_02, desktop_01)** -- The two panels of 10.5 and 10.6 are slightly different sizes (panel A narrower and shorter than B), and in 10.6A the dense y-axis labels (5-unit steps from 20 to 115) become a grey smudge. Some clutter is intended, but the mismatched panel dimensions look accidental.
  *Fix:* Give both panels identical fig-width/fig-height so the comparison is like-for-like; keep the clutter but not the size difference.

## module04b -- Graphing in Excel

On desktop this is a clean, short chapter: comfortable line length, consistent H2 styling, a tidy learning-objectives callout, and no code or figure problems because there are no figures or code at all. On mobile it reads fine too, with the only awkward element being the embedded workbook, which becomes a tall empty column. The single worst problem is that a chapter titled "Graphing in Excel" contains zero images of a chart or of the Excel interface: sections 11.2 to 11.5 are five screens of unbroken prose about menus, axes, legends and paste options with nothing to look at, and the one visual (the embedded workbook) is a roughly 700px tall box whose bar chart, line chart and scatter plot are invisible unless the SharePoint embed loads.

- **[must-fix, both] Sections 11.2 Create a Chart through 11.5 Moving a Chart Into a Report (module04b_desktop_01.png, module04b_desktop_02.png, module04b_mobile_02.png, module04b_mobile_03.png)** -- The chapter has no static figure anywhere. After the embed, four sections of prose describe chart types, Chart Design > Add Chart Element, Format Axis, legends, data labels, 3D effects and Paste as Picture without a single screenshot or rendered chart. For a chapter about making graphs this reads as visually monotonous and makes the instructions hard to follow for someone who has not seen the Excel ribbon.
  *Fix:* Add three small PNG figures exported from the workbook (bar, line, scatter) with the same style and width as the text column, placed in 11.1 or 11.2, and one or two cropped screenshots of the Insert > Charts group and the Format Axis pane in 11.2 and 11.3. Give each a short caption.
- **[should-fix, both] 11.1 Start With the Table, embedded workbook (module04b_desktop_00.png / module04b_desktop_01.png, module04b_mobile_01.png)** -- The workbook embed is about 700px tall on desktop and spans most of a mobile screen (roughly 520px tall at 390px wide). It sits between the paragraph introducing the three sheets and the paragraph that tells the reader what to look for, so the explanatory text is split across the embed. On mobile the embed area is too narrow to show a chart plus its table usefully anyway.
  *Fix:* Reduce the iframe height (around 450-500px on desktop) and move the 'Open the workbook and look at the table beside each chart' paragraph above the embed so the reader knows what to look for before the box. Consider hiding the embed below ~600px viewport and showing only the Download / Open online links plus the static chart PNGs.
- **[consider, both] Paragraph immediately below the embed: 'Download this workbook · Open online (File → Save a Copy...)' (module04b_desktop_01.png, module04b_mobile_01.png)** -- The download/open-online line is styled as a normal body paragraph directly under the box, so it reads as a detached caption. On mobile the parenthetical wraps onto a second line and the arrow glyph looks out of place with the rest of the typography.
  *Fix:* Style this as a small, muted caption line (smaller font, grey) attached to the embed, or fold it into a consistent 'Workbook' callout used across all Excel chapters.
- **[consider, both] Learning Objectives callout (module04b_desktop_00.png, module04b_mobile_00.png)** -- Each list item carries both a number and a green check mark, which is redundant decoration; the check marks also push the text inward, so on mobile the items wrap to two or three lines each and the callout grows to nearly a full screen for six short lines.
  *Fix:* Drop either the numbering or the check icon (keep a plain ordered list) so the callout is more compact, particularly on mobile.
- **[consider, both] 11.2 Create a Chart, bullet list 'For the three charts in the workbook' (module04b_desktop_01.png, module04b_mobile_01.png / module04b_mobile_02.png)** -- The third bullet is a two-sentence paragraph while the first two are one line each, so the list looks lopsided; on mobile it splits across a tile boundary and the extra sentence about line charts not substituting for scatter gets lost in the bullet.
  *Fix:* Keep the bullets parallel (chart: selection, chart type) and move the 'A line chart is not a substitute here...' sentence into the prose after the list.
- **[consider, both] Heading hierarchy: chapter title and sections 11.1-11.5 (module04b_desktop_00.png, module04b_desktop_02.png, module04b_mobile_03.png)** -- Every section is a flat H2 with 2-4 short paragraphs; section 11.5 is three paragraphs and then the page ends with a large empty gap before the prev/next footer. Without sub-structure, callouts or figures, the chapter looks like a list of notes rather than a worked tutorial. On mobile the H2 '11.5 Moving a Chart Into a Report' wraps to two lines, which is fine, but emphasises how heading-heavy the short page is.
  *Fix:* Either merge 11.4 and 11.5 into one section, or give the longer sections (11.3) a figure or a tip callout (e.g. a 'Check before handing over' list in 11.5 as a callout) so the heading rhythm matches the amount of content under each.
- **[consider, both] Inline code spans in 11.3 Titles, Axes and Legends (module04b_desktop_01.png, module04b_mobile_02.png)** -- Chart title text ('Average field yield by region (bu/ac)') and a legend label ('Mean yield') are set in monospace code styling, while menu paths (Chart Design > Add Chart Element, Format Axis, Insert) are bold. Chart titles are not code, and on mobile the monospace span wraps awkwardly across two lines with a trailing orphaned period.
  *Fix:* Use quotation marks or italics for example title/label text and reserve code styling for formulas and cell references; keep bold for UI menu paths only.

## module04c -- Graphing in R

On desktop the chapter reads well: a clean code-then-figure rhythm, consistent grey-theme ggplot output, sensible caption lengths, and no overflow. The figures are the weak point: the right edge of every scatter plot is clipped (the "500" tick reads as "50C"), axis text is small, and the two-year line chart (Figure 12.3) is a visually silly plot with fractional year ticks and a y-axis spanning 0.2 bushels. On mobile the charts shrink to 340px with ~6px axis labels that are illegible, and every code block is clipped at the right edge with no visible scroll affordance, so the student cannot see the full aes() call in most examples. The worst single problem is the right-edge clipping of the plots, which is visible on both devices.

- **[must-fix, both] 12.2 A First Plot, Figure 12.1 and all later scatter plots (module04c_desktop_01.png, desktop_03.png, desktop_04.png; mobile_01.png)** -- The rendered plot images are cropped on the right: the final x-axis tick label '500' is cut to '50C' and the panel's right margin is missing on Figures 12.1, 12.2, 12.8, 12.9 and 12.10. It looks like a rendering bug rather than a design choice.
  *Fix:* Regenerate the figures with a slightly wider device or add plot.margin in the theme; or set fig-width/fig-asp in the chunk options so ggplot has room for the last tick label. Check the saved PNGs directly rather than the page.
- **[must-fix, both] geom_line(): change over time, Figure 12.3 (module04c_desktop_01.png bottom / desktop_02.png top; mobile_02.png)** -- A line joining two points, with x ticks at 2024.00, 2024.25 ... 2025.00 and a y-axis spanning 45.80 to 45.95, looks broken and teaches a bad habit. The chart is taller than its content warrants and makes a 0.15 bu difference look dramatic.
  *Fix:* Either use more years in the synthetic data, or add scale_x_continuous(breaks = c(2024, 2025)) and an explicit y range (or expand_limits(y = 0)) and state in one sentence that the line is steep because the y range is narrow. A short, wide aspect (fig-asp 0.4) would also suit two points better.
- **[should-fix, mobile] All figures, e.g. Figure 12.4 and 12.7 (module04c_desktop_02.png, desktop_03.png; mobile_03.png, mobile_04.png)** -- Figures are ~460px wide on desktop inside a 780px column, with axis tick labels around 8px; on mobile they scale to ~340px and the tick text (Central/North/South, 0-60) becomes roughly 5px and unreadable. The default ggplot theme at this output size is the cause.
  *Fix:* Render with a larger base_size (theme_grey(base_size = 14) or knitr fig.retina/dpi settings) and let figures fill the text column (out-width: 100%). Consistent fig-width 7, fig-asp 0.6 across the chapter would also keep all plots the same width; currently Figures 12.4-12.7 are a slightly different width and left offset than the scatter plots.
- **[should-fix, mobile] Code blocks throughout, e.g. 12.2 First Plot, geom_smooth, 12.5 Labels and Themes (module04c_mobile_01.png, mobile_04.png, mobile_05.png, mobile_06.png)** -- Every code block is clipped at the right edge on mobile ('aes(x = acres, y = yie', 'na.rm = TR', 'subtitle = "Sixty hypothetical Sa'). The light grey box gives no hint that the block scrolls horizontally, and the aes() call, which is the point of the chapter, is the part that is hidden.
  *Fix:* Wrap the aes() call across lines in the source (one argument per line, as the labs() block already does) so lines stay under ~40 characters, or enable code-overflow: wrap in _quarto.yml for this chapter. Adding a visible scrollbar or right-edge fade on mobile would also help.
- **[should-fix, desktop] 12.4 Mapping and Setting Aesthetics, Figures 12.9 and 12.10 side by side (module04c_desktop_04.png)** -- The two-column layout leaves the paired figures small, and the captions 'A. Colour mapped to region' and 'B. One colour set for all points' sit under panels of unequal width (left panel has a legend, so its plot area is narrower than the right one). The A/B lettering is redundant with the 12.9/12.10 numbering.
  *Fix:* Use one figure with two subfigures (fig-subcap) so there is a single caption and consistent panel width, or stack them at full width. Drop the 'A.'/'B.' prefixes.
- **[consider, both] 12.5 Labels and Themes, Figure 12.11 (module04c_desktop_04.png; mobile_06.png and mobile_07.png)** -- The one themed plot is visually inconsistent with the rest: white background, larger size, and a legend that pushes the panel off-centre. The source caption text 'Source: synthetic AREC 261 data' appears twice in different styles (inside the plot and in the figure caption area). The subtitle shows the literal '2024--2025' with two hyphens.
  *Fix:* Use an en dash or a plain hyphen in the subtitle string ('2024-2025'); consider keeping this figure at the same width as the others. On mobile the caption splits across tiles at a cramped 5px, which the base_size fix above would address.
- **[consider, both] Chapter opener, 'Show console session' collapsed blocks (module04c_desktop_00.png, desktop_05.png; mobile_00.png, mobile_07.png)** -- The collapsed 'Show console session' strip uses very small small-caps text (about 9px) in a tan box that does not match the green callout style used elsewhere on the page, and its purpose is not obvious. The opening code block then runs straight into Learning Objectives with no prose between, so the chapter opens with code, a hidden transcript, then objectives.
  *Fix:* Move the setup code block to after the Learning Objectives box (or into 12.2), and increase the label font size on the collapsible strip so it reads at normal body-label size.
- **[consider, both] Heading hierarchy in 12.3 Common Geoms, e.g. 'geom_histogram() and geom_boxplot(): distributions' (module04c_desktop_02.png; mobile_03.png)** -- Inline code in h3 headings renders in a large monospace box that is heavier than the heading text itself; on mobile the histogram/boxplot heading wraps to three lines with the lone word 'distributions' on its own line, and the 'bars' heading leaves a single word orphaned.
  *Fix:* Shorten the headings to plain text ('Scatter plots', 'Bars', 'Distributions', 'Fitted lines') and name the geom in the first sentence instead, which also sharpens the hierarchy between the h2 and the h3s.

## module05a -- AI Tools

On desktop this is a clean, readable chapter: comfortable line length, consistent heading hierarchy, well-paced mix of prose, code, figures and lists. On mobile it holds together except that every code block is clipped at the right edge with no visible scroll affordance, and the small figures (13.1, 13.2, 13.4) shrink to illegibility. The worst single problem is Figure 13.2, the failure-mode timeline, whose label text is tiny even on desktop and unreadable on mobile.

- **[must-fix, both] 13.4 Common Failure Modes, Figure 13.2 (module05a_desktop_01.png bottom, module05a_mobile_03.png)** -- The timeline figure uses very small label text (about 9px on desktop); on mobile the whole figure is about 300px wide and the five labels are unreadable. It is also rendered in a different sans font and style from the ggplot-style bar charts around it. The tile boundary also cuts it across desktop_01/02, but the figure itself is just very short and wide.
  *Fix:* Regenerate at a larger font size (labels at least 11-12px after scaling) and a taller aspect ratio, or replace with a simple two-column list (R stops you / nothing stops you). Match the font and green/rust palette of Figures 13.1 and 13.3.
- **[must-fix, mobile] 13.5 A Silent Failure, all three R code blocks (module05a_mobile_03.png, module05a_mobile_04.png, module05a_mobile_05.png)** -- Long lines (read_csv paths, mutate(Revenue = ...), weighted.mean(...)) are clipped at the right edge of the code block with no visible scrollbar, so the reader cannot tell the line continues.
  *Fix:* Either shorten the long lines in the source (break the read_csv calls and weighted.mean arguments over multiple lines) or enable code-overflow: wrap in _quarto.yml for this site, so mobile shows the complete code.
- **[should-fix, both] 13.4, Figure 13.1 unit comparison bar chart (module05a_desktop_01.png, module05a_mobile_03.png)** -- The chart is rendered narrower than the text column on desktop with small grey axis text (roughly 9-10px), and on mobile the axis labels and value labels drop to about 6px. It also carries an in-plot title ('The same 2025 canola crop, three ways') that duplicates the caption.
  *Fix:* Export at the text-column width with base font size ~14 (theme_minimal(base_size = 14)), drop the in-plot title since the caption already says it, and keep the same sizing for Figure 13.3 so the two bar charts match.
- **[should-fix, both] 13.9 Course Policy, Figure 13.4 Economist-style density chart (module05a_desktop_04.png, module05a_desktop_05.png, module05a_mobile_07.png)** -- This is a screenshot of a third-party graphic in a completely different visual style (red palette, different typography) and it is very tall on desktop (about 900px), dominating the section. On mobile the three panels shrink to ~330px wide and the annotations ('Pupils using AI scored higher...', the source footnote) become unreadable.
  *Fix:* Either redraw the three distributions in the book's own ggplot style at a compact size, or crop to just the exam-score panel (the one the text actually argues from) and link to the full figure. If kept, constrain it to ~70% column width on desktop.
- **[should-fix, both] 13.5 A Silent Failure, 'Show console session' collapsed blocks (module05a_desktop_02.png, module05a_desktop_03.png)** -- Three collapsed console sections hide the output the prose then quotes ($476.79, 1,416 rows/1,051 NA, $571.17). The reader has to expand each to verify the numbers, and the collapsed bar's small-caps label is low-contrast and easy to skip.
  *Fix:* Show at least the first console session expanded (it is short) or put the key output lines inline in the prose as a small code block; keep collapsing only for long transcripts.
- **[consider, both] 13.4 Common Failure Modes, five H3 subsections (module05a_desktop_01.png, module05a_mobile_02.png)** -- Five consecutive heading-plus-two-sentence blocks produce a choppy rhythm; on mobile the long H3 'Numbers produced without a reproducible calculation' wraps to two lines and the headings nearly outweigh the body text.
  *Fix:* Consider shortening the H3s ('Invented functions', 'Assumed column names', 'Stale syntax', 'Unreproducible numbers', 'Wrong geography') or converting the five to a definition-style bold-lead list as used in 13.2.
- **[consider, both] 13.10 The Rest of This Module, 'Other resources' callout and page end (module05a_desktop_05.png, module05a_mobile_07.png)** -- The chapter ends on a single collapsed callout followed by ~130px of empty space before the prev/next links, which reads as an unfinished section. The callout title uses an en-dash while the body text uses '--'.
  *Fix:* Add one sentence after the callout or move it above the 13.10 paragraph so the section ends on prose; standardise the dash in the callout title.
- **[consider, both] 13.3 Understand What You Submit, block quote (module05a_desktop_01.png, module05a_mobile_01.png)** -- The quoted sentence is set in light grey with a thin left rule and is only one line, so it looks like a faded aside rather than the central standard the paragraph builds on.
  *Fix:* Either run the quote inline in the paragraph or darken the blockquote text colour in the theme so it reads as body weight.

## module05b -- GitHub Copilot

On desktop this is a clean, short chapter: good line length, clear heading hierarchy, two small code blocks and one callout break up the prose, and the whole thing fits in about two and a half screens. On mobile it also reads well, apart from the second code block being clipped on the right. The single worst problem is Figure 14.1, which is a broken image (only the browser's missing-image icon shows) sitting above a caption on both desktop and mobile, and it is the chapter's only figure.

- **[must-fix, both] 14.1 Getting Access, Figure 14.1 (module05b_desktop_00.png, module05b_mobile_01.png)** -- The figure image fails to load: a tiny broken-image icon appears centred above the caption 'Figure 14.1: Applying for GitHub Education student benefits.' The chapter's only figure is therefore missing, and the orphaned caption looks unfinished.
  *Fix:* Fix the image path in module05b.qmd (check the file exists in the images folder and is committed; filenames are case-sensitive on GitHub Pages). If no screenshot exists yet, remove the figure block rather than ship a broken one. Once it loads, constrain width to the text column (e.g. fig-width / width="100%").
- **[should-fix, mobile] 14.4 Show the Shape of the Data, second code block (module05b_mobile_03.png)** -- The five-line comment block is cut off on the right ('Risk_Zone, Crop, Var', 'Canola/Rap', 'by crop, we'); the long comment lines overflow the 390px column and rely on horizontal scrolling with no visible cue.
  *Fix:* Shorten and wrap the comment lines at ~50 characters (e.g. split the column list across two comment lines and move the Wheat category to its own line), or enable code-overflow: wrap for this block in _quarto.yml.
- **[consider, mobile] 14.3 Inline completions, first code block (module05b_mobile_02.png)** -- The one-line comment '# Read the yield data and count rows by crop' is clipped at 'count rows' on mobile.
  *Fix:* Shorten to '# Count rows by crop' or split it over two comment lines so it fits the mobile column.
- **[consider, both] 14.6 Checking the Work, 'Other resources' collapsed callout (module05b_desktop_02.png, module05b_mobile_04.png)** -- The chapter ends on a collapsed callout followed by a large block of empty whitespace (~150px desktop, ~100px mobile) before the prev/next navigation; the collapsed box gives no hint of what it holds, and as the last element of the chapter it reads as an afterthought.
  *Fix:* Either expand the callout by default (it is short and is the final content), or add a one-line lead-in sentence before it naming what the resources are, and check the bottom margin of the page content so the gap before the footer nav is consistent with other chapters.
- **[consider, desktop] 14.1 Getting Access, numbered steps and 'Current access note' callout (module05b_desktop_00.png)** -- The section stacks a 5-item numbered list, an info callout, a short paragraph, then the (broken) figure, so the reader meets four different visual blocks in quick succession; the callout's dated note ('paused since April 20, 2026') will also age badly and already looks like a maintenance footnote in the middle of the chapter.
  *Fix:* Move the access-pause note to the end of 14.1 (after 'Apply early…') or into the collapsed resources box, and keep the steps, the figure, and the 'Apply early' advice together as one flow.
- **[consider, both] 14.2 Connecting Copilot to Positron, numbered steps (module05b_desktop_01.png, module05b_mobile_01.png)** -- The chapter uses bold for UI command names ('Authentication: Configure Language Model Providers', 'AI: Create Diagnostic Report', 'GitHub Copilot') but code styling for keyboard shortcuts and plan names are bold too ('Copilot Free', 'Copilot Student'); the mix of bold for three different kinds of thing flattens the emphasis and is inconsistent with Module 2's convention.
  *Fix:* Pick one convention for menu/command names (bold) and reserve it for those; render plan names in plain text and keep code styling for keys and identifiers.
- **[consider, desktop] 14.5 Accepting and Rejecting, bulleted list (module05b_desktop_02.png)** -- Sections 14.5 and 14.6 are both a single sentence, a list, and a single sentence, with no figure or example; after the figure-heavy earlier modules the back half of the chapter is visually monotonous.
  *Fix:* Add one small screenshot of an inline completion (grey ghost text) or a Posit Assistant chat pane in 14.3 or 14.5 so the reader sees what is being described; size it to the text column like other chapters' figures.

## module05c -- Graphing with AI

On desktop this is a clean, short chapter: sensible line length, clear heading hierarchy, one small figure and a single code-style block, no clutter. On mobile the prose reflows well, but the example prompt in 15.3 is rendered as a preformatted block that does not wrap, so half of the text is cut off at the right edge of the screen. That clipped prompt is the worst problem; the rest is minor (a small, faintly rendered figure, a cramped mobile footer nav, and an empty-feeling collapsed resources box at the chapter end).

- **[must-fix, mobile] 15.3 A Useful Prompt Structure, example prompt block (module05c_mobile_03.png; desktop module05c_desktop_01.png)** -- The example prompt is set as a preformatted/code block with hard line breaks. On a 390px screen it is clipped at the right edge ('Crop, Yea', 'recorded Y', 'Show t') and there is no visible scrollbar, so the reader cannot see the full prompt. On desktop it sits with ragged hard-wrapped lines that look like code rather than prose a student would type.
  *Fix:* This is natural-language text, not code. Put it in a blockquote or a callout (e.g. ::: {.callout-note title="Example prompt"}) so it wraps normally on every width. If it must stay monospace, drop the hard line breaks and add `white-space: pre-wrap` for that block.
- **[should-fix, both] 15.1 Figure 15.1, missing vs recorded bar chart (module05c_desktop_00.png, module05c_mobile_01.png)** -- The figure is rendered at roughly 450px wide inside a ~780px text column, with a light-grey 'missing' fill that nearly disappears against the white background, and axis/legend text that is very small (on mobile the tick labels and legend are barely legible). It also looks lower-resolution/soft compared with the crisp page type.
  *Fix:* Render the figure at the text-column width (fig-width ~7 in, fig-dpi 150 or SVG) with base_size 13-14 in the theme, and use a darker grey (e.g. grey60) for 'missing' so the split is visible at a glance.
- **[should-fix, both] 15.4 'Other resources - ggplot2' collapsed callout at chapter end (module05c_desktop_02.png, module05c_mobile_03.png)** -- The chapter ends on a single collapsed callout with a large block of empty whitespace below it before the prev/next nav. Collapsed, it reads as an empty placeholder and the reader has no hint of what is inside.
  *Fix:* Either expand it by default (collapse="false") since it is short, or add a one-line lead-in sentence before it ('The ggplot2 reference pages below cover annotation and labelling arguments.') so the chapter does not end on an empty-looking box.
- **[consider, mobile] 15.1 Figure 15.1 caption (module05c_mobile_01.png)** -- On mobile the caption wraps to two lines directly under a chart whose legend text is smaller than the caption, producing an awkward visual weight inversion; the chart looks like a thumbnail with an oversized caption.
  *Fix:* Same fix as the figure sizing; once the figure fills the column the caption proportions fall into place. Alternatively shorten the caption to 'Rows with and without a recorded yield.'
- **[consider, both] 15.2 The Exercises, numbered H3 headings 1-5 (module05c_desktop_01.png, module05c_mobile_02.png)** -- Five consecutive H3 'N. Title' headings with a paragraph and a bold 'Checks:' paragraph each produce a repetitive, list-like stretch with no visual variety; the manually numbered headings also duplicate Quarto's own section numbering style and wrap awkwardly on mobile ('5. Rebuild a chart from Module / 4').
  *Fix:* Either drop the manual numbers and let the headings be plain, or present the five exercises as a numbered list with bold lead-ins and indented 'Checks' lines, which reads more compactly and avoids the orphaned '4' on mobile.
- **[consider, mobile] Prev/next page navigation footer (module05c_mobile_03.png, module05c_mobile_04.png)** -- The footer nav links ('14 GitHub Copilot' / '16 Making a Workbook Someone Else Can Read') wrap into a cramped three-line cluster with the arrows detached from their labels on the 390px screen.
  *Fix:* Site-level CSS: stack the two nav links vertically on narrow screens (flex-direction: column in .nav-page container below ~500px) or truncate long titles with text-overflow: ellipsis.
- **[consider, mobile] 15.2 intro paragraph listing column names in inline code (module05c_mobile_01.png)** -- Six inline-code column names in one sentence wrap across four lines on mobile, with the shaded code backgrounds forming a choppy, busy block.
  *Fix:* Either list the columns once in plain text separated by commas, or move them into the prompt example below where they already appear.

## module06a -- Making a Workbook Someone Else Can Read

On desktop the chapter is clean and consistent: good line length, clear heading hierarchy, and the learning-objectives callout reads well. It is also visually monotonous, since the chapter is about visual design of workbooks yet contains no figure or screenshot at all; the three example workbooks are hidden in collapsed callouts, so a reader scrolling through sees only prose and bullet lists. On mobile the layout holds up except for the HYPERLINK code block, which is clipped at the right edge with no visible scrollbar. The worst problem is that the three workbook examples, the chapter's only concrete material, are collapsed by default and give no hint of what is inside.

- **[must-fix, both] 16.3 Three Workbooks, Three Audiences -- the three collapsed callouts (module06a_desktop_01.png, module06a_mobile_02.png)** -- The only concrete examples in the chapter (Manitoba elevator capacity, Crop Planning Guide, RM yields) are collapsed one-line callouts with a lightbulb icon. A reader sees three identical thin bars and no indication that an embedded workbook or screenshot is behind them. The prose describes bold headers, borders and layout that the reader cannot see without clicking.
  *Fix:* Open the three callouts by default (collapse="false") or replace them with a static screenshot of each workbook's first sheet above the embed, so the visual contrast between the three files is visible on the page.
- **[must-fix, mobile] 16.6 Hyperlinks -- =HYPERLINK code block (module06a_mobile_04.png)** -- The two-line HYPERLINK example is cut off at the right edge on mobile ("Go to sum", "saskatc", "Dash") with no visible scrollbar, so the URL and second argument are unreadable.
  *Fix:* Break the long URL call across lines (one argument per line) so it fits in ~40 characters, or enable code-overflow: wrap for this block. Consider dropping the URL example to a shorter placeholder URL.
- **[should-fix, both] Whole chapter -- no figures (module06a_desktop_00.png through module06a_desktop_02.png)** -- A chapter about formatting, frozen panes, shading, borders, README sheets and cell locking contains zero images. The Locking Cells workflow (Format Cells > Protection, Review > Protect Sheet) and the Hyperlink dialog are described only in words; the page is three screens of unbroken prose and bullet lists.
  *Fix:* Add two or three screenshots: the Format Cells > Protection dialog with Locked unchecked, a before/after of a sheet with header formatting and frozen panes, and a README sheet example. Size them to the text column width with short captions.
- **[should-fix, both] 16.5 Formatting for Handoff -- nine-item bold-lead bullet list (module06a_desktop_01.png, module06a_mobile_03.png)** -- Nine bold-lead bullets run for most of a screen on desktop and more than a full screen on mobile, right after similar bold-lead lists in 16.1 and 16.2. Three bold-lead lists in a row make the bold leads read as noise and the sections look identical.
  *Fix:* Either move the checklist into the embedded README workbook example (per the project's 'put Excel explanation in the worksheet' rule) or split it into two short groups under sub-subheadings (Structure / Appearance), and reduce bold leads elsewhere.
- **[consider, both] 16.3 sub-subheading 'A table for reading' (module06a_desktop_01.png, module06a_mobile_01.png)** -- The H3 sub-headings (A table for reading, A model for entering assumptions, Data for further analysis) use sentence case while every H2 uses title case, and they sit very close to the preceding paragraph compared with the generous gap above H2s, so the three examples feel cramped relative to the rest of the page.
  *Fix:* Use consistent heading case across levels and add a little top margin to H3 (or rely on the default Quarto spacing by not tightening it), so each example reads as its own block.
- **[consider, desktop] 16.6 Hyperlinks -- desktop code block (module06a_desktop_02.png)** -- The code block has no background shading or border, unlike inline code spans which are shaded, so the two formula lines float in the prose and the continuation indent on the second HYPERLINK argument looks like a stray alignment.
  *Fix:* Give the block the same light grey background as other chapters' code blocks (check the theme's pre styling), and put each argument on its own line with consistent indentation.
- **[consider, mobile] Page footer navigation (module06a_mobile_05.png)** -- The prev/next links wrap to two lines each ('15 Graphing with / AI', '17 Writing a / Report') and are cut at the top of the tile, leaving an awkward near-empty final screen.
  *Fix:* Shorten the nav titles via a short-title in the chapter YAML, or accept it; this is a theme-level issue shared across chapters rather than specific to this one.

## module06b -- Writing a Report

A clean, short, text-only chapter. On desktop the typography, heading hierarchy and the green learning-objectives callout read well, with comfortable line length. On mobile everything reflows without overflow or clipping. The worst problem is that a chapter about tables, charts and captions contains no table, chart or example figure at all, so the page is uniformly prose plus green blockquotes and gives the reader nothing to look at; the blockquotes also use a pale grey-green text that is lower contrast than body text.

- **[should-fix, both] 17.4 Tables and Charts (module06b_desktop_01.png, module06b_mobile_02.png)** -- The section explains how to introduce a chart and write a caption but shows no chart. The 'Figure 3 compares average yield across soil zones' example refers to a figure that does not exist on the page, which reads as a missing image.
  *Fix:* Add one small example chart (e.g. a bar chart of canola yield by soil zone, 2021-2025) with a model caption underneath, placed right after the blockquote, sized to the text column. Alternatively reword the example so it does not name a 'Figure 3'.
- **[should-fix, both] Whole chapter (module06b_desktop_00.png to _02.png)** -- The page is almost entirely prose plus four identical green blockquotes and one callout. With nothing tabular, no code and no figure it is visually monotonous and the blockquotes all look the same whether they are an example opening paragraph, a chart introduction or a method sentence.
  *Fix:* Add one example table (a three-row summary with units in the header) in 17.3 or 17.4, and consider a compact good/bad side-by-side for the 'Write / Do not write' contrast in 17.5 so the examples are distinguishable from each other.
- **[should-fix, both] Example blockquotes, e.g. 17.1 'Canola yields in the black soil zone...' (module06b_desktop_00.png, module06b_mobile_00.png)** -- Blockquote text is set in a pale grey-green that is noticeably lower contrast than the body text, yet these are the model sentences the student is meant to study. On the phone the pale text on the narrow column is hard to read.
  *Fix:* Darken the blockquote colour (or use the body text colour with italics and keep only the left rule green) so example text is at least as legible as the surrounding prose.
- **[should-fix, both] 17.5 Describe the Method, Not the Keystrokes (module06b_desktop_01.png / _02.png, module06b_mobile_03.png)** -- The section has a 'Write:' lead-in and a good example, but no counter-example, so the 'not the keystrokes' half of the heading is never illustrated. The lone word 'Write:' as a paragraph also looks like an unfinished label, and on desktop the example straddles the tile boundary with a good deal of empty space above 17.6.
  *Fix:* Pair the example with a short 'Do not write:' blockquote (e.g. 'We clicked Insert > PivotTable, dragged Yield to Values...') so the two sit together and the label reads as part of a deliberate contrast.
- **[consider, both] 17.7 The Course Project and chapter footer (module06b_desktop_02.png, module06b_mobile_04.png)** -- The chapter ends on two short paragraphs followed by a large blank gap before the prev/next navigation. On mobile the footer links wrap awkwardly ('16 Making a Workbook Someone Else Can Read' over three lines beside '18 Presentations' over two).
  *Fix:* Either expand 17.7 with a brief checklist callout for the project report (matching the learning-objectives box style) or tighten the bottom margin; shorten the previous-chapter title in the nav if Quarto allows a short title.
- **[consider, both] 17.3 Presenting Numbers sub-headings (module06b_desktop_01.png, module06b_mobile_01.png / _02.png)** -- The four H3 sub-headings (Round to the measurement, Supply a comparison, State the units, Report fewer numbers) each carry only one or two sentences, so the headings are nearly as visually heavy as the content and the section looks like a list dressed as sections. They also do not appear in the right-hand TOC, so the hierarchy is invisible from the sidebar.
  *Fix:* Either fold the four into a single bulleted list with bold lead-ins (matching the style already used in 17.2 and 17.6) or give each sub-heading a short worked numeric example so the heading weight is justified.
- **[consider, mobile] Learning Objectives callout (module06b_desktop_00.png, module06b_mobile_00.png)** -- On the phone the numbered list inside the callout uses a number, a check icon and then text indented a long way, leaving a narrow text column and five objectives that each wrap to two or three lines, so the box fills most of a screen.
  *Fix:* Reduce the gap between the check icon and the text on small screens (or drop the number when the check icon is present) so the objectives occupy fewer lines.

## module06c -- Presentations

A short, clean, text-only chapter that renders without breakage on either device: comfortable line length on desktop (~95 characters), consistent heading hierarchy, sensible list spacing, and nothing overflows or is cut off on mobile. The weakness is visual monotony -- seven sections of prose and bullet lists with no figure, example slide, or callout apart from one blockquote and one collapsed resources box, which is conspicuous in a chapter about building slides. The single worst problem is the absence of any visual example: a chapter telling students to make readable, claim-headlined slides never shows one.

- **[should-fix, both] 18.3 Build Slides for Listening (module06c_desktop_00/01, module06c_mobile_01/02)** -- The chapter about slide design contains no image of a slide. The 'claim as headline' and 'readable from the back of the room' rules are asserted in bullets only, so the page is an unbroken run of text from top to bottom.
  *Fix:* Add one small figure: a before/after pair of a single slide ('Yield by soil zone' vs 'Canola yields were 15% higher in the black soil zone'), sized to the text column, with a one-line caption.
- **[should-fix, both] 18.4 Explain a Chart Out Loud (module06c_desktop_01, module06c_mobile_02)** -- The four-step script (Axes, Marks, Pattern, Meaning) narrates a yield-by-soil-zone line chart the reader cannot see. The narration refers to 'the black zone' and 'five years' with no figure to check against.
  *Fix:* Embed the line chart being described (a ggplot of canola yield by soil zone over five years, column width) directly above the numbered list so the steps can be followed against it.
- **[consider, desktop] Whole chapter, section rhythm (module06c_desktop_00/01)** -- Every section follows the same pattern of one intro paragraph, a bold-lead bullet or numbered list, one or two closing paragraphs. With no figures, tables, or callouts, the page reads as visually monotonous and the sections blur together.
  *Fix:* Vary at least one section: turn the five-step structure in 18.2 into a compact two-column table (step / what to say), or put the 18.7 assessment criteria in a callout-note box so it reads as a checklist.
- **[consider, both] 18.6 Questions blockquote (module06c_desktop_01, module06c_mobile_03)** -- The example answer is set as a grey blockquote with a thin left rule and low-contrast grey text, the only non-standard element on the page. It reads as a de-emphasised quotation rather than a model answer the student should copy.
  *Fix:* Render it as a callout-tip or a labelled example box ('Example answer') with normal-contrast text so it stands out as a model rather than receding.
- **[consider, both] Other resources -- presenting collapsed callout (module06c_desktop_02, module06c_mobile_04)** -- The collapsed callout sits alone after the final paragraph with ~120px of empty space beneath it before the prev/next footer, and the en-dash in the title ('resources – presenting') is the only dash style on the page. Collapsed by default, most readers will not open it.
  *Fix:* Either expand it by default (it is presumably a short link list) or tighten the bottom margin; make the title 'Other resources: presenting' to match the chapter's punctuation.
- **[consider, mobile] Learning Objectives box (module06c_mobile_00)** -- On the phone the objectives box runs 430px tall with each objective wrapping to two or three lines inside a heavily indented numbered-plus-checkmark list, leaving a narrow text column of ~230px. It occupies most of the first screen before any content.
  *Fix:* Reduce the list indent inside the box on narrow viewports (drop the number or the checkmark, not both) so objectives wrap to at most two lines.
- **[consider, desktop] Chapter opening paragraphs (module06c_desktop_00)** -- The chapter opens with two short paragraphs, then the Learning Objectives box, then 18.1; the spacing above the 'Learning Objectives' H2 and above '18.1' is larger than between body paragraphs, which makes the top third of the page feel sparse compared with the dense bullet sections below.
  *Fix:* Keep the layout but consider placing the objectives box immediately under the H1 and merging the two opening paragraphs into the 18.1 intro, so the first screen has one block rather than three small ones.
