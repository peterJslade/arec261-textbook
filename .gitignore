# Quarto output
_book/
/site_libs/          # render spill if a build is interrupted
_freeze/
/.quarto/
*.html
!practice/quiz/index.html
!practice/quiz2/index.html
# Source, not render output: injected via include-after-body in _quarto.yml.
!console-fold.html

# Quarto render artifacts
*_files/
*.knit.md

# Archive folder (old single-file version, not published)
_archive/

# OS junk
.DS_Store
Thumbs.db
*.swp
*~

# Python helper artifacts
__pycache__/
*.pyc
.venv/
venv/

# Large re-downloadable raw StatsCan table (60MB) — regenerate via the URL
# in generate_data_module01_statcan.py, then run the cleaner.
practice/data_raw/32100359*.csv
practice/data_raw/32100359-eng.zip

# Editor settings
.vscode/
.idea/

**/*.quarto_ipynb

# Generated per-student tests + roster (student PII — never commit)
module1_tests/
roster*.csv
*.roster.json
