# Section 3 — Manitoba Wheat Variety: ranking (E26–E30 vs N26–N30)

1. E30 — mutate builds a genuinely new quantity (total_bu), then mean and arrange put it to work, and (d) lands the yield-vs-production distinction; full skill sequence, strong pedagogy, right length.
2. N28 — the only question in the block that actually uses `&` with numeric comparisons (E27 only discusses it verbally), and its (c) on farm-count as evidence quality is a real data-literacy point tied to the dataset's suppression theme.
3. E27 — | vs %in% equivalence with a verbal `&` counterfactual is the cleanest test of comparison logic; concise, distinct, and the (c) explanation grades easily from a comment.
4. E29 — sole coverage of select's three forms (naming, `-` dropping, `:` ranges) and of rename; the mechanics are the skill here, so the confirm-the-names step is legitimate rather than busywork.
5. E26 — the gateway question: filter on the logical column, select, and a (c) that checks the module's key misconception (filtering returns a new data frame, nothing is deleted); lighter on skills but the concept earns the slot.
6. E28 — comparison filter, sequential re-filter, arrange desc; solid and gradable, but each skill it uses appears with more purpose elsewhere in the block.
7. N30 — the suppressed-rows flip side (Reported == FALSE) is a distinct move nothing else makes, but its (c) retreads ground E26 already covers and drifts toward an essay prompt.
8. N26 — competent single-municipality filter plus sort, but == on a string and arrange are both covered by E28, and the (c) comment ("what differs between them?") does little work; likely under the 8-minute mark.
9. N29 — near-duplicate of E27 (%in% vs three `==` joined with `|`, same "why %in% scales" moral) with E26's select-and-names bolted on; adds a third variety but no new skill or idea.
10. N27 — structural clone of E30 part for part (mutate a derived column, sort desc, take the mean, comment on what it measures); acres_per_farm is a less interesting quantity than total production, so the copy loses to the original.

RANKING: E30,N28,E27,E29,E26,E28,N30,N26,N29,N27
