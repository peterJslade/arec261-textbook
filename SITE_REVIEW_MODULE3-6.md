# Site review of Modules 3–6 (live pages at agdataanalytics.com)

One reviewer per chapter inspected the rendered page as published from commit `ddfb1ed` on 2026-08-22: curled every image, iframe and download link, counted unresolved cross-references and console transcripts, then read the page as a student would. No screenshots were available; findings are from the fetched HTML.

## Mechanical checks

| Chapter | Images (broken) | Iframes (unreachable) | Downloads (broken) | Unresolved xrefs | Console blocks (with errors) |
|---|---|---|---|---|---|
| module03a | 0 (0) | 0 (0) | 2 (0) | 0 | 4 (0) |
| module03b | 1 (0) | 1 (0) | 5 (0) | 0 | 5 (1) |
| module03c | 0 (0) | 1 (0) | 2 (0) | 0 | 18 (0) |
| module04a | 31 (0) | 0 (0) | 4 (0) | 0 | 0 (0) |
| module04b | 0 (0) | 1 (0) | 1 (0) | 0 | 0 (0) |
| module04c | 12 (0) | 0 (0) | 1 (0) | 0 | 2 (0) |
| module05a | 4 (0) | 0 (0) | 0 (0) | 0 | 3 (1) |
| module05b | 1 (1) | 0 (0) | 1 (1) | 0 | 0 (0) |
| module05c | 1 (0) | 0 (0) | 1 (0) | 0 | 0 (0) |
| module06a | 0 (0) | 3 (0) | 3 (0) | 0 | 0 (0) |
| module06b | 0 (0) | 0 (0) | 0 (0) | 0 | 0 (0) |
| module06c | 0 (0) | 0 (0) | 0 (0) | 0 | 0 (0) |

## Must-fix items

- **module03a** (7.4.1 What double-clicking a CSV does / 7.4.2 Power Query / Text to Columns is for repairs (fig-csv-doubleclick, fig-csv-getdata, fig-csv-text-to-columns)): All three screenshots are HTML comments in module03a.qmd (lines 124-127, 138-141, 180-183) and images/csv_doubleclick.png, csv_getdata.png, csv_text_to_columns.png do not exist (the only csv image in images/ is positron_csv_viewer.png). The prose depends on them: 'the type of each column shown by the icon in its header', 'on the last step set each column's type ... under Advanced a comma as the decimal separator'. A student following along has no picture of the Power Query editor or the Text to Columns dialog. *Fix:* Take the three screenshots (open elevator_tickets.csv by double-click showing A-C; Power Query editor with the ticket_id type menu open; Text to Columns step 3 with the Advanced dialog), save them under images/ with the names already in the commented markdown, and uncomment the three figure blocks.
- **module04b** (Workbook mod04_graphing_excel.xlsx, Data sheet (embedded in 11.1 Start With the Table)): All values on the Data sheet (acres, yield_bu_acre, year) are written as shared strings (cell type t="s"), not numbers. AVERAGEIF ignores text in the average range, so Bar chart!B5:B7 and Line chart!B5:B6 are cached as #DIV/0! (visible in sheet3.xml: <v>#DIV/0!</v>), and the scatter series reads text acres/yield. The prose says "Change a value in the Data sheet and the summary formulas and charts update" -- a student sees three empty charts and a column of errors instead. *Fix:* Regenerate the workbook writing acres, yield_bu_acre and year as numeric cells (in openpyxl, pass int/float rather than str; in the generator script, do not str() the values). Re-upload the fixed file to SharePoint so the embed (sourcedoc 7E79C20B-...) shows the corrected copy, and re-check that Bar chart!B5 evaluates to a number.
- **module04c** (12.3 geom_line(): change over time, Figure 12.3 (fig-r-line)): The rendered line chart has x-axis ticks at 2024.00, 2024.25, 2024.50, 2024.75, 2025.00 (year is a double) and a y-axis from 45.77 to 45.98, so a 0.2 bu/acre change looks like a dramatic rise. The axis labels are raw column names (mean_yield, year). It is the only line chart in the book and it is a misleading one. *Fix:* In the chunk add `scale_x_continuous(breaks = c(2024, 2025))` (or `mutate(year = factor(year))` plus `group = 1` in aes) and `labs(x = "Year", y = "Mean yield (bu/acre)")`, and consider `expand_limits(y = c(40, 50))` so the y range is honest. Alternatively switch the example to monthly mean yield by seeding_date so there are more than two points.
- **module04c** (Learning Objectives, item 6): Renders as "save a plot with ggsave()." with a lowercase first letter; every other objective is capitalised. *Fix:* Capitalise: "Save a plot with `ggsave()`."
- **module05a** (Figure 13.4 (fig-ai-penalty, images/ai_learning_penalty.png) in 13.9 Course Policy): The image is a third-party published graphic (newspaper-style three-panel density chart with its own source line 'Source: ... by D. Strömberg et al., preprint June 2026 *26,811 pupils aged 12-18'), not a figure from the paper. The caption credits only Strömberg, Lei and Wu, so the actual publisher is uncredited and the reproduction rights are unclear. It is also the heaviest asset on the page (688 KB, 1456x1742 px) and renders as a very tall full-width image on mobile. *Fix:* Either redraw the three panels in R/ggplot from the paper's reported numbers (consistent with fig-units/fig-join-loss styling) or replace with a link to the original graphic and credit the publisher in the caption. If the image stays, downscale to ~900 px wide and compress (pngquant/webp) to under 150 KB.
- **module05b** (Figure 14.1 (#fig-copilot-pack), end of 14.1 Getting Access): images/copilot_student_pack.png returns HTTP 404 on agdataanalytics.com and the file is absent from textbook_261/images/ and _book/images/. The page shows a broken image icon with a numbered figure caption under it. *Fix:* Add a screenshot of the GitHub Education benefits page as textbook_261/images/copilot_student_pack.png, or delete the figure line (module05b.qmd line 39) until a screenshot exists. Nothing in the prose references @fig-copilot-pack, so removing it breaks no cross-reference.

Totals: 6 must-fix, 32 should-fix, 48 consider.

## module03a -- Getting Data

The page renders cleanly: no broken images, links or cross-references, headings match the TOC, the practice CSVs and all three external URLs (Saskatchewan dashboard CSV, Open-Meteo request) return 200. The worst problem is that the three screenshots the Excel sections are written around (double-click damage, Power Query type icons, Text to Columns step 3) are still HTML-commented placeholders, so 7.4.1, 7.4.2 and "Text to Columns is for repairs" describe dialogs the student cannot see, and the chapter has no embedded workbook at all.

- **[must-fix] 7.4.1 What double-clicking a CSV does / 7.4.2 Power Query / Text to Columns is for repairs (fig-csv-doubleclick, fig-csv-getdata, fig-csv-text-to-columns)** -- All three screenshots are HTML comments in module03a.qmd (lines 124-127, 138-141, 180-183) and images/csv_doubleclick.png, csv_getdata.png, csv_text_to_columns.png do not exist (the only csv image in images/ is positron_csv_viewer.png). The prose depends on them: 'the type of each column shown by the icon in its header', 'on the last step set each column's type ... under Advanced a comma as the decimal separator'. A student following along has no picture of the Power Query editor or the Text to Columns dialog.
  *Fix:* Take the three screenshots (open elevator_tickets.csv by double-click showing A-C; Power Query editor with the ticket_id type menu open; Text to Columns step 3 with the Advanced dialog), save them under images/ with the names already in the commented markdown, and uncomment the three figure blocks.
- **[should-fix] 7.4 Getting Data Into Excel (whole section)** -- The chapter contains no .workbook-callout and no iframe, unlike module03b and module03c which each have one. The Power Query and Text to Columns walkthroughs are entirely prose ("Click the icon to change ticket_id, permit_book and lot_code to Text"), which is the pattern CLAUDE.md says to move into an embedded workbook.
  *Fix:* Add a mod03_getting_data.xlsx with the loaded tickets query (typed columns), the semicolon file split by Text to Columns, and the Open-Meteo Power Query table, then embed it in a workbook-callout after 7.4.2 and trim the step-by-step prose.
- **[should-fix] 7.5.2 Check what R guessed, 7.5.4, 7.5.7 Calling an API (R output blocks)** -- The four R transcripts (problems() tibble, the `...2` header tibble, `[1] 200`, the 5x2 rain tibble) are plain untyped fences, so they render as bare <pre> blocks with no console styling and no collapsible fold. The book's console-fold script only wraps .cell-output and .console-transcript elements, and this chapter has neither, so its output looks different from chapters where output is folded.
  *Fix:* Wrap each output fence in `::: {.console-transcript}` ... `:::` (or give the fence the class the script targets) so it gets the same collapsed console treatment as the rest of the book.
- **[consider] Right-hand TOC: 'Four ways to get it', 'File formats', 'Text to Columns is for repairs'** -- Three unnumbered h3s sit between numbered sections (7.2, then 'Four ways to get it', then 7.3; 7.4, 'File formats', 7.4.1; 7.4.2, 'Text to Columns is for repairs', 7.5). In the TOC they read as orphaned entries and break the numbering sequence.
  *Fix:* Either drop {.unnumbered} so they become 7.2.1, 7.4.1 (shifting the others) and 7.4.3, or demote them to bold lead-in phrases in the paragraph so they leave the TOC.
- **[consider] Chapter title '7 Getting Data' and in-text references like 'Section 7.5.6'** -- Module 3a renders as chapter 7 because every sub-file counts as a Quarto chapter, so cross-references say 'Section 7.5.6' while the sidebar and course call it Module 3. Students reconcile two numbering systems.
  *Fix:* Book-wide: set `number-depth`/`number-sections: false` for sub-module files or use Quarto's `chapters` with `part` grouping so section numbers follow module numbers (3a.5.6) or are suppressed.
- **[consider] 7.5.7 Calling an API, final paragraph** -- The inline code `writeLines(resp_body_string(weather_response), "data/raw/saskatoon_2024-05.json")` is 85 characters inside a prose sentence; it wraps mid-argument on a normal-width column and is hard to copy.
  *Fix:* Move it to its own r code block after the sentence 'Save the raw response too, for the same reason as the raw CSV:'.
- **[consider] Collapsed callout 'Other resources — getting data'** -- Callout title uses a typeset em-dash, which is the tell CLAUDE.md flags; everywhere else in the chapter the dash convention is followed. It is the only em-dash in the chapter body (the other two are in the site navbar).
  *Fix:* Rename to 'Other resources: getting data' or 'Other resources -- getting data'.
- **[consider] 7.5.2 Check what R guessed, paragraph on grain_deliveries_messy.csv** -- grain_deliveries_messy.csv is named in backticks but not linked, although it exists at practice/data/grain_deliveries_messy.csv and the two tickets files earlier in the chapter are linked downloads. A student who wants to reproduce problems() has no link.
  *Fix:* Link it: [`grain_deliveries_messy.csv`](practice/data/grain_deliveries_messy.csv), and note the code's path `data/grain_deliveries_messy.csv` assumes it was saved into the project's data/ folder.

## module03b -- Merging Data

The page renders cleanly: the one image, the SharePoint embed, all three data links and the workbook download all return 200, every cross-reference resolves, and all five console transcripts fold correctly. The worst problem is the first R transcript, which opens with about 35 lines of tidyverse/janitor attach messages and three read_csv column-spec blocks before reaching the glimpse() output the prose asks the student to look at; on a phone the long "── Attaching core tidyverse packages ──" rules also force horizontal scrolling inside the fold.

- **[should-fix] 8.3 Merging in R, first console block (library/read_csv/glimpse)** -- The transcript prints the full tidyverse attach banner, the conflicts block, the janitor masking message, and three read_csv column-specification messages (about 35 lines) before the three glimpse() outputs the prose says to inspect. The dashed rules ('── Attaching core tidyverse packages ────') are ~136 characters wide and overflow the transcript on narrow screens.
  *Fix:* In the console() call add show_col_types = FALSE to the three read_csv() calls and wrap the library() lines in suppressPackageStartupMessages(), or split the chunk so the package loading is a separate (unfolded or hidden) cell and the transcript starts at the glimpse() calls.
- **[should-fix] 8.2 Merging in Excel, .excel-embed iframe** -- The Merge sheet spans A1:L42 (Year, RM, six crops, station, precip, WRONG column, FORMULATEXT column) plus a long explanation paragraph in A2. The embed is forced to 100% width (max 54rem) and 700px tall, so at typical widths only columns A-H are visible and the three columns the prose talks about (I, J, K) sit off-screen; the embed has scrolling="no" so the student must rely on the embedded viewer's own scrollbars.
  *Fix:* Either freeze/hide the six crop columns in the Merge sheet so the key and lookup columns are visible first, or move the lookup columns left of the crop yields, or set ActiveCell to a cell near column I (e.g. 'Merge'!F1) so the viewer opens scrolled to the lookups.
- **[consider] Page heading and section numbers ("8 Merging Data", 8.1-8.4) vs sidebar "Module 3"** -- The chapter renders as Chapter 8 with sections 8.1-8.4, and the in-text reference to @sec-categories renders as 'Section 9.3.6', while the sidebar and all course materials call this Module 3. The numbering is consistent across the book, but a student reading 'Module 3' and seeing 'Chapter 8' will wonder if they are on the wrong page.
  *Fix:* Set number-depth or `number-sections: false` for chapter files and rely on module labels, or accept the book-wide numbering and leave as is (a global decision, not specific to this chapter).
- **[consider] 8.3 The row-count trap console block** -- The transcript contains a dplyr Warning ('Detected an unexpected many-to-many relationship'). The prose does acknowledge it ('dplyr warns about the unexpected many-to-many relationship'), so this is counted but not a defect. The fold summary for it reads 'Show console session' rather than the amber 'Show console message' styling, because the warning is inside a console() transcript rather than a cell-output-stderr block.
  *Fix:* No change required; optionally add a short note before the fold telling the student to expect a warning so the amber accent is not needed.
- **[consider] 8.2 Merging in Excel, prose after the embed ('Power Query ... Data > Get Data ... Merge Queries')** -- The Power Query paragraph describes menu mechanics in prose with no worksheet, screenshot, or link, and nothing in the embedded workbook demonstrates it. Per the project's own guidance, Excel mechanics belong in the workbook; as rendered it is an orphan paragraph a student cannot act on.
  *Fix:* Either cut the paragraph to one sentence pointing to Power Query as the repeatable alternative, or add a 'Power Query' sheet (or a screenshot under images/) that shows the merged query output.
- **[consider] 8.1 The Three Tables, data links** -- The links point to practice/data/*.csv (all 200) but the R code reads from data/rm_yields_2015_2024.csv etc. A student following along will download into a different folder name than the one the transcript uses.
  *Fix:* Add one sentence saying the code assumes the three CSVs are saved in a data/ folder inside the project, or change the read_csv paths to match where Module 3a told students to put files.
- **[consider] Figure 8.1 (images/join_types.svg)** -- The SVG has a hard-coded white background rect and dark #24302A text, so in the site's dark theme it appears as a bright white panel. It is legible, just visually jarring; the caption is long (four sentences) and is repeated verbatim in the lightbox title attribute.
  *Fix:* Remove the white background rect from the SVG (or make it transparent) and trim the caption to the first sentence, moving the three case descriptions into the bullet list that already follows the figure.

## module03c -- Cleaning Data

The live page matches the local _book render (only the Quarto version string differs): all 18 code listings have a matching console transcript, the one workbook embed loads (302 -> 200), both file links return 200, and there are no unresolved cross-references, images, or stray markup. The worst problem is the opening: before the student reaches the Learning Objectives they hit a ~50-line transcript of tidyverse/janitor startup chatter (attach banner, conflicts, masking, column-spec messages), which buries the two glimpse() lines the prose actually talks about.

- **[should-fix] Chapter intro, first console block (before Learning Objectives)** -- The first transcript is dominated by package startup noise: the tidyverse attach banner with version numbers, the Conflicts block, janitor's 'objects are masked from package:stats' message, and the read_csv column-specification message (which repeats in the rm_yields and canola_trial transcripts). Roughly 35 of the 50 lines are not about the data, and the banner has a 101-character line that forces horizontal scrolling. The prose only refers to the two glimpse() lines at the bottom. This block also sits above the Learning Objectives, so the chapter opens with a wall of console output.
  *Fix:* In the console() call wrap the library() lines in suppressPackageStartupMessages() (or load the packages in the hidden setup chunk and drop them from the transcript), and add show_col_types = FALSE to the three read_csv() calls. Consider moving the Learning Objectives block above the example so the chapter starts the way the other chapters do.
- **[should-fix] 9.3.1 Reshaping / 9.3.8 Missing Values (code reading data/rm_yields_2015_2024.csv and data/canola_trial.csv)** -- Only grain_deliveries_messy.csv is linked from this page. The code also reads rm_yields_2015_2024.csv and canola_trial.csv from data/, but neither is linked here (rm_yields is linked in module03b and canola_trial in module02b/07a). A student working this chapter cannot tell where to get these two files.
  *Fix:* At the first use of each file add a link to practice/data/rm_yields_2015_2024.csv and practice/data/canola_trial.csv (both return 200 on the live site), e.g. in the intro sentence listing the files the chapter uses.
- **[should-fix] 9.2 Cleaning with formulas, workbook-callout** -- The prose after the embed says 'The filter dropdown is useful when checking categories because it lists each distinct value', but the iframe src has wdAllowInteractivity=False, so filters cannot be used in the embedded view. The Cleaning sheet is also 11 columns wide (column widths sum to roughly 140 characters, with the FORMULATEXT column K at width 28), wider than the 54rem max embed, so the formula column the prose points to is off-screen until the student scrolls inside the embed.
  *Fix:* Say 'In the downloaded workbook, the filter dropdown...' or set wdAllowInteractivity=True in the embed URL; and either set ActiveCell to the G1 area so the cleaning columns are visible on load, or narrow columns A-F / shorten column K.
- **[should-fix] 9.2 Cleaning with formulas, workbook-callout ('Open online' link and embed)** -- The HTML comment in the source notes the public view-only link expires 2026-11-20 under the 90-day policy. AREC 261 runs to December 2026, so the embed and the 'Open online' link will stop working in the last weeks of the term. (curl on the 'Open online' link already returns 302 -> 302 -> 403, which is consistent with SharePoint requiring a browser session; confirm it opens in an incognito window.)
  *Fix:* Put a reminder in the course calendar to regenerate the share link in mid-November, or move the workbook to a location without the 90-day expiry. Keep the 'Download this workbook' link as the primary path since it is served from the site itself.
- **[consider] 9.3.6 Inconsistent Categories, last code block (case_when synonyms)** -- The case_when() block reconciles 'Canola/Rapeseed' and 'Soybean', neither of which exists in grain_deliveries_messy.csv, so the transcript shows the assignment and nothing else. The student cannot see it do anything, and the preceding count() already shows the crop column is clean.
  *Fix:* Either add a 'Canola/Rapeseed' ticket to the messy CSV so the block has a visible effect (show count() after it), or state in the prose that the example is illustrative and the file has no synonyms left.
- **[consider] Heading numbering under 9.1 and 9.2 (TOC)** -- Within section 9.2, 'Reshaping with Power Query' is numbered 9.2.1 while its sibling h3s 'Cleaning with formulas' and 'Cleaning with Power Query' are unnumbered; under 9.1 the only h3 is unnumbered. The right-hand TOC therefore shows one numbered and two unnumbered entries at the same level, which reads like an error. All source headings do appear in the TOC.
  *Fix:* Pick one convention for the h3s in 9.2: give the two cleaning subsections ids and numbers (matching 9.3.x), or add {.unnumbered} to 'Reshaping with Power Query' (it has #sec-unpivot, so keep the id while unnumbering).
- **[consider] theme.scss .excel-embed and .quarto-dark block** -- The .quarto-dark overrides cover .objectives and .console-fold but not .excel-embed, which keeps its pale-sky background and light border in dark mode, so the workbook panel stands out as a light box. Only visible if the dark toggle is enabled for the book.
  *Fix:* Add .excel-embed { background: #1f262b; border-color: #33404a; } under .quarto-dark, or leave as is if the book never exposes a dark toggle.

## module04a -- Graphing Best Practices

The page renders cleanly: all 31 images resolve (200), no unresolved cross-references, headings and right-hand TOC match the source exactly, and the chapter has no workbook embed or console transcripts by design. Note the first fetch (before the 18:35 UTC deploy) returned stale content, so the site had been briefly serving the wrong page under module04a.html. The worst remaining problem is caption lettering: Quarto adds "(a)"/"(b)" to every sub-figure while the fig-subcap text and several in-chart titles also carry "A."/"B.", so students see captions like "(a) A. Axis starting at 100" and, for the cluttered and pie panels, the letter three times with inconsistent wording.

- **[should-fix] All layout-ncol figures (fig-truncated, fig-clutter, fig-pattern, fig-fertilizer, fig-dual, fig-bar-orientation, fig-pie, fig-extras-1, fig-extras-2)** -- Rendered sub-captions read "(a) A. Axis starting at 100", "(b) B. Axis starting at zero" etc. Quarto supplies the (a)/(b) lettering automatically, so the "A."/"B." in fig-subcap duplicates it on every two-panel figure in the chapter.
  *Fix:* Drop the "A. "/"B. " prefixes from every fig-subcap entry in module04a.qmd (e.g. "Axis starting at 100"). The prose that says "In panel A" still works because Quarto labels the panel (a).
- **[should-fix] fig-clutter panel A/B and fig-pie panel A/B (R/chart_types.R labs(title=...))** -- These panels also carry an in-chart title ("A. Cluttered", "B. Decluttered", "A. As a pie chart"), so the letter appears three times and the wording disagrees with the subcap ("B. Reduced" vs in-chart "B. Decluttered"; "A. Pie" vs "A. As a pie chart"). The same section tells students not to repeat the caption inside the chart.
  *Fix:* Set title = NULL for the panel plots in R/chart_types.R (fig_clutter, fig_pie) and let the Quarto sub-captions carry the label; or pass the subcap text through and keep exactly one of the two.
- **[should-fix] fig-line ("Line charts: show change over time")** -- The chart renders with an in-chart title "Average yield by year" directly above the Quarto caption "Figure 10.13: Average crop yield by year...". This is the exact duplication the "Use descriptive labels" section tells the reader to avoid.
  *Fix:* Remove title = "Average yield by year" from the line-chart function in R/chart_types.R (line ~101) so the figure caption is the only title.
- **[should-fix] "Label lines directly" image pair (variety_shares_direct_labels.png / variety_shares_legend.png)** -- This is the only two-panel comparison in the chapter that is not a numbered figure: it renders as two bare images captioned "A. Labels placed beside the lines." and "B. The same lines identified with a legend." with no "Figure 10.x" line, so it cannot be cross-referenced and looks inconsistent with the neighbouring figures. The two PNGs are also 2700x1650 and ~450 KB each, heavier than anything else on the page.
  *Fix:* Wrap the pair in ::: {#fig-direct-labels layout-ncol=2} with a fig-cap, drop the A./B. prefixes, and downscale the two PNGs to ~1200 px wide.
- **[consider] fig-fertilizer panel A ("Annotate what needs explanation")** -- In the rendered 480-px-wide panel the annotations collide: "steady climb begins" sits on top of the line, and "COVID low" overprints "long slide as new capacity comes on". Clutter is the point of panel A, but overlapping text reads as a rendering bug rather than a deliberate example.
  *Fix:* Nudge the "COVID low" and "steady climb" annotation positions in R/chart_types.R so the labels are crowded but legible, or note in the prose that the labels collide.
- **[consider] fig-dual ("Add variables carefully")** -- Neither panel identifies which line is yield and which is acres except by matching the axis-title colour to the line colour (green solid vs brown dashed). At the rendered size a reader has to infer this; a student copying the pattern would not know to colour the axis titles.
  *Fix:* Add direct end-of-line labels ("Yield", "Acres") to both panels, consistent with the "Label lines directly" advice a few paragraphs earlier.
- **[consider] Page header / sidebar ("Module 4: Graphing" then "10 Graphing Best Practices")** -- The chapter is numbered 10 while the part label says Module 4, and figures are numbered 10.1-10.20. Chapter numbers run across all three sub-chapters per module, so the module and chapter numbers never agree anywhere in the book; it is visible here because the breadcrumb and h1 sit side by side.
  *Fix:* Either accept the mismatch or set number-depth / chapter numbering off (number-sections: false for chapters, keep figure numbering via fig-prefix) so students see only the module labels.
- **[consider] Live deploy timing (first fetch of module04a.html)** -- The first fetch at ~18:35 UTC returned the content of a different chapter ("7 Getting Data", then "11 Graphing in Excel") under the module04a.html URL before the Pages deploy settled; the previous Render and Publish run (ef43ff1) had failed, so the site was serving a stale half-updated build after the file renames.
  *Fix:* No action needed now that ddfb1ed deployed successfully, but after file renames check the Render and Publish action is green before announcing links, since a failed render leaves renamed pages pointing at old content.

## module04b -- Graphing in Excel

The page itself renders cleanly: headings and TOC match the source, the cross-reference resolves to "Section 10.1", the download link and the SharePoint embed both return 200, and there is no raw markup, no images, no console blocks. The worst problem is in the embedded workbook, not the HTML: every number on the Data sheet is stored as text, so the AVERAGEIF cells the bar and line charts read from are cached as #DIV/0! and the three charts the prose describes are empty when a student opens the file or the embed.

- **[must-fix] Workbook mod04_graphing_excel.xlsx, Data sheet (embedded in 11.1 Start With the Table)** -- All values on the Data sheet (acres, yield_bu_acre, year) are written as shared strings (cell type t="s"), not numbers. AVERAGEIF ignores text in the average range, so Bar chart!B5:B7 and Line chart!B5:B6 are cached as #DIV/0! (visible in sheet3.xml: <v>#DIV/0!</v>), and the scatter series reads text acres/yield. The prose says "Change a value in the Data sheet and the summary formulas and charts update" -- a student sees three empty charts and a column of errors instead.
  *Fix:* Regenerate the workbook writing acres, yield_bu_acre and year as numeric cells (in openpyxl, pass int/float rather than str; in the generator script, do not str() the values). Re-upload the fixed file to SharePoint so the embed (sourcedoc 7E79C20B-...) shows the corrected copy, and re-check that Bar chart!B5 evaluates to a number.
- **[should-fix] 11.1 Start With the Table, .workbook-callout SharePoint iframe (source comment line 30)** -- The source comment says the public view-only link expires 2026-11-20 under the 90-day policy. The Fall 2026 term runs to December, so the embed (and the Open online link) will stop working for students in the last weeks of the course and through the final exam period.
  *Fix:* Set a calendar reminder to regenerate the embed link before 2026-11-20, or ask IT for a non-expiring anonymous link for the course site; update the comment with the new expiry.
- **[should-fix] 11.1 Start With the Table, "Open online" link** -- The sharing URL (https://usaskca1-my.sharepoint.com/:x:/g/personal/pjs998_usask_ca/IQALwnl-...?e=xj194K) returns HTTP 403 to an unauthenticated request, while the embedview iframe for the same document returns 200. A student not signed into USask SharePoint, or one using a personal account, will hit an access-denied page rather than the workbook.
  *Fix:* Test the link in a private browser window; if it prompts for sign-in, replace it with the 'Anyone with the link' view URL generated alongside the embed code, or drop the Open online link and rely on the download link plus the embed.
- **[should-fix] 11.1 Start With the Table, embedded workbook** -- The workbook's active sheet is Data, so the embed opens on 60 rows of raw field records; the three chart sheets the chapter is about (and the README with its 'what to notice' table) are only visible by clicking the sheet tabs at the bottom of a 700px frame with scrolling="no". The prose says 'look at the table beside each chart' but the first thing rendered is not a chart.
  *Fix:* Save the workbook with README or Bar chart as the active sheet (wb.active = wb['Bar chart'] before saving), and add one sentence to the prose telling the student the three chart sheets are tabs along the bottom of the embed.
- **[consider] 11.1 Start With the Table, .workbook-callout div** -- theme.scss styles .workbook-callout as a blue collapsible callout (.workbook-callout div.callout.callout-tip ...), but the source wraps only an .excel-embed div and a link line in it, with no callout inside. The rendered result is a bare iframe box followed by a loose 'Download this workbook · Open online' line with no heading, which reads as a stray paragraph after the embed. Same pattern as module01a, so either the SCSS rules are dead or the chapters are missing the inner callout.
  *Fix:* Either wrap the embed and links in the collapsible tip callout the SCSS expects (::: {.callout-tip collapse="true" title="Workbook: mod04_graphing_excel.xlsx"}), or delete the unused .workbook-callout callout rules from theme.scss and give the download line a small caption style so it does not look orphaned.
- **[consider] 11.1 Start With the Table, iframe element** -- The iframe carries scrolling="no" and a fixed 700px height (480px under the mobile media query). The Bar chart and Line chart sheets place the chart at column E row 4 at 15cm x 7.5cm beside a table plus a 'Check / Why / In this chart' notes block; at 480px on a phone the notes block and the bottom of the chart are cut off with no way to scroll inside the frame.
  *Fix:* Remove scrolling="no" from the iframe (the SharePoint viewer handles its own scrollbars), or raise the mobile height in theme.scss so the whole chart sheet fits.
- **[consider] 11.3 Titles, Axes and Legends, line chart paragraph and Line chart sheet** -- The Data sheet contains only two years (2024, 2025), so the 'line chart of mean yield by year' is two markers joined by one segment. The prose about a narrower vertical range for 'positions connected through time' and about direct labels for 'a few lines' is illustrated by a chart that barely shows a trend, and the sheet title asserts 'changed little between 2024 and 2025' from two points.
  *Fix:* Extend the synthetic data to four to six years so the line chart has a visible shape, or change the line-chart example to something in the data with more time points (e.g. mean yield by seeding month) and adjust the sheet title.
- **[consider] 11.3 Titles, Axes and Legends, cross-reference '(Section 10.1)'** -- The reference renders as 'Section 10.1', which is correct but opaque: chapter numbering runs across the whole book so 10.1 is General Principles in the previous chapter of Module 4, and a student has no cue that it is one page back rather than somewhere in Module 10.
  *Fix:* Keep the Quarto link but add the name: 'measured from a baseline (General Principles, @sec-chart-principles)', or rely on the inline link text only.

## module04c -- Graphing in R

The page renders cleanly: all 12 figure images load, the single data link resolves, no unresolved cross-references, both console() transcripts fold into details elements correctly, and the TOC matches the source headings (12.1-12.7 plus the unnumbered geom subsections). The worst problem is the geom_line figure (Figure 12.3): with only two years on a continuous axis the x-axis reads 2024.00, 2024.25 ... 2025.00 and the y-axis spans 45.77-45.98, so a 0.2 bu/acre difference is drawn as a steep line, which teaches a bad chart in the section meant to show a good one.

- **[must-fix] 12.3 geom_line(): change over time, Figure 12.3 (fig-r-line)** -- The rendered line chart has x-axis ticks at 2024.00, 2024.25, 2024.50, 2024.75, 2025.00 (year is a double) and a y-axis from 45.77 to 45.98, so a 0.2 bu/acre change looks like a dramatic rise. The axis labels are raw column names (mean_yield, year). It is the only line chart in the book and it is a misleading one.
  *Fix:* In the chunk add `scale_x_continuous(breaks = c(2024, 2025))` (or `mutate(year = factor(year))` plus `group = 1` in aes) and `labs(x = "Year", y = "Mean yield (bu/acre)")`, and consider `expand_limits(y = c(40, 50))` so the y range is honest. Alternatively switch the example to monthly mean yield by seeding_date so there are more than two points.
- **[must-fix] Learning Objectives, item 6** -- Renders as "save a plot with ggsave()." with a lowercase first letter; every other objective is capitalised.
  *Fix:* Capitalise: "Save a plot with `ggsave()`."
- **[should-fix] 12.5 Labels and Themes, Figure 12.11 (fig-r-polished)** -- The subtitle string "2024--2025" is an R string literal, so the chart shows a literal double hyphen: "Sixty hypothetical Saskatchewan fields, 2024--2025". The `--` convention is for prose, not code.
  *Fix:* Change the subtitle in the code to "2024-2025" (or a real en-dash character inside the string).
- **[should-fix] 12.4 Mapping and Setting Aesthetics, fig-r-map-set** -- The chunk has fig-subcap but no fig-cap, so Quarto renders two independent figures numbered "Figure 12.9: A. Colour mapped to region" and "Figure 12.10: B. One colour set for all points" instead of one figure with (a)/(b) panels; the hand-typed A./B. prefixes then duplicate the numbering.
  *Fix:* Add `#| fig-cap: "Mapping colour to a variable versus setting it to a constant."` and drop the "A. " / "B. " prefixes from the subcaptions so Quarto produces Figure 12.9 (a) and (b).
- **[consider] Opening console block (library(tidyverse) / read_csv / glimpse)** -- The folded transcript opens with the full tidyverse attach banner and conflicts block (80-character box-drawing lines, "dplyr::filter() masks stats::filter()") plus the read_csv column-specification message, none of which the prose mentions. The code also reads "data/field_yields.csv" while the link above it points to practice/data/field_yields.csv, so a student who downloads the file has to infer the data/ folder convention.
  *Fix:* Either add `show_col_types = FALSE` and suppress the startup banner (suppressPackageStartupMessages) in the console() call, or add one sentence after the block saying the banner and column message are normal. State where to save the CSV (a data/ folder in the project) in the sentence that links it.
- **[consider] All figure chunks (fig-r-first-scatter through fig-r-facet)** -- None of the 12 <img> tags has an alt attribute; the chunks have fig-cap but no fig-alt, so the figures are invisible to screen readers.
  *Fix:* Add `#| fig-alt:` to each chunk (one short description each).
- **[consider] Chapter heading and page title ("12  Graphing in R")** -- The chapter renders as "12 Graphing in R" under the sidebar part "Module 4: Graphing", and all figures are numbered 12.x; the book-wide sequential chapter number collides with the module numbering students use (Module 12 is a different module). This is a _quarto.yml-level issue, not specific to this file.
  *Fix:* Set `number-depth`/`number-sections: false` for chapters in _quarto.yml, or label chapters with the module id (e.g. `{{< meta module >}}`) so the heading reads "4c Graphing in R".
- **[consider] 12.7 Saving Plots console block** -- The transcript shows ggsave() producing no output, which is correct, but the rendered page gives no confirmation that a file was written, and the path "output/region_boxplot.png" assumes an output/ folder exists (ggsave errors if it does not).
  *Fix:* Add a sentence after the block noting that ggsave() prints nothing on success and that the output/ folder must exist first, or add `dir.create("output", showWarnings = FALSE)` to the example.

## module05a -- AI Tools

The page renders cleanly: all four images load, the three console transcripts fold correctly, the cross-reference to module05b resolves, and there is no stray markup, LaTeX or unresolved reference. The live site was rendered with Quarto 1.10.18 (local _book is 1.8.26) but the HTML is otherwise identical. The worst problem is the Strömberg figure: it is a reproduction of a newspaper-style graphic (Economist-format chart, 688 KB, 1456x1742) credited only to the paper, which is both an attribution problem and the heaviest asset on the page; after that, the first console transcript opens with the tidyverse "Conflicts" banner (✖ masks lines, 101-character rules) which the prose never mentions and which a student predicting the output will not expect.

- **[must-fix] Figure 13.4 (fig-ai-penalty, images/ai_learning_penalty.png) in 13.9 Course Policy** -- The image is a third-party published graphic (newspaper-style three-panel density chart with its own source line 'Source: ... by D. Strömberg et al., preprint June 2026 *26,811 pupils aged 12-18'), not a figure from the paper. The caption credits only Strömberg, Lei and Wu, so the actual publisher is uncredited and the reproduction rights are unclear. It is also the heaviest asset on the page (688 KB, 1456x1742 px) and renders as a very tall full-width image on mobile.
  *Fix:* Either redraw the three panels in R/ggplot from the paper's reported numbers (consistent with fig-units/fig-join-loss styling) or replace with a link to the original graphic and credit the publisher in the caption. If the image stays, downscale to ~900 px wide and compress (pngquant/webp) to under 150 KB.
- **[should-fix] 13.5 A Silent Failure, first console transcript (library(tidyverse) call)** -- The folded transcript opens with the full tidyverse attach banner: two '── Attaching core tidyverse packages ──' and '── Conflicts ──' rule lines of 101 characters, plus '✖ dplyr::filter() masks stats::filter()' and '✖ dplyr::lag() masks stats::lag()'. The prose says nothing about these messages; in a chapter about recognising worrying output, a student who clicks 'Show console session' sees red X marks first. The 101-char rule lines also wrap or scroll on narrow screens (the transcript pre is 0.88em but the rules are still the longest lines in the chapter).
  *Fix:* In the console() block call `suppressPackageStartupMessages(library(tidyverse))` or add `show_col_types = FALSE` and load only dplyr/readr, so the transcript starts at the read_csv lines. Alternatively keep the banner and add one sentence after the code saying the 'masks' lines are normal startup messages, not errors.
- **[should-fix] Figure 13.1 (fig-units) in 'Assumptions from another place'** -- The in-chart x-axis label 'The same 2025 canola crop, three ways' duplicates the Quarto caption 'Figure 13.1: The same 2025 canola yield reported in three units.' The reader sees the same sentence twice, once inside the plot and once under it. The axis also has no unit label (it is a mixed-unit bar length), so the axis title is doing caption work rather than axis work.
  *Fix:* In R/ai_failures.R fig_units(), set `labs(x = NULL)` or `x = "value in the stated unit"` and let the figure caption carry the description.
- **[should-fix] Figure 13.3 (fig-join-loss) in 13.5 A Silent Failure** -- The chart labels the crops 'Canola' and 'Spring wheat', but the transcript immediately above it prints the count table with 'Canola/Rapeseed' and 'Wheat - Hard Red Spring' (the names in the yield file, which is the whole point of the example). The legend also says 'silently dropped', while the prose deliberately avoids 'silently' and says 'have no price'. A student matching figure to transcript has to re-map the names themselves.
  *Fix:* In fig_join_loss() use the yield-file crop names on the y axis (or 'Canola/Rapeseed (no price match)') and change the legend to 'matched a price' / 'no price (NA)' to match the count table and prose.
- **[consider] Figure 13.2 (fig-spectrum)** -- The figure's five labelled points ('invented function', 'wrong column name (dplyr)', 'out-of-date argument', 'confidently wrong number', 'the silent join') do not match the five subsection headings directly above it ('Invented functions and packages', 'Assumed column names', 'Out-of-date syntax', 'Numbers produced without a reproducible calculation', 'Assumptions from another place'). 'Assumptions from another place' (units) is missing from the spectrum even though it is the failure the preceding paragraph and Figure 13.1 just discussed, and 'the silent join' is introduced in the figure before the section that explains it. The figure also has large empty top and bottom margins at fig-height 2.2.
  *Fix:* Use the subsection names as the point labels, add a 'wrong unit' point on the right-hand side, and trim the vertical margins (fig-height 1.6 or plot.margin reduction).
- **[consider] Chapter heading '13 AI Tools' and all section numbers (13.1-13.10)** -- The sidebar labels this 'Module 5: Using Artificial Intelligence' but the page heading and TOC are numbered 13.x, and the cross-reference in 13.8 reads 'Section 14.4'. Students navigate by module number; chapter 13 for module 5 is confusing, and the course's own description of the book says it is numbered as 12 modules.
  *Fix:* Book-wide: set `number-depth` or use unnumbered chapters with module labels in the titles, or add `number-sections: false` and rely on the sidebar module headings. Not specific to this chapter, but it is visible on every heading here.
- **[consider] 13.5 A Silent Failure, code listing and transcript for the first block** -- Because the chunk uses `console()` with echo: false, the code appears as a plain transcript-style listing followed by the same code repeated inside the folded session ('> # Load the two files ... '). Every line of the 30-line block is therefore shown twice on the page once the fold is opened. The two later blocks have the same duplication but are short enough not to matter.
  *Fix:* Either drop the first listing and rely on the transcript, or split the first block into two console() calls (load + filter; join + weighted.mean) so the duplicated region is shorter and the 'weighted.mean ... 476.7889' result is closer to the sentence that interprets it.
- **[consider] Other resources callout (end of 13.10)** -- The callout title renders as 'Other resources – AI-assisted coding' with an en dash: Quarto's smart punctuation converts the source '--' into '–' inside the heading. This is consistent with the rest of the book, so only worth noting if the intent was a literal double hyphen.
  *Fix:* No change needed unless the house style wants a colon ('Other resources: AI-assisted coding'), which avoids the smart-dash conversion entirely.

## module05b -- GitHub Copilot

The page renders cleanly: headings are numbered 14.1–14.6 and match the TOC, both callouts render, the single cross-reference resolves to module05a.html#sec-ai-privacy (Section 13.8), and the two R code blocks highlight correctly. The one real defect is the only figure on the page: images/copilot_student_pack.png returns HTTP 404 on the live site and does not exist in images/ or _book/images/, so students see a broken image with the caption "Figure 14.1: Applying for GitHub Education student benefits."

- **[must-fix] Figure 14.1 (#fig-copilot-pack), end of 14.1 Getting Access** -- images/copilot_student_pack.png returns HTTP 404 on agdataanalytics.com and the file is absent from textbook_261/images/ and _book/images/. The page shows a broken image icon with a numbered figure caption under it.
  *Fix:* Add a screenshot of the GitHub Education benefits page as textbook_261/images/copilot_student_pack.png, or delete the figure line (module05b.qmd line 39) until a screenshot exists. Nothing in the prose references @fig-copilot-pack, so removing it breaks no cross-reference.
- **[should-fix] 14.1 Getting Access, callout 'Current access note'** -- The callout states a specific pause date ('temporarily paused since April 20, 2026') that will read as stale or wrong the moment GitHub changes its policy; the course runs Fall 2026 and the claim cannot be verified from the source.
  *Fix:* Soften to 'At the time of writing GitHub had paused new student Copilot sign-ups; check the student access page for current status' and drop the hard date, or verify the date against the linked GitHub Docs page before term start.
- **[consider] 14.3 Two Ways to Use It, 'Inline completions' and 'Posit Assistant chat' subsections** -- The two ### subsections are {.unnumbered}, so in the right-hand TOC they appear as unnumbered entries ('Inline completions', 'Posit Assistant chat') between numbered 14.3 and 14.4, which looks inconsistent with the rest of the chapter.
  *Fix:* Either remove {.unnumbered} so they render as 14.3.1 and 14.3.2, or keep them unnumbered but lower toc-depth so they drop out of the sidebar.
- **[consider] 14.2 Connecting Copilot to Positron** -- This is a purely procedural section (Command Palette, Authentication: Configure Language Model Providers, Accounts menu) with no screenshot, while the only screenshot in the chapter is attached to the sign-up page instead. Students following the steps have nothing to compare against.
  *Fix:* Add a screenshot of the Positron language-model-provider dialog or the Posit Assistant pane with Copilot listed as a provider, e.g. images/copilot_positron_provider.png with a fig- id.
- **[consider] 14.4 Show the Shape of the Data, second code block** -- The R block is comments only, so the page shows a highlighted block with no runnable code and no console transcript; a student may expect an output fold like elsewhere in the book and find none.
  *Fix:* Either add the one or two lines of code Copilot would produce (a group_by/summarise with weighted.mean) under the comments, or note in the prose that the block is a prompt, not code to run.

## module05c -- Graphing with AI

The page renders cleanly: one figure (200), one data link (200), the cross-reference resolves, the objectives box and the collapsed tip callout render, the TOC matches the source headings, and there is no raw markdown, LaTeX, or stray HTML. There are no workbook embeds or console transcripts in this chapter, so the mechanical checks are mostly empty. The worst problem is that the exercise prose names crops and groups ("spring wheat", "four crops", "six crops") that do not match the actual values in sask_variety_yields.csv ("Wheat - Hard Red Spring", and a separate "Wheat - Durum"), so a student copying the suggested prompt into an assistant will get a filter that matches nothing or the wrong thing, which is exactly the failure mode the chapter is warning about.

- **[should-fix] 15.2 The Exercises, exercises 1 and 3, and the prompt block in 15.3** -- The prose says "spring wheat" and the example prompt says "Keep spring wheat rows", but the Crop column holds "Wheat - Hard Red Spring" (1,885 rows) and a separate "Wheat - Durum". An assistant given the prompt verbatim will write filter(Crop == "Spring wheat") or grepl("wheat") and either return zero rows or fold durum in. The chapter never tells the student the real value.
  *Fix:* State the actual level once in 15.2 ("spring wheat is recorded as Crop == \"Wheat - Hard Red Spring\"") and use that string in the 15.3 prompt text, or add a line in the prompt such as "Spring wheat is the Crop value 'Wheat - Hard Red Spring'". This also turns the mismatch into a teachable check.
- **[should-fix] 15.2 The Exercises, heading "5. Rebuild a chart from Module 4" and its cross-reference** -- The heading says "Module 4" but the rendered cross-reference reads "Section 10.2" (module04a.html#sec-chart-types), because chapter numbers count qmd files, not modules. The same page is numbered "15 Graphing with AI" under a sidebar label "Module 5". A student sees Module 4 / Section 10.2 / Chapter 15 on one page with no explanation.
  *Fix:* Either suppress chapter numbering (number-sections: false, or number-depth tuning) so module labels are the only numbering, or rewrite the reference to name the section: "Choose a figure from the Types of Charts section of Module 4 (@sec-chart-types)".
- **[should-fix] Figure 15.1 (fig-missing)** -- The <img> has no alt attribute; the source chunk has no fig-alt. The bar chart is the only evidence for the claim about uneven missingness, and a screen reader gets nothing.
  *Fix:* Add `#| fig-alt: "Stacked bars for 2021 to 2025 showing rows with a recorded yield in green and missing in grey; missing share falls from about half in 2021 to almost none in 2025."` to the chunk.
- **[consider] 15.2 The Exercises, exercise 2 ("four crops") and exercise 4 ("six crops")** -- Exercise 2 says "four crops" without saying which, and the data has near-duplicate crop categories (Canola/Rapeseed vs IP Canola/Rapeseed; Lentils, Lentils - Red, Lentils - Large Green; two wheats). The top-six-by-acreage panel in exercise 4 will depend on whether those are merged, which the checks do not mention.
  *Fix:* Name the four crops for exercise 2 (e.g. the CROPS vector in R/ai_failures.R: Wheat - Hard Red Spring, Canola/Rapeseed, Barley, Oats), and add a check to exercise 4: "Did the assistant treat IP Canola and Canola, or the three lentil classes, as one crop or several?"
- **[consider] 15.1 A Chart Does Not Check Its Data, paragraph after Figure 15.1** -- "About half the 2021 rows have no yield" is right (50.6%), but the figure has no data labels or percentage axis, so the reader has to eyeball a stacked bar to confirm it; 2022 (44%) and 2023 (34%) are not mentioned even though the figure makes the trend the point.
  *Fix:* Either add the missing percentage above each bar in fig_missing_by_year(), or state the run in prose: "The share with no yield falls from 51% in 2021 to 0.1% in 2025."
- **[consider] 15.2 The Exercises, second paragraph ("Work in Positron with an assistant connected")** -- The chapter assumes Positron and a connected assistant but gives no pointer to where that setup is described; the project brief describes the tooling as VS Code plus GitHub Copilot, and module02a still mentions VS Code once.
  *Fix:* Add a cross-reference to the Module 5 chapter that sets up the assistant (e.g. "(@sec-... in Module 5a)") and make sure module02a's VS Code mention matches.
- **[consider] Whole page: no embedded output** -- The chapter has no console transcripts or example plots for any of the five exercises, so a student has nothing rendered to compare their AI-produced chart against; the only figure is the missingness chart. This is consistent with the source, not a render bug, but on the page it reads as thin next to Module 4.
  *Fix:* Add one rendered reference figure for exercise 2 or 4 (R/direct_labels.R already exists) in a collapsed callout, labelled as one acceptable answer rather than the answer.

## module06a -- Making a Workbook Someone Else Can Read

The page renders cleanly: no images, zero unresolved cross-references, all three workbook download links return 200, all three SharePoint embeds return 200 after redirects, the right-hand TOC lists every source heading (numbered 16.1-16.7 with the three unnumbered sub-headings under 16.3), and the theme.scss rules override the inline width="402" so the iframes fill the column. The worst problem is pedagogical rather than mechanical: the chapter's whole point is that the three workbooks look different, yet all three embeds are inside collapsed callouts, and the RM-yields embed hides the row/column headers and disables interactivity, so a student cannot see the "starts in A1, one header row" structure the prose describes or filter the data as the prose says the next user would.

- **[should-fix] 16.3 Three Workbooks, Three Audiences -- "Data for further analysis" embed (Saskatchewan RM yields)** -- The prose says the sheet "begins in A1, uses one header row and keeps one observation per row", but the iframe src has wdHideHeaders=True (row/column headers hidden) so the student cannot see A1 or row numbers, and wdAllowInteractivity=False so they cannot filter or sort the table the prose says the next user is expected to filter.
  *Fix:* For this embed only, change wdHideHeaders=True to wdHideHeaders=False and wdAllowInteractivity=False to wdAllowInteractivity=True in the iframe src in module06a.qmd (the ActiveCell parameter already targets 'RM_Yields_2024_2025'!A1).
- **[should-fix] 16.3 Three Workbooks, Three Audiences -- all three .workbook-callout blocks** -- All three workbooks are in callout-tip collapse="true", so on load the section shows three closed blue bars and no spreadsheets. The section's argument ("the workbooks look different because their readers need different things") depends on the student seeing the three side by side; most will not click through three collapsed callouts.
  *Fix:* Set collapse="false" on these three callouts (or at least the first), or add one sentence before the callouts telling the student to open all three and compare them before reading on.
- **[should-fix] 16.7 Locking Cells and 16.6 Hyperlinks** -- Both sections describe dialog mechanics in prose ("Format Cells > Protection and clear Locked", "Insert > Link", "=HYPERLINK(...)") with no screenshot and no embedded workbook. The page has zero images and the only worked example is a two-line text code block. Per the project convention, Excel mechanics belong in an embedded sheet the student can copy, not in a paragraph.
  *Fix:* Add a small mod06_protect.xlsx with two unlocked input cells, a protected formula cell, a FORMULATEXT column and a README cell with HYPERLINK examples; embed it in a .workbook-callout in 16.7 and cut the numbered step list to a pointer at the sheet.
- **[consider] 16.6 Hyperlinks -- code block** -- The HYPERLINK example is fenced as ```text, so it renders as an unhighlighted grey block, unlike Excel formula blocks elsewhere in the book if they use a different fence; also the second formula is split over two lines with alignment spaces, which a student pasting into a cell will carry into the formula.
  *Fix:* Put each HYPERLINK formula on one line, and use the same fence language the Module 1 chapters use for Excel formulas so highlighting is consistent.
- **[consider] 16.4 Sympathy for the Reader (heading and section)** -- "Sympathy for the reader" was flagged in the project audit as an invented phrase attributed to Peter; it survives here as a section title and its content (five bullet questions then "open the file as though it arrived without an explanation") largely repeats 16.1 Start With the Audience.
  *Fix:* Fold the five questions into 16.1 as a short list and delete 16.4, or rename the heading to something plain such as "Test the Handoff".
- **[consider] 16.1 Start With the Audience -- closing paragraph** -- "Polish should solve a reading problem. It is not decoration added after the analysis." is the "It's not X, it's Y" pattern and a punchy closer the style guide says to cut; the chapter opener "Correct arithmetic is necessary; a usable handoff is the rest of the job." is the same move.
  *Fix:* Delete the second sentence of the 16.1 closer and end the chapter intro on the first sentence.
- **[consider] Chapter heading "16 Making a Workbook Someone Else Can Read" under part "Module 6"** -- Chapter numbering is book-wide (16.x) while the sidebar part is Module 6; this matches the rest of the book, but on this page nothing ties 16 to Module 6 other than the sidebar, which is hidden on narrow screens.
  *Fix:* No change needed if the book-wide scheme is intentional; otherwise set number-depth or per-part numbering in _quarto.yml so sections read 6.1, 6.2.

## module06b -- Writing a Report

The live page at agdataanalytics.com/module06b.html renders as "17 Writing a Report" and matches the source exactly: no images, iframes, data links, code, console transcripts or cross-references exist, so there is nothing to break, and the headings, numbering (17.1-17.7 with unnumbered subsections under Presenting Numbers) and right-hand TOC all agree with the source. The worst problem is not a rendering defect but that the chapter is pure prose with no example, workbook, figure or data file, which makes it the only chapter in Module 6 with no worked artifact and leaves the "Figure 3" in a blockquote reading like a broken cross-reference. Note for the orchestrator: the shared scratchpad filename live.html was being overwritten by parallel reviewers mid-run; I re-fetched into a unique filename before recording results.

- **[should-fix] 17.4 Tables and Charts, blockquote beginning "Figure 3 compares average yield..."** -- In rendered form the blockquote's "Figure 3" sits in a book whose figures are numbered 13.1, 17.1 etc., so a student scanning the page reads it as a reference to a figure that does not exist on the page (there are no figures in the chapter at all). The quote is an example of report prose, but nothing visible in the rendered page signals that.
  *Fix:* Either preface the quote with "For example, in a report:" or change the example to something that cannot be mistaken for a book cross-reference (e.g. "The chart below compares average yield..."). Alternatively add an actual small figure (a two-line ggplot of yield by soil zone from practice/data/) so the example has something to point at.
- **[should-fix] Whole chapter (compare 18 Presentations and 16 Making a Workbook Someone Else Can Read)** -- This is the only chapter in Module 6 with no example artifact: no sample report, no workbook embed, no figure, no data link. The objectives ask students to "introduce a chart" and "present numbers", but the rendered page gives them nothing concrete to imitate beyond two-sentence quotes. Neighbouring chapters end with an "Other resources" tip callout; this one does not.
  *Fix:* Add a short worked example: a one-page sample report (as a linked PDF/.docx in textbook_examples/ or as a rendered section) built from practice/data/sask_variety_yields.csv, showing the opening paragraph, one introduced chart with caption, and a limits paragraph. Add an "Other resources -- writing" callout matching module06c for consistency.
- **[should-fix] 17.7 The Course Project** -- The section says "The course project asks a question ... Use the structure in this chapter" but gives no length, due date, format, marking scheme or link. A student landing here from the sidebar expecting the project brief gets two paragraphs of advice and no specification. CLAUDE.md lists course project details as TBD, so this is a content gap that reads as a missing link in rendered form.
  *Fix:* Either add a cross-reference to wherever the project brief will live (index/intro/syllabus page) or add a placeholder sentence stating where and when the brief will be posted, so the section does not read as the brief itself.
- **[consider] 17.3 Presenting Numbers, subsection headings (Round to the measurement, Supply a comparison, State the units, Report fewer numbers)** -- These are h3 headings marked .unnumbered, so the right-hand TOC shows four unnumbered entries nested under 17.3 while every other entry is numbered. Rendered, it looks like a numbering inconsistency rather than a deliberate choice, and the four subsections are one or two sentences each.
  *Fix:* Either drop the {.unnumbered} so they become 17.3.1-17.3.4, or demote them to bold lead-in phrases in a bulleted list so they stay out of the TOC.
- **[consider] 17.1 Lead With the Answer and 17.5 Describe the Method, blockquote examples** -- The example report text is rendered as a plain Bootstrap blockquote (grey left rule), the same styling Quarto uses for quotations from other authors (e.g. the Willison quote in 13.3). Students may read the canola-yield sentences as citations rather than model text to copy.
  *Fix:* Wrap the examples in a distinct div (e.g. ::: {.example} with a small theme.scss rule, or a ::: {.callout-note title="Example opening paragraph"} callout) so sample prose is visually different from quotations.
- **[consider] Source build process (not page content): scratchpad live.html** -- Not a page defect, but worth passing upstream: parallel chapter reviewers share one scratchpad directory and the generic filename live.html was overwritten by other chapters' fetches mid-run, which initially produced a false reading that module06b.html served a different chapter. Re-fetching to a unique filename confirmed the live page is correct.
  *Fix:* Have the orchestrator instruct subagents to use chapter-specific temp filenames.

## module06c -- Presentations

The live page (https://agdataanalytics.com/module06c.html) is byte-for-byte the same content as the local _book render and is mechanically clean: no images, iframes, data links, unresolved cross-references or console blocks, and all seven numbered sections appear in the right-hand TOC. The worst rendered problem is the Learning Objectives panel: the source uses a numbered list, but theme.scss styles the panel for a bulleted list and stamps a green check mark before every item, so each objective renders with both "1." and a check mark. Beyond that the findings are prose and consistency issues rather than rendering faults.

- **[should-fix] Learning Objectives (.objectives div, rendered <ol type="1">)** -- theme.scss `.objectives` sets `ul { list-style: none }` and `li::before { content: "\2713" }`, but module06c (and every module from 02 onward) writes the objectives as a numbered list. The rendered panel shows the browser's ordinal "1." outside the li box plus the absolutely positioned green check mark inside it, so each line reads "1. ✓ Structure a short talk..." with the default <ol> indent on top of the 1.6rem padding.
  *Fix:* Either add `ol` to the selector in theme.scss (`ul, ol { list-style: none; margin: 0; padding: 0; }`) so numbered objectives get the check-mark treatment, or change the objectives in the .qmd to `-` bullets as module01a-c do. One CSS line fixes it book-wide.
- **[should-fix] Opening paragraph: "This module is assessed with a short presentation in a lab session."** -- The syllabus (261_syllabus.md line 16) says the presentation belongs to Module 5 (communication) and is given in the Monday/Tuesday lab, while the book places this chapter under Module 6: Communicating Results (rendered as Chapter 18). A student reading the page cannot tell which module's test this replaces, or whether the presentation is the module test, the assignment, or the course project.
  *Fix:* State exactly what the presentation replaces ("the Module 6 test is a five-minute presentation given in the Monday or Tuesday lab") and reconcile the module number with the syllabus. Also cross-link to wherever the presentation schedule lives, once that page exists.
- **[consider] Whole chapter (no figure, no workbook, no example slide)** -- This is the only chapter in Modules 1-6 with no embedded artefact at all. Sections 18.3 (Build Slides for Listening) and 18.4 (Explain a Chart Out Loud) describe a headline-as-claim slide and a two-line soil-zone chart in words, but the student never sees one. The "assertion-evidence" idea in the Other resources callout is much easier to grasp from one before/after slide image.
  *Fix:* Add one figure: a single slide built on the chapter's own example ("Canola yields were 15% higher in the black soil zone" headline over the black/brown line chart, source note at the bottom). A PNG exported from PowerPoint or a ggplot rendered at slide aspect ratio in images/ would do; reference it with a fig- label from 18.3.
- **[consider] 18.7 What Is Being Assessed, last paragraph: "Nervousness is not an analytical error. A talk succeeds when the audience can state what was found and why the evidence supports it."** -- Chapter ends on a pull-quote closer, which is the pattern CLAUDE.md flags as the most consistent thing Peter removes. The same tell appears at the end of 18.6 ("This is stronger than guessing. It distinguishes what the analysis shows from what remains possible.") and 18.2 ("'Any questions?' should invite discussion, not replace the conclusion.").
  *Fix:* Stop each of these sections on the substantive sentence before the closer. For 18.7 end on the bulleted list, perhaps with "Nervousness is not marked." folded into the list as a plain statement.
- **[consider] 18.6 heading "Questions"** -- In the right-hand TOC and the sidebar, "18.6 Questions" sits directly above "18.7 What Is Being Assessed" and reads as if it were the chapter's practice questions or test-bank pointer, which every other module has. The section is about handling audience questions.
  *Fix:* Rename to "Handling Questions" or "Answering Questions from the Audience" (the anchor sec-talk-questions can stay).
- **[consider] 18.1, third paragraph: signposting example sentences** -- The rendered line is `"The question is...", "The main result is...", and "This chart provides the evidence..."` followed by `These sentences may feel explicit to the speaker; they make the talk easier to follow.` The three quoted fragments run together with ellipses and curly quotes and are hard to scan in rendered form; and "feel explicit" is an odd phrase for "feel obvious/clunky".
  *Fix:* Set the three phrases as a short bulleted list (one per line) and rewrite the follow-on as "They sound obvious when you say them, and that is the point." or similar plain wording.
- **[consider] Other resources callout (collapsed callout-tip at the end)** -- The callout title renders as "Other resources – presenting" (the `--` is converted to an en dash by smart punctuation). That is fine, but the callout collapses by default and contains the only external links in the chapter; the Storytelling with Data link points at the blog index rather than a specific post, so it will date quickly.
  *Fix:* Leave the title; link a specific post (e.g. a before/after declutter example) or the book itself rather than the rolling blog index.
