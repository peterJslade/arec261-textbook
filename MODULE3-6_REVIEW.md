# Review of Modules 3–6 (module03a–module06c)

Thirty-six reviews: each of the twelve chapters read by three reviewers taking fixed perspectives. **Excel guru** -- accuracy and completeness of the Excel material, menu paths, formulas, Power Query. **Hadley Wickham** -- R idiom, tidy data, teaching order, whether code and output are real and reproducible. **Andrew Gelman** -- honesty of claims, overreach in interpretation, provenance, prose that performs rather than explains. Files reviewed as they stood in the working tree on 2026-08-22, including uncommitted edits.

Severity: **must-fix** (wrong or will not work), **should-fix** (materially weakens the chapter), **consider** (judgement call).

## Must-fix items across all chapters

- **module03a** (Excel guru; Power Query (#sec-power-query)): The recipe says the editor opens with Promoted Headers and Changed Type already recorded, then "Click the icon to change ticket_id, permit_book and lot_code to Text". The Changed Type step has already converted ticket_id to Whole Number, so 00001 is 1 by the time the student acts. When they change the type, Power Query asks "Replace current" or "Add new step"; a student who picks Add new step (or who has the prompt suppressed) gets text "1" with the zeros gone, which is exactly the failure the section claims to prevent. lot_code survives only because the auto-detect left it as text. *Fix:* Use the dropdown in the From Text/CSV preview: set Data Type Detection to "Do not detect data types" before pressing Transform Data, so every column arrives as text and the student then sets the numeric and date columns deliberately. If you keep the current flow, say explicitly: choose "Replace current" when prompted, or delete the Changed Type step first, and explain why (the zeros are gone once the column has been a number).
- **module03a** (Hadley Wickham; CSV, TSV and other delimited files (#sec-csv-files)): The first R code block reads data/deliveries.csv, data/prices.tsv and data/prices_semicolon.txt. None of these exist in practice/data/. The student has just downloaded elevator_tickets.csv and elevator_tickets_semicolon.csv for the Excel section, and those are not used here. So the opening example cannot be run, and the one that could be run is delayed. *Fix:* Open the section with read_csv("data/elevator_tickets.csv") and show the spec message (move the block from #sec-col-types up), then show read_delim("data/elevator_tickets_semicolon.csv", delim = ";", locale = locale(decimal_mark = ",")) and print the first rows so the student sees 24,3 become 24.3. Drop the TSV line or point it at a real file. Every path in the chapter should be a file in practice/data/ or a URL that resolves.
- **module03a** (Hadley Wickham; Excel files (#sec-readxl) and When the header is not on row 1 (#sec-skip-rows)): data/deliveries.xlsx with sheets Notes/2024/2025/Summary does not exist. The skip example is also internally inconsistent: the printed tibble shows the title on row 1, NA on row 2 and the header (Ticket, Date, Crop, Weight) on row 3, which means skip = 2, but the code uses skip = 3 and range = "2025!A4:F66", which put the header on row 4. A student who counts rows from the output gets a different answer from the code. Also the output shows 62 x 6 with only 4 columns while the header row shows 4 names; fine if truncated, but there is no trailing marker to say so. *Fix:* Build a small deliveries.xlsx in practice/data/ (Notes sheet, a 2025 sheet with title, blank row, header on row 4, ~60 rows) so excel_sheets() and read_excel() can be run, and regenerate the printed output from it. Make skip and range agree with what is printed. Mention that the first read_excel() guess for a messy sheet turns every column to character, which is itself the tell.
- **module03a** (Hadley Wickham; Keep a raw copy (#sec-raw-copy)): The script as written defeats its own purpose. raw_file is built from Sys.Date(), download.file() runs unconditionally, and read_csv(raw_file) reads the file just fetched. Run it next year and it downloads next year's table and reads that, which is exactly the failure the prose says the raw copy prevents. The paragraph after the code even says so ('the script will still run and fetch whatever the dashboard holds then'). *Fix:* Either guard the download (if (!file.exists(raw_file)) download.file(...)) with a fixed filename like rm_yields_2026-08-21.csv, or split it: a 01_get_data.R that downloads into a dated file, and a 02_prepare.R that reads a named, pinned file. The README example at the end already implies the second pattern, so make the code match it.
- **module03a** (Andrew Gelman; Start With the Table You Need / A file on the web (#sec-table-needed, #sec-direct-download)): The chapter instructs: "What does one row represent? Who produced it, and is it measured, surveyed, or modelled? ... A file that opens without trouble can still be the wrong file." Then it reads the RM yields file, describes its shape ("One row per RM per year, one column per crop"), and never answers those questions for it. The student is not told where an RM-level canola yield comes from (crop insurance records, covering insured acres only; or survey; or an administrative estimate), whether it is a simple or acreage-weighted average, what an NA means (no crop grown, suppressed, or not insured), or that pre-war rows exist for some crops and not others. The glimpse output shows Canola as NA for the early years with no comment. This is exactly the provenance the chapter says matters, and it is missing for the one dataset the whole module is built on. *Fix:* Add a short paragraph after the glimpse in #sec-direct-download (or in #sec-table-needed) stating, from the dashboard page, who produces the RM yields and how (e.g. derived from SCIC crop insurance data, insured acres only), what an NA row means, and any caveat the dashboard itself gives. If the dashboard does not say, say that it does not say and that this is a limitation to carry forward. Mirror the same facts in the README example in #sec-readme, which currently records units and retrieval date but nothing about how the yields were measured.
- **module03b** (Hadley Wickham; Four joins / First join: yields and the RM lookup): The chapter never shows a join that fails. All three anti_join() calls return 0-row tibbles, left_join() never produces an NA, and inner_join(), full_join() are listed in the bullet points but never executed. The student is told what the four joins do but never sees it, and has no picture of what an unmatched row looks like in the output. *Fix:* Add one deliberately broken example before the real join. For instance, `rain_short <- rain |> filter(year != 2021)` then show `with_station |> anti_join(rain_short, by = ...) |> count(weather_station)` (16 station-years listed), `left_join(...) |> filter(is.na(precip_may_aug_mm)) |> nrow()` (295 rows of NA), and `inner_join(...) |> nrow()` (2,655) against `left_join(...) |> nrow()` (2,950). Three short console() chunks make the four-join bullets concrete and give the student a template for diagnosing their own joins.
- **module03b** (Andrew Gelman; What the Merge Gives You): "Canola yield is highest in wet 2016 and among its lowest values in dry 2017. The pattern is not exact. In 2021 rainfall was ordinary but yields fell sharply, because heat also mattered." Ten annual means, and the text picks the two that fit, then explains away the one that does not with a variable that is not in the data. 2017 (33.8 bu/ac) is not even among the lowest: 2021 (21.9), 2024 (31.5) and 2023 (33.9) are lower or equal. And 2024 had above-average rain (229 mm) with the second-lowest yield, which the text omits. "Because heat also mattered" is a claim the merged table cannot support; the student has no temperature column. *Fix:* Replace the three sentences with a description that is checkable from the printed table: wetter years tend to have higher canola yields but the relationship is loose (the RM-level correlation is about 0.41, and 2021 and 2024 are both low-yield years with ordinary rain). Say plainly that the merged data cannot say why, and that a temperature variable would be needed to test the heat explanation. Or drop the interpretation entirely and leave the table as evidence the merge worked, which is what this chapter is about.
- **module03b** (Andrew Gelman; The Three Tables): The yields file is traced to the Saskatchewan dashboard in module03a, but `station_precip.csv` and `rm_lookup.csv` appear with no provenance at all. Module03a spends a page on READMEs that record source, units and retrieval date; this chapter then hands the student two files of unknown origin. A reader cannot tell whether the precipitation totals are ECCC gauge observations, Open-Meteo model output, or constructed for the course, and whether `Station_km` and `Population_2021` were computed or typed in. *Fix:* Add one or two sentences after the file list stating where each of the three files came from, how the precipitation total was computed (which daily variable, summed over which dates, how missing days were handled), and how the nearest station was assigned. If any file is synthetic or hand-built, say so.
- **module03c** (Excel guru; Reshaping with Power Query (#sec-unpivot)): "The original 2,950 RM-year rows become 17,700 RM-year-crop rows." That is the pivot_longer() count. Power Query's Unpivot Other Columns silently drops every row whose value is null, and rm_yields_2015_2024.csv has blank cells (e.g. 2017 RM 1 has no Durum or Barley), so the Power Query result will have noticeably fewer than 17,700 rows. A student who checks the row count against the R output will think they did something wrong. *Fix:* State the actual Power Query row count (load the CSV in PQ and read it off the status bar) and add one sentence: Unpivot removes rows where the value is null, whereas pivot_longer() keeps them as NA; add `values_drop_na = TRUE` to the R call if you want the two to agree, or note the difference explicitly.
- **module03c** (Hadley Wickham; Dates Stored as Text (#sec-dates-text)): The chapter uses base `as.Date(..., format = "%Y-%m-%d")`, then says the two d/m/y rows "need a second stated format or a date parser that accepts several formats" and leaves them unparsed. lubridate is attached by `library(tidyverse)` and is exactly that parser. As written, the `delivery_date` column is never fixed, so the worked example ends with checklist item 7 ("dates were read as dates") unmet for its own dataset. *Fix:* Replace the base call with `ymd(delivery_date)`, which prints `Warning: 2 failed to parse` -- a better diagnostic than a silent NA -- and then show the one-line fix: `mutate(delivery_date = coalesce(ymd(delivery_date, quiet = TRUE), dmy(delivery_date, quiet = TRUE)))`. I verified both rows (T0004 -> 2025-10-15, T0047 -> 2025-11-03) parse correctly. Add one sentence that `03/11/2025` is ambiguous between d/m/y and m/d/y and that the choice came from the file documentation, not the data.
- **module03c** (Hadley Wickham; Impossible Values (#sec-impossible-values) and Missing Values (#sec-missing-values)): The -12.4, 0 and 250 values are found but never acted on. Two sections later `mean(deliveries$moisture_pct, na.rm = TRUE)` is computed and printed with the 250 still in the column (the `summary()` output shows the contaminated mean of 13.22 against a median of 9.5). The prose says "Correct a value only when the source supports the correction" but never gives the student the action available when it does not, which is to set the value to NA and record why. *Fix:* After the filter, add `deliveries <- deliveries |> mutate(moisture_pct = if_else(moisture_pct > 100, NA, moisture_pct), weight_tonnes = if_else(weight_tonnes <= 0, NA, weight_tonnes))` with a comment stating the rule, and note that this is itself a recorded decision, not a correction. Then the NA counts in the missing-values section change (moisture 3, weight 2) and the chapter should say so, because it makes the point that cleaning creates missing values.
- **module03c** (Andrew Gelman; Working with missing values / Impossible Values): The Impossible Values section displays T0018 (moisture 250), T0006 (weight -12.4) and T0043 (weight 0) and says "Correct a value only when the source supports the correction." Nothing is then done with them, and later `mean(deliveries$moisture_pct, na.rm = TRUE)` prints about 13.7 with no comment. The median is 9.6. The chapter is quietly doing what it tells the student not to do: leaving known-bad values in the data and reporting a summary that depends on them. *Fix:* After the impossible-values check, make an explicit decision in code and prose: e.g. set the three values to NA with `if_else()` / `case_when()` and a comment recording why, or state plainly that they are left in and that the later mean is distorted by the 250. Either way, add one sentence at the `mean()` call noting which flagged values are or are not included.
- **module03c** (Andrew Gelman; Text in a Numeric Column): "The file documentation says every weight is in tonnes, so the suffix can be removed." There is no documentation for `grain_deliveries_messy.csv`; it is a synthetic file written for the book. The sentence invents a provenance check that did not happen, in a chapter whose point is that such checks matter. *Fix:* Say what is true: the file was constructed for this chapter and every weight is in tonnes by construction; in real work, that fact has to come from a data dictionary or the person who made the file, and the next sentence ("If the suffix indicated a different unit...") is the reason to confirm it before stripping text. Consider stating once near the top that the deliveries file is synthetic, built to contain each problem the chapter discusses.
- **module04a** (Excel guru; Check the scale and order): The text presents the truncated axis (panel A, starting at 100) as a choice someone makes, but for the barley data shown (103 to 114 bu/ac) it is what Excel produces by default. Excel's automatic minimum is not zero when the values are clustered tightly relative to their size (roughly when the smallest value is more than about five-sixths of the largest), and this applies to column charts as well as line charts. So a student who selects those five varieties and inserts a clustered column chart gets panel A without doing anything wrong. The chapter never says this, and Module 4b only says 'check the minimum bound'. *Fix:* Add one sentence after the panel A explanation: Excel chooses the axis minimum automatically and will start a column chart above zero when the values are close together, as they are here; the fix is right-click the value axis, Format Axis, and set the Minimum bound to 0 (Module 4b shows this). Consider having the Module 4b workbook include this exact five-variety table so the student can see Excel do it.
- **module04a** (Excel guru; Types of Charts (intro and Bar charts)): The chapter uses 'bar chart' throughout for vertical bars (fig-bar, fig-bar-orientation panel A) and 'horizontal bars' for the turned version. In Excel's Insert > Charts gallery, vertical bars are a Column chart and 'Bar' means horizontal bars. A student told to 'turn the chart' to get horizontal bars will not find a rotate option; they need to change chart type from Clustered Column to Clustered Bar (Chart Design > Change Chart Type). Module 4b's 'uses a bar or column chart' does not resolve this. *Fix:* In the bar chart section, add: Excel calls vertical bars a Column chart and horizontal bars a Bar chart; switch between them with Chart Design > Change Chart Type. Also note that Excel draws the first category at the bottom of a horizontal bar chart, so a table sorted largest-first appears smallest-on-top unless the axis is set to 'Categories in reverse order' (Format Axis > Axis Options).
- **module04a** (Hadley Wickham; Bar charts: compare categories (fig-bar)): The caption says "Average spring wheat yield by risk zone" but fig_bar() draws average yield by crop (Barley, Canola, Oats, Spring wheat) with in-chart title "Average yield by crop, 2021-2025". The caption describes a chart that does not exist. *Fix:* Change the fig-cap to "Average yield by crop, 2021-2025, bu/ac" (or change fig_bar to actually plot spring wheat by risk zone if that was the intent -- the zone version would be a better horizontal-bar example with 21 long labels).
- **module04a** (Hadley Wickham; Use descriptive labels / all paired figures): The prose says "use the document's caption rather than repeating the same title inside the chart", but fig_bar_vertical, fig_bar_horizontal, fig_clutter, fig_declutter, fig_pattern, fig_flat and fig_pie_vs_bar all bake "A. ..."/"B. ..." titles into the plot while the chunk also supplies fig-subcap, so each panel is labelled twice. Worse, the two disagree: the code says "B. Decluttered" while the subcap says "B. Reduced"; the code says "A. As a pie chart" while the subcap says "A. Pie". fig_scatter also hard-codes a sentence-length title ("Zones that grow good wheat also grow good barley (2025)") that duplicates the prose. *Fix:* Drop the title = "A. ..." arguments from those functions (or set title = NULL and let the chunk's fig-subcap carry the label) so the figures follow the rule the section states. Same for fig_scatter and fig_bar: remove the in-chart title since the caption carries it.
- **module04a** (Andrew Gelman; Types of Charts (intro) and all figures drawn from load_yields()): "The examples below use the same Saskatchewan variety data." That is the only provenance statement for fig-bar, fig-line, fig-scatter, fig-stacked, fig-grouped, fig-facet, fig-pie, fig-extras-1, fig-extras-2, fig-bubble, fig-dual and fig-clutter. The file is practice/data/sask_variety_yields.csv (Risk_Zone, Crop, Variety, Year, Acres, Yield, 2021-2025) but the chapter never names the file, the agency that published it, what a risk zone is, or what "Yield" is (insured yield? reported? bu/ac on which moisture basis?). The section on labels says a caption "should identify the source"; the chapter's own captions do not. *Fix:* Add one sentence after "The examples below use the same Saskatchewan variety data" naming the file, the publisher (SCIC variety yield data by risk zone, if that is what it is), the years, and that yields are acre-weighted averages rolled up from zone x variety rows. If the file is synthetic or modified (module04c labels something "synthetic AREC 261 data"), say so here.
- **module04a** (Andrew Gelman; Scatter plots: compare two numeric variables): The rendered chart's title is "Zones that grow good wheat also grow good barley (2025)" and the prose says "The points rise from left to right, so zones with higher wheat yield also tended to have higher barley yield." The correlation across the 21 zones is r = 0.98 (I computed it from the same aggregation the figure uses). A reader will take that as a striking biological fact. It is mostly a single-year weather effect: both crops in a zone got the same 2025 growing season, so a dry zone is low on both. The same plot in a different year could look different, and the 21 points are one year of one province. The title also asserts a conclusion about zones in general from a single cross-section. *Fix:* Change the figure title to a descriptive one ("Spring wheat and barley yield by risk zone, 2025"). In the prose, keep "the points rise from left to right" and add the obvious caveat in one sentence: one growing season, both crops share each zone's weather, so the plot says little about whether good wheat zones are good barley zones in general. Drop "also tended to" as a general claim.
- **module04b** (Excel guru; Start With the Table / workbook callout): In textbook_examples/mod04_graphing_excel.xlsx the Data sheet stores acres, yield_bu_acre and year as text (cells are t="s" shared strings, e.g. D2='202', E2='48.3', F2='2025'). AVERAGEIF skips text, so 'Bar chart'!B5:B7 and 'Line chart'!B5:B6 are cached as #DIV/0! and both charts render blank. The chapter's claim that 'Change a value in the Data sheet and the summary formulas and charts update' is false as shipped. *Fix:* Rebuild the Data sheet with true numeric cells (in Excel: select D2:F61, Data > Text to Columns > Finish, or re-export from the CSV with numeric types). Re-open in Excel and confirm B5:B7 show numbers before re-uploading to SharePoint, since the embed shows cached values.
- **module04b** (Excel guru; Create a Chart (scatter bullet)): The scatter chart in the workbook (xl/charts/chart3.xml) has an empty x-value literal (<c:xVal><c:numLit><c:ptCount val="0"/>) and only a y reference to Data!$E$2:$E$61. Excel therefore plots yield against point index 1..60, not against acres, even after the text-number problem is fixed. The 'Scatter plot' sheet's own note says x comes from Data!D2:D61, which is not what the chart does. *Fix:* In Excel, right-click the chart > Select Data > Edit the series and set Series X values to =Data!$D$2:$D$61 (or rebuild by selecting D1:E61 and Insert > Scatter). Check the x-axis runs roughly 100-500 acres, not 0-60.
- **module04b** (Hadley Wickham; Start With the Table (mod04_graphing_excel.xlsx, Data sheet)): On the Data sheet, acres (D), yield_bu_acre (E) and year (F) are stored as text in 57 of 60 rows (openpyxl reports data_type 's'; the cached values of Bar chart!B5:B7 and Line chart!B5:B6 are all #DIV/0!). AVERAGEIF ignores text cells, so the summary tables are errors and the bar and line charts are empty. The README's claim that the Data sheet is 'left in analysis-ready form' is false, and the prose's 'Change a value in the Data sheet and the summary formulas and charts update' cannot be demonstrated. *Fix:* Regenerate the workbook writing D, E and F as numbers (write floats/ints, not strings, when building the file), re-save from Excel so cached values are populated, and confirm Bar chart!B5:B7 shows 47.6, 47.3, 42.7 and Line chart!B5:B6 shows 45.8, 46.0 (true means from the 57 non-missing yields).
- **module04b** (Hadley Wickham; Create a Chart (Scatter plot sheet)): The scatter chart's series has no x reference (xVal is an empty numLit with ptCount 0) and the y cache is all zeros. The text says 'The scatter plot selects acres and yield'; the actual chart does not reference Data!D2:D61 at all. *Fix:* Rebuild the scatter series with xVal = Data!$D$2:$D$61 and yVal = Data!$E$2:$E$61, and after fixing the text-number problem above, verify in Excel that 57 points appear spread over roughly 130 to 470 acres.
- **module04b** (Andrew Gelman; Start With the Table (#sec-excel-chart-table)): The chapter says "Change a value in the Data sheet and the summary formulas and charts update" and "The bars in the workbook begin at zero." In mod04_graphing_excel.xlsx every cell in the Data sheet, including acres, yield_bu_acre and year, is stored as a text string ('202', '48.3', '2025'). AVERAGEIF ignores text, so the cached values in Bar chart!B5:B7 and Line chart!B5:B6 are all #DIV/0!, and the charts those sheets contain are plotting error cells. A student who opens the workbook sees empty charts under a chapter that describes them in detail. *Fix:* Rewrite the Data sheet with acres, yield_bu_acre and year as numeric cells (and seeding_date as a date), recalculate, and confirm the three means (Central 47.6, North 47.3, South 42.7; 2024 45.8, 2025 46.0) actually appear before publishing. Add a one-line check to the chapter's final paragraph: if a summary cell shows #DIV/0! or a chart is blank, the source column is text, which connects back to Module 3's cleaning material.
- **module04c** (Excel guru; Common Geoms (geom_histogram, geom_boxplot, geom_smooth)): The chapter introduces histograms, box plots and a fitted line with a confidence band as R features, and module04b never mentions them, so the student is left to conclude Excel cannot do them. Excel 365 (Windows and Mac) has Insert > Charts > Statistical (Histogram, Box and Whisker) and Chart Design > Add Chart Element > Trendline > Linear (with an option to display the equation and R-squared). The bin control the text insists on ('state the number of bins or the bin width') is Format Axis > Bin width / Number of bins in Excel's histogram chart. *Fix:* Add one short sentence under each of the histogram/boxplot and geom_smooth subsections naming the Excel equivalent (Insert > Statistical chart; Add Chart Element > Trendline > Linear), or add a 'Histograms, box plots and trendlines' section to module04b and cross-reference it from here. Either way, say which parts Excel lacks: a Box and Whisker chart needs the groups side by side as separate columns, and Excel's trendline has no confidence band.
- **module04c** (Hadley Wickham; Common Geoms / geom_line(): change over time): The line chart connects two points (2024 mean 45.8, 2025 mean 46.0). `year` is a double, so the x axis will carry fractional breaks (2024.00, 2024.25, ...) for a variable that only takes integer values, and two points do not show a trend. Nothing in the text warns about either. This is the one geom in the chapter where the example actively works against the concept. *Fix:* Use a series with enough points to be a line. `rm_yields_2015_2024.csv` is already used in Module 3 and has ten years; summarise mean yield per year from it and plot that. If you keep field_yields, at least add `scale_x_continuous(breaks = 2024:2025)` and say why (a numeric axis does not know that years are integers). Either way, this is a natural place to introduce a scale function, since the Grammar section lists Scales and the chapter never uses one.
- **module04c** (Hadley Wickham; A First Plot / Common Geoms (all chunks)): Every geom carries `na.rm = TRUE` and every chunk also sets `warning: false` and `message: false`. The data has three missing yields. The first time a student runs `geom_point()` on this file they will see `Removed 3 rows containing missing values or values outside the scale range (geom_point())`, and the chapter has arranged never to show it or explain it. `na.rm = TRUE` is also clutter in every listing; it is not idiomatic to type it by default, and here it exists only to silence something the student needs to learn to read. *Fix:* Drop `na.rm = TRUE` everywhere. In the first plot, let the warning render (remove `warning: false` on that chunk) and add two sentences: the file has three fields with no recorded yield, ggplot2 drops them and tells you; the warning is information, not an error, and `na.rm = TRUE` suppresses it once you have confirmed the count is what you expect. Keep `warning: false` on later chunks if you want clean figures, but the student has now seen the message once.
- **module04c** (Andrew Gelman; geom_line(): change over time (fig-r-line)): The caption reads "Mean field yield in 2024 and 2025" and the text says "geom_line() then connects those two rows in year order." The two means are 45.8 and 46.0 bu/acre, from 33 and 27 different fields. Drawing a line between two points invites the reader to see a trend; with two years, different fields each year, and a 0.2 bu/acre difference against a within-year SD of about 6, there is no trend to see. The chapter says nothing about this and the figure's y-axis, which ggplot will zoom to the two values, will make the change look large. *Fix:* Either pick an example with enough time points to justify a line (e.g. aggregate by seeding month, or use a different dataset), or keep the example and add one sentence saying outright that a line through two annual means of different fields shows almost nothing, and that the apparent slope depends entirely on the y-axis range ggplot chose. Consider adding `scale_y_continuous(limits = c(0, NA))` or `expand_limits(y = 0)` in the example to make the point visibly.
- **module04c** (Andrew Gelman; geom_smooth(): fitted patterns (fig-r-smooth)): "The line summarizes the relationship present in these observations." The fitted slope is -0.0039 bu/acre per acre with SE 0.0072 (t = -0.54). There is no discernible relationship, and the confidence band will be wide and will contain a horizontal line. The sentence asserts a relationship exists; the figure shows it does not. A student will come away thinking the line is the finding. *Fix:* Replace with something like: "Here the line is nearly flat and the shaded band is wide -- in these sixty fields there is no clear relationship between field size and yield. The line is drawn whether or not a relationship exists, so look at the band and the scatter, not just the line. Module 7 explains what the line estimates." Also say in one clause what the band is (a 95% confidence band by default) rather than calling it "an interval around it".
- **module05a** (Excel guru; Three Ways to Use AI for Code (#sec-ai-generations)): The taxonomy (chatbot, in-editor assistant, coding agent) is R/Positron-only. Every student with a Microsoft 365 Copilot licence has an in-editor assistant sitting in Excel's Home ribbon that will write formulas, add formula columns, build PivotTables and charts, and 'Get insights' from a table. It is the AI tool most of these students will meet at work and in other classes, and the chapter never names it. Its failure modes are also different: it only works on data formatted as a Table (Ctrl+T), it writes the formula into the sheet for you, and it will summarise a column visually without a formula you can audit. *Fix:* Add a short paragraph to this section on Copilot in Excel as the Excel form of an in-editor assistant: where it lives (Home > Copilot, Microsoft 365 on Windows, Mac and web; requires a Copilot licence, not just Microsoft 365), that it needs the data in a Table, that it offers 'add a formula column' (auditable, the formula is in the cell) and 'show insights' (a chart or PivotTable it builds, which you should check against a SUMPRODUCT or PivotTable of your own). Note that the USask licensing situation may vary and point to the access note in @sec-copilot-signup.
- **module05a** (Excel guru; A Silent Failure (#sec-ai-silent-failure)): The example is presented as a general lesson about silent join failures, but in Excel the same merge (XLOOKUP price by crop, then SUMPRODUCT of Revenue and Acres / SUM of Acres) is not silent at all: 1,051 rows show #N/A and SUMPRODUCT returns #N/A, so the wrong number never appears. Module 3b already teaches this ('An unmatched XLOOKUP returns #N/A, which is easy to count'). The chapter's R-only framing misses that the silent version in Excel is different and just as common: an AI suggesting =VLOOKUP(B2,prices,2) with the approximate-match default TRUE, or wrapping the XLOOKUP in IFERROR(...,0) because 'the #N/A errors look ugly', both of which produce a plausible revenue with no warning. *Fix:* After the corrected R result, add two or three sentences contrasting Excel: XLOOKUP makes the unmatched rows visible as #N/A, which is the behaviour you want; the silent Excel failures are VLOOKUP's approximate-match default (on sorted text it returns the nearest preceding crop name, e.g. 'Canola/Rapeseed' picks up 'Canola' by accident and 'Wheat - Hard Red Spring' picks up 'Spring wheat' or 'Soybeans' depending on sort) and an AI-suggested IFERROR or IFNA wrapper that turns #N/A into 0. The check is the same: =COUNTIF(price_col,"#N/A") or =SUMPRODUCT(--ISNA(price_col)) before any revenue number is reported.
- **module05a** (Hadley Wickham; A Silent Failure (d25_fixed chunk, line 180)): `recode()` has been superseded since dplyr 1.1.0 in favour of `case_match()`. The chapter warns two sections earlier that models emit out-of-date syntax; the chapter then does it itself. A student who asks an assistant "is recode still current?" will be told no. *Fix:* Replace with `mutate(Crop = case_match(Crop, "Canola/Rapeseed" ~ "Canola", "Wheat - Hard Red Spring" ~ "Spring wheat", .default = Crop))`. Note `.default = Crop` is required or Barley and Oats become NA, which is itself a nice one-line illustration of a silent failure. Also update `recode()` inside `fig_join_loss()` in R/ai_failures.R.
- **module05a** (Hadley Wickham; A Silent Failure (first check chunk, lines 146-158)): Module 3b teaches `anti_join()` as the tool for finding failed matches before a join and uses it twice. This chapter instead joins first and then counts `is.na(Price_per_bu)`. That works, but it silently abandons the routine students were just taught, and it only works because Price_per_bu happens to have no genuine NAs. The chapter should show the check that generalises. *Fix:* Before the join, add `anti_join(d25, p, by = "Crop") |> distinct(Crop)` (returns the two mismatched names directly) and cross-reference @sec-joins or whatever 3b's anchor is. Keep the `count(Crop, price_missing = is.na(Price_per_bu))` afterwards if you like, but lead with anti_join so the routine is: anti_join, join with relationship =, row count.
- **module05a** (Andrew Gelman; Course Policy (#sec-ai-policy), Strömberg paragraph): "The largest losses were concentrated among students whose homework became unusually fast while their homework marks rose" and "students who continued spending about as much time on their homework showed much smaller losses" condition on a post-adoption outcome (time spent) and then treat the split as if it were a treatment contrast. Students who keep working at the same pace after getting AI are different students from those who speed up; the comparison cannot separate the tool's use from who chose to use it that way. The next sentence, "Using a tool to understand an error and using it to replace the practice are different activities," presents this as the study's finding when it is the authors' (and the chapter's) interpretation. *Fix:* Report the main result and stop. Something like: "After adoption, homework scores rose and completion times fell, while exam scores fell. The paper argues this is because students substituted the tool for practice; that is a plausible reading, but the subgroup comparison it rests on compares students who chose to work differently, so it is not a clean test." Drop the sentence that treats the 'same time on homework' group as evidence for the activity distinction, or label it explicitly as the paper's interpretation.
- **module05b** (Excel guru; Whole chapter (## GitHub Copilot, #sec-copilot)): The chapter is R/Positron-only. Modules 1-4 teach Excel and R side by side, but this chapter never mentions that Copilot also exists in Excel (the Copilot button on the Home tab in Microsoft 365 apps, with formula-writing, 'add a column that...' and data-summary prompts), nor that the Excel version is a different product with a different licence (Microsoft 365 Copilot, not GitHub Copilot) even though both carry the Copilot name. A student who has seen the Copilot icon in Excel will assume the GitHub sign-up in this chapter unlocks it; it does not. *Fix:* Add a short section (or a paragraph near the top) stating that 'Copilot' names two separate products: GitHub Copilot (code editor, this chapter) and Microsoft 365 Copilot in Excel (a paid Microsoft add-on, not covered by GitHub Education). Say which one the course uses and whether USask's Microsoft 365 licence includes Copilot in Excel, so students do not chase the wrong subscription.
- **module05b** (Excel guru; ## Getting Access, line 39 (fig-copilot-pack)): The figure references `images/copilot_student_pack.png`, but that file does not exist in `textbook_261/images/` (the folder has no copilot_* file). Quarto render will fail or emit a broken image. *Fix:* Either add the screenshot to `images/` or delete the figure line. If kept, caption it with what the student should click on the benefits page, since the prose says 'Education approval and Copilot activation are separate steps' and a screenshot of the activation toggle would resolve that ambiguity.
- **module05b** (Hadley Wickham; Show the Shape of the Data (#sec-copilot-context)): The comment prompt asks for "mean 2025 yield by crop, weighted by acres" and then the chapter stops. The student never sees what Copilot suggests, whether it runs, or what the result looks like. This is the one place in the chapter where show-don't-tell matters, and it tells. *Fix:* Add a runnable chunk immediately after the comment: `d <- read_csv("practice/data/sask_variety_yields.csv")`; `glimpse(d)` (with real output); then the code a reasonable suggestion would produce, e.g. `d |> filter(Year == 2025, !is.na(Yield), !is.na(Acres)) |> group_by(Crop) |> summarise(mean_yield = weighted.mean(Yield, Acres), n = n())`, executed with console() so the output is real. Then point at one thing in the output that the comment's context made correct (the `!is.na` handling) and one thing a student must still verify (which crop categories ended up in which group).
- **module05b** (Hadley Wickham; Checking the Work (#sec-copilot-verify)): The five checks are stated as a list and none is demonstrated. "Count rows before and after", "test on a small case whose answer is known" and "recalculate one result another way" are each a few lines of R, and a student cannot reproduce them from the list. *Fix:* Demonstrate checks 2 and 4 on the weighted-mean example: `nrow(d)` vs `nrow(filter(d, Year == 2025, !is.na(Yield), !is.na(Acres)))`; then recompute one crop by hand, `d |> filter(Year == 2025, Crop == "Barley") |> summarise(sum(Acres * Yield, na.rm = TRUE) / sum(Acres[!is.na(Yield)], na.rm = TRUE))`, and show that it matches. Note that the hand recomputation exposes a real subtlety (rows with Acres but no Yield must be dropped from the denominator too) which is exactly why the check exists.
- **module05b** (Andrew Gelman; Getting Access -- Current access note callout): "GitHub's documentation says that new sign-ups for student and several paid Copilot plans have been temporarily paused since April 20, 2026." This is a very specific factual claim presented with no indication of when the author checked it. The chapter elsewhere invents a plan name ("Copilot Student") that does not match GitHub's own naming (the student benefit has been Copilot Pro via the Student Developer Pack). A precise date lends false authority; if the pause has ended, or never had that date, the student reads a confident statement that is wrong and cannot tell. *Fix:* Either cite the exact GitHub page and add "as of [date checked]" to the callout, or replace the specific claim with the honest version: "Plan names and eligibility change often. Check GitHub's student access page for what is available this term; do not enter payment information." Use GitHub's current plan names or avoid naming plans at all.
- **module05b** (Andrew Gelman; Getting Access -- figure): `![Applying for GitHub Education student benefits.](images/copilot_student_pack.png)` references a file that does not exist in `images/`. The render will show a broken figure, and the caption promises evidence of a process the chapter has not actually shown. *Fix:* Add the screenshot, or delete the figure reference until one exists.
- **module05c** (Excel guru; Graphing with AI (chapter opening) / The Exercises): The chapter is R-only and never says so. Module 4 has a full Excel graphing chapter (@sec-graphing-excel) and the course is billed as Excel and R side by side, yet 'Work in Positron with an assistant connected' is the only instruction, and every exercise (direct labels, geom_ribbon band, facets) is framed in ggplot2 terms. Students will reasonably ask whether they can use Copilot in Excel or paste a sheet into ChatGPT, and the chapter gives no answer. Note also that Copilot in Excel (the in-app one that can see the sheet) requires a paid Microsoft 365 Copilot licence that the USask student account does not include, so an unqualified 'use AI in Excel' would mislead. *Fix:* Add a short paragraph after the opening stating that the exercises are done in R because ggplot2 code is text an assistant can write and you can read, whereas Excel charts are built by clicking and the free assistants cannot see your workbook. Say explicitly that Copilot inside Excel needs a separate paid licence students do not have, and that a browser assistant can still give Excel steps if you paste the header row plus a few rows, but you then have to carry out and check the clicks yourself. One paragraph is enough; the point is to close the question rather than add an Excel track.
- **module05c** (Hadley Wickham; The Exercises (#sec-ai-graph-exercises)): There is no code to load the data or show its structure. The prompt template in the next section refers to a data frame `d`, but nothing in this chapter creates `d`; module05a reads it with read_csv("practice/data/sask_variety_yields.csv") but the student on a fresh session for this chapter has no starting point. A student cannot reproduce the exercises from the text alone. *Fix:* Add a short setup block before the exercises: `library(tidyverse); d <- read_csv("practice/data/sask_variety_yields.csv"); glimpse(d)` with the glimpse output shown (run with console so it is real). The glimpse output also gives the student the exact column names and types to paste into the prompt, which is the point of Objective 1.
- **module05c** (Hadley Wickham; 1. Annotate one point): "Spring wheat" is not a value in the Crop column. The data has "Wheat - Hard Red Spring", "Wheat", "Wheat - Durum", "Wheat - Winter" and "Khorasan wheat/Kamut brand". An assistant asked for spring wheat will guess (I would expect it to grep "Spring", or to filter Crop == "Spring wheat" and get zero rows). The check "Did the calculation average over the intended varieties?" hints at this but the student has no way to know which value is intended. The same ambiguity appears in Exercise 3 and in the example prompt ("Keep spring wheat rows"). *Fix:* Either state the exact value (`Crop == "Wheat - Hard Red Spring"`) in the exercise and the example prompt, or make the ambiguity the explicit teaching point: show `count(d, Crop)` or `distinct(d, Crop)` output and tell the student to pick and name the value themselves before prompting. The latter is better pedagogy, but do it on purpose rather than leaving it as a trap.
- **module05c** (Andrew Gelman; A Chart Does Not Check Its Data): The file is used for five exercises and a figure but its provenance is never stated: where the rows come from, what one row is (a variety in a risk zone in a year), and what a missing Yield means. The pattern in the data suggests structural missingness rather than unreported data: Acres is NA in every row where Yield is NA, the count of missing rows falls monotonically from 1,380 in 2021 to 3 in 2025, and the file looks like a variety-by-zone grid padded to all five years. That is a different thing from 'no recorded yield', and it changes the interpretation of 'A chart of 2021 therefore rests on a much thinner set of observations.' If 2021 rows are missing because those varieties had not been released yet, the 2021 average is not thinner in any worrying sense; it is an average over a different, older set of varieties, which is a composition problem, not a sample-size problem. *Fix:* Add two or three sentences naming the source (SCIC variety yield data or whatever it is) and the unit of observation, and state plainly what a missing Yield most likely means in this file (variety not grown or not insured in that zone-year). Then rewrite the 2021 sentence to describe the actual consequence: the set of varieties behind the 2021 point differs from the set behind 2025, so a trend line partly reflects turnover in varieties, not only weather.
- **module05c** (Andrew Gelman; The Exercises (1, 3) and A Useful Prompt Structure): Every exercise says 'spring wheat' and the example prompt says 'Keep spring wheat rows', but no Crop value is 'spring wheat'. The file has 'Wheat', 'Wheat - Hard Red Spring', 'Wheat - Durum', 'Wheat - Winter' and 'Khorasan wheat/Kamut brand'; the 'Wheat' category has varieties prefixed '- Canada Prairie', so it is apparently a different class, not a parent category. The assistant will resolve this ambiguity silently (string-matching on 'Wheat', or picking one class), and the listed checks ('Did the calculation average over the intended varieties?') do not point the student at it. This is exactly the forking path the chapter claims to teach: the chart will look fine whichever way it is resolved. *Fix:* Either name the Crop value explicitly in the exercises and the example prompt ('Wheat - Hard Red Spring'), or keep the ambiguity deliberately and add a check: 'Which Crop values did the code keep? Print unique(d$Crop) and decide which ones you meant.' The second option is more in the spirit of the chapter, but it must be explicit.
- **module06a** (Excel guru; Locking Cells (#sec-locking-cells)): The four-step workflow is correct but incomplete for a student following it. Step 2 says 'Open Format Cells > Protection' without saying how to get there (Ctrl+1 / Cmd+1, or right-click > Format Cells, or Home > Format > Format Cells). Step 3 does not mention that the Protect Sheet dialog has a checklist of allowed actions (Select locked cells, Format cells, Sort, Use AutoFilter...) which students will see and not understand; the defaults leave only 'select locked/unlocked cells' ticked, so sorting and filtering a protected data table silently stops working. And the text never says how to undo it (Review > Unprotect Sheet), which is the first thing a student who locked the wrong cells will need. *Fix:* Add the keyboard shortcut/right-click path for Format Cells, one sentence on the Protect Sheet checklist (tick Sort and Use AutoFilter if the sheet holds a table the reader will filter), and 'Review > Unprotect Sheet reverses it'. Better still, embed a small protected workbook (see next finding) so the steps can be verified against a real file.
- **module06a** (Excel guru; Locking Cells / Formatting for Handoff): The chapter depends on three embedded workbooks, but none of them illustrates the chapter's own advice. I inspected the files: no sheet in any of the three has sheetProtection, none has a README sheet, the Crop Planning Guide has 'Disclaimer' and 'General Assumptions' but no frozen panes and no protection, and only the RM-yield file has a frozen header row. The Crop Planning Guide styles.xml does contain 38 unlocked (locked="0") cell styles, so the ministry prepared it for protection but never turned it on -- which is actually a nice teaching point, but the text does not mention it. Per the project's own rule ('put Excel explanations in the worksheet, not the prose'), the chapter needs one small workbook of its own with README, Raw, Clean, Analysis, Output tabs, a frozen header, shaded unlocked input cells, a FORMULATEXT column, internal HYPERLINKs, and sheet protection on the Analysis tab. *Fix:* Build mod06_handoff.xlsx (e.g. a tiny crop-budget: inputs for acres/yield/price, formulas for revenue) following the mod01_sum.xlsx pattern, embed it, and point the Locking Cells and Hyperlinks sections at it. Add a sentence in 'A model for entering assumptions' noting the Guide's input cells are already marked Unlocked but the sheets are not protected, and invite the student to protect one and test it.
- **module06a** (Hadley Wickham; Data for further analysis (#sec-aesthetics)): The RM-yield workbook is presented as the model for a file someone will 'filter, summarize or import', yet it has no units anywhere in the header and the Lentils column is in lb/acre (median 1609, max 3411) while every other crop is bu/acre (Peas median 41). A student who imports it and does what Module 3 taught (pivot_longer to Year/RM/crop/yield, then summarise) gets nonsense and has no way to know from the file. The chapter says 'Label every input and output. Include units in headers' two sections later. *Fix:* Either fix the example file (add units to the header, e.g. 'Lentils (lb/ac)', or convert lentils to bu/ac and say so on a README sheet) or keep the file as-is and use it explicitly as a counter-example: 'Open it, look at Lentils, and ask what unit it is in. The file does not say.' Do not present it as a clean model while it carries a silent unit trap.
- **module06a** (Andrew Gelman; A table for reading (#sec-aesthetics)): "Bold headers, borders and restrained number formats expose the table's structure without changing the data underneath." Opening the file: the header cell A6 is bold but has no border; headers span two rows (row 6 names, row 7 units); the data start in row 8 after an agency name, a URL and a title; no panes are frozen over a 75-row table; the sheet is named "Hist Primary Elevator". Nearly every habit the chapter lists under Formatting for Handoff (descriptive sheet names, freeze the header row, README) is absent from the workbook presented as the model of a readable table. *Fix:* Either describe the file as it is -- a typical government release table, readable in print, with specific shortcomings the student can name (two-row headers, no frozen panes, title block above the data) -- or replace it with a workbook that actually exhibits the formatting the sentence claims. Do not claim borders that are not there.
- **module06a** (Andrew Gelman; A table for reading (#sec-aesthetics)): The chapter gives "Source: Manitoba Agriculture, Foresight and Analysis" but row 72 of the worksheet reads "SOURCE: Canadian Grain Commission, Grain Elevators in Canada, August 2025", and row 70 flags "r: revised" values. Manitoba compiled the table; the data are CGC's. In a chapter whose README checklist says "identify sources", the text itself gets the provenance one step short. *Fix:* Cite both layers: data from the Canadian Grain Commission, Grain Elevators in Canada (August 2025), as compiled by Manitoba Agriculture. Point the student at the footnotes in rows 70-72 as an example of the source and revision notes a good table carries.
- **module06a** (Andrew Gelman; A model for entering assumptions (#sec-aesthetics)): "It opens with a disclaimer, lists assumptions separately and repeats the same layout across crop sheets." True, but the chapter then teaches Locking Cells as what "a workbook with editable assumptions should" do, and this workbook has no protected sheets (the My Farm cells are unlocked, but protection was never turned on). It also ships a sheet "Guide2026" whose only content is the placeholder " Please do this page". The example is presented as a clean exemplar while quietly contradicting the chapter's own advice. *Fix:* Say explicitly that the Ministry's file has the Locked property set on inputs but never applied sheet protection, and has a leftover placeholder sheet -- then use that as the exercise: protect the MBarley sheet and delete or fill Guide2026. A real file with real flaws teaches the habit better than an asserted ideal.
- **module06b** (Excel guru; Presenting Numbers / Round to the measurement): "Write 42.4 bu/ac" is given with no indication of how to get there from a cell holding 42.3851. Module 1 already distinguishes number format (display only) from ROUND (changes the value). That distinction matters here: a cell formatted to one decimal still copies into Word as 42.3851 with Paste Special > Text or when pasted as unformatted text, and downstream formulas still use the full value. Students routinely format the display, paste, and get four decimals in the report. *Fix:* Add two or three sentences pointing back to @sec for Module 1 number formats: use a number format for display in the workbook; if the rounded value itself is copied into a report or used in later calculations, use ROUND(value, 1). Note that a formatted cell pasted into Word keeps the display format only with the default paste (Keep Source Formatting).
- **module06b** (Excel guru; Tables and Charts): The section says when to use a table versus a chart and how to introduce each, but gives no Excel route for putting either into a report. Module 4b (@sec-excel-chart-export) covers chart paste options; nothing in the book covers moving a table. Pasting a range from Excel into Word gives a live-looking table that carries gridlines, fill colours and General-format numbers, which contradicts the chapter's advice on precision and restraint. *Fix:* Cross-reference @sec-excel-chart-export for charts. For tables, add a short paragraph: format and round the summary range in Excel first (number formats, units in the header), then in Word use Paste > Keep Source Formatting or Paste as Picture; state that Paste as Picture is safest for a fixed report and that the pasted table will not update. Mention that the summary table should be a small presentation range, not the raw data sheet.
- **module06b** (Hadley Wickham; Lead With the Answer (#sec-lead-with-answer)): The opening example ("Canola yields in the black soil zone averaged 42 bushels per acre ... about 15% higher than in the brown zone ... including the drought year") is invented. No dataset in practice/data/ has canola by soil zone, so a student cannot reproduce the sentence, and the chapter never shows the calculation that the sentence summarises. The whole chapter is about turning a calculation into a sentence, and the calculation is missing. *Fix:* Use practice/data/sask_wheat_2025.csv, which the students already have. `read_csv("practice/data/sask_wheat_2025.csv") |> group_by(soil_zone) |> summarise(n = n(), yield = mean(yield_bu_ac, na.rm = TRUE))` gives Black 53.0, Dark Brown 45.2, Brown 38.6 bu/ac (n = 44/58/48). Show that console output, then show the sentence written from it ("Black-zone wheat fields averaged 53 bu/ac in 2025, about 14 bu/ac (37%) more than brown-zone fields"). Keep the rest of the chapter's examples on the same numbers so the reader can trace every figure.
- **module06b** (Hadley Wickham; Presenting Numbers / Round to the measurement): The section says to report 42.4 not 42.3851 but never says how, and the first thing a student will do is paste `53.04545` from the console. There is also no distinction between rounding for display and rounding in intermediate steps, which is where real errors come from. *Fix:* Add the mechanics in both tools, briefly: `round(x, 1)` and `signif()` in R; `mutate(across(where(is.numeric), \(x) round(x, 1)))` for a summary table; `scales::label_percent()` / `label_comma()` for axis labels; in Excel, number formatting changes display while `ROUND()` changes the stored value. One sentence: round at the point of reporting, never before the calculation finishes.
- **module06b** (Andrew Gelman; Lead With the Answer): The model opening paragraph is presented as exemplary but its figures are invented and not marked as such: "Canola yields in the black soil zone averaged 42 bushels per acre over the last five years, about 15% higher than in the brown zone." None of the practice datasets (rm_canola_yields_1990_2025.csv, field_yields.csv, etc.) is cited, and the chapter that tells students to give "enough information for another person to locate the data" does not do so for its own example. A student reading this learns that confident numbers need no provenance. *Fix:* Either compute the example from an actual course dataset (rm_canola_yields_1990_2025.csv with rm_lookup.csv would support a soil-zone comparison) and say which file and years, or label the block explicitly as a hypothetical: "(Invented numbers, for illustration.)" The first option is better because the same numbers are reused in the Figure 3 and Method examples.
- **module06b** (Andrew Gelman; Lead With the Answer): The model paragraph's inferential clause overreaches: "The gap appears in every year in the data, including the drought year, so it is not explained by one unusual season." Consistency across five years rules out one explanation (a single bad season) but the sentence is framed as if it settles the interpretation. It also slides toward a causal reading of soil zone while the chapter later, correctly, lists confounding and causation as limitations. The example is teaching the storytelling move the caveats section warns against. *Fix:* Rewrite the clause to claim only what five years of two group means support, e.g. "The gap appears in every one of the five years, so it is not an artifact of a single season. Whether it reflects soil or differences in varieties, inputs or weather between the zones cannot be told from these data." Or keep the first sentence and let the second paragraph of the chapter note that the "why" belongs in the limits section.
- **module06c** (Excel guru; Build Slides for Listening (#sec-slides)): "Enlarge charts. Remove fine detail, small annotations and unnecessary series" gives no Excel mechanics. Stretching a pasted Excel chart in PowerPoint scales the picture but not the font sizes if pasted as a chart object, and pasting as a picture then enlarging produces blurry text. Students will do exactly that. *Fix:* Add a short paragraph: resize the chart in Excel first (drag the chart frame to roughly 25 cm x 14 cm, or set Format > Size), then set chart text to 18-24 pt via Home > Font with the chart selected, delete gridlines and unneeded series, and only then copy it. Link to @sec-excel-chart-export for the paste options. Better still, add a one-sheet workbook (mod06_slide_chart.xlsx) with the same chart at report size and slide size side by side.
- **module06c** (Hadley Wickham; Build Slides for Listening (#sec-slides)): The section tells the student to 'enlarge charts' and 'make every element readable from the back of the room' but never shows how. Module 4 ends with ggsave("output/region_boxplot.png", width = 6, height = 4, dpi = 300), which produces a chart whose 11-pt axis text is unreadable when projected. Nothing in the book tells the student that the fix is theme_minimal(base_size = 18) or similar plus a wider aspect ratio, or that in Excel the equivalent is resizing the chart before copying and bumping the font size. *Fix:* Add one short worked example that takes the region_plot object from @sec-saving-plots and rebuilds it for a slide: theme_minimal(base_size = 18), labs() with a claim as the title, ggsave("output/region_slide.png", width = 10, height = 5.6, dpi = 150). Show the two PNGs side by side (report size vs. slide size) so the difference is visible. Add the Excel twin in one sentence: resize the chart to roughly 25 cm wide, then set Chart Area font to 18 pt before Paste as Picture (link to module04b).
- **module06c** (Hadley Wickham; Build Slides for Listening (#sec-slides)): 'Use a claim as the headline' is the best idea in the chapter and it is illustrated with one sentence about canola in the black soil zone that does not correspond to any dataset in practice/data/. The student cannot reproduce it, and the rest of the chapter's examples (five years of yield by soil zone) also refer to data that does not exist in the book. *Fix:* Pick one dataset the student already has (rm_yields_2015_2024.csv or sask_wheat_2025.csv, which has a soil-zone column) and use it for every example in the chapter: the headline claim, the axes/marks/pattern/meaning walkthrough, and the question-and-answer example. Compute the number in the headline from that data so 'quoted numbers can be traced to the analysis' (your own assessment criterion) is modelled rather than only asserted.
- **module06c** (Andrew Gelman; Explain a Chart Out Loud (sec-presenting-charts)): The four-step example moves from pattern to meaning with a 'therefore' that the evidence does not license: "The black zone remains above the brown zone in all five years" then "The yield gap therefore appears broader than one unusual season." Five points on a line with no sense of year-to-year variation, field counts or weighting cannot tell you whether a gap is 'broader' than one season; it can tell you the sign was the same five times. The student is being shown, as the model of good practice, exactly the inference-from-a-small-table move the rest of the module warns against. *Fix:* Make step 4 a descriptive claim that matches step 3, e.g. 'The gap is not confined to one year,' and add a fifth step or a sentence that says what the chart does not show (how many fields, how much the gap varies, what else differs between zones). Alternatively keep the 'meaning' step but have it explicitly name the hedge: 'consistent with a persistent difference, though five years is a short series.'
- **module06c** (Andrew Gelman; Build Slides for Listening (sec-slides)): The recommended headline "Canola yields were 15% higher in the black soil zone" is presented as the virtuous alternative to "Yield by soil zone", but it is a bare point estimate with no period, no base, no uncertainty, and it is read by any listener as 'black soil causes higher yield.' The assertion-evidence style is fine; the assertion here is overconfident. Also, this number has no provenance anywhere in the course: the only soil-zone dataset in practice/data is sask_wheat_2025.csv, which is wheat, and module06b invents the 42 bu/ac figure in a box without a source. *Fix:* Either attach the example to a real dataset the student can open (and change the crop to wheat if that is what the data are), or flag it as hypothetical in the first use. Then write the headline the way the chapter's own 'Limits' step would demand: 'Black-zone canola yields averaged about 15% above brown-zone yields, 2021-2025' -- a description of the comparison, not a claim about the zone.

Totals: 62 must-fix, 130 should-fix, 95 consider.

## module03a -- Getting Data

### Excel guru

The Excel half is well chosen: leading with what a double-click does to elevator_tickets.csv, then Power Query as the recorded alternative, with M shown once to make "recorded steps" concrete. The biggest problem is that the Power Query recipe as written will not preserve the leading zeros it promises: the auto-generated Changed Type step has already turned ticket_id into a whole number, and changing the type to Text afterward (as a new step) gives "1", not "00001". The chapter needs to tell students either to turn off type detection in the preview or to replace the Changed Type step rather than add to it.

- **[must-fix] Power Query (#sec-power-query)** -- The recipe says the editor opens with Promoted Headers and Changed Type already recorded, then "Click the icon to change ticket_id, permit_book and lot_code to Text". The Changed Type step has already converted ticket_id to Whole Number, so 00001 is 1 by the time the student acts. When they change the type, Power Query asks "Replace current" or "Add new step"; a student who picks Add new step (or who has the prompt suppressed) gets text "1" with the zeros gone, which is exactly the failure the section claims to prevent. lot_code survives only because the auto-detect left it as text.
  *Fix:* Use the dropdown in the From Text/CSV preview: set Data Type Detection to "Do not detect data types" before pressing Transform Data, so every column arrives as text and the student then sets the numeric and date columns deliberately. If you keep the current flow, say explicitly: choose "Replace current" when prompted, or delete the Changed Type step first, and explain why (the zeros are gone once the column has been a number).
- **[should-fix] Power Query, the API case (paragraph starting "For the API case the editor does not show a table")** -- The clicks described do not produce the table described. Clicking into `daily` gives a record of two lists; Convert > Into Table gives a Name/Value table with two rows, each holding a List; expanding Value to new rows gives a 10-row long table (time, time, ... precipitation_sum, ...), not "a two-column table of dates and millimetres". Getting side-by-side columns through the UI needs either two separate To Table conversions with Index columns merged, or the Table.FromColumns call that appears in the M listing, which the UI never generates.
  *Fix:* Either describe a UI route that works (drill into `time`, To Table, Add Index; repeat for `precipitation_sum`; Merge on Index), or, simpler and consistent with the M block you already show, tell the student to open Get Data > Blank Query > Advanced Editor and paste the listed M. That also makes the M listing do work instead of being decorative, and it is the route Mac users need anyway (next finding).
- **[should-fix] Power Query, "The same menu reaches other sources"** -- The only Mac caveat is about menu wording. On Excel for Mac the Get Data (Power Query) dialog does not offer From Web; From Folder has only recently appeared and is version-dependent. A Mac student following "From Web ... paste the RM yields URL" will not find the button. Web.Contents does work on Mac from a Blank Query via the Advanced Editor.
  *Fix:* Add one sentence: on a Mac, there is no From Web button; use Get Data (Power Query) > Blank Query, open Advanced Editor, and paste `Csv.Document(Web.Contents("https://dashboard.saskatchewan.ca/export/rm-yields-data/4950.csv"))` (and the Open-Meteo M shown below). Say that From Folder depends on the Excel version.
- **[should-fix] Text to Columns is for repairs** -- The Excel treatment of the European file is thinner than the R one. R gets `locale(decimal_mark = ",")`; Excel gets the Text to Columns Advanced button, and then "import it through Power Query instead" with no indication of how Power Query handles `27,4`. A student who does as told will get the From Text/CSV preview reading weight_tonnes as text (or as 274 on some builds) and no way forward.
  *Fix:* Add the Power Query route for the semicolon file: in the preview set Delimiter to Semicolon, then for weight_tonnes use Change Type > Using Locale, choosing Decimal Number and a locale such as German (Germany) or French (Canada). This is the direct counterpart to readr's `locale()` and is worth a sentence for that reason.
- **[should-fix] What double-clicking a CSV does (#sec-csv-excel)** -- Current Excel 365 on Windows (since late 2023) shows a notification bar when opening a CSV that it converted leading zeros or long numbers, and File > Options > Data > Automatic Data Conversion lets you turn those conversions off. Students on a current install will see something the chapter does not mention and may conclude the warning is the fix.
  *Fix:* Add a sentence noting the warning bar and the Automatic Data Conversion options, and why they are not a substitute for Power Query: the setting is per machine, not per file, so a colleague opening the same CSV gets different data, and it does nothing for the date conversion.
- **[should-fix] Check It Arrived (#sec-validate-import)** -- R gets glimpse(), problems(), summary() and a duplicate check; Excel gets "row count, a filter dropdown, MIN and MAX, and COUNTBLANK". Power Query has a direct equivalent of problems() that the chapter never mentions: the View tab's Column quality, Column distribution and Column profile, which show per-column error, empty, distinct and unique counts in the editor, and the "n errors" link in the Queries & Connections pane after loading.
  *Fix:* Add two or three sentences to the Excel half of this section: turn on View > Column quality and Column profile in the editor; a value that failed a type conversion shows as Error and is counted; Column profile's distinct/unique count on ticket_id is the duplicate check. Mention the error count link after Close & Load.
- **[consider] What double-clicking a CSV does, bullet on lot codes** -- The text says `3-2` becomes 2-Mar. In this file every lot_code (5-13, 10-13, 9-27, 1-28 ...) is a valid month-day and all twenty convert, not just the one cited; on a machine set to day-month order 3-2 becomes 3-Feb instead. The example reads as though only the single code is at risk.
  *Fix:* Say that every lot code in the file converts, and that which date it becomes depends on the computer's date order. This strengthens the point rather than softening it.
- **[consider] Power Query (commented-out screenshots)** -- Three figures are commented out. The prose stands without the double-click and Text to Columns ones, but the Power Query paragraph relies on the student finding the type icon in the column header and the Data Type Detection dropdown (if you adopt the first finding), neither of which is obvious on first opening.
  *Fix:* Restore at least the Power Query editor screenshot, cropped to the column headers with one type menu open, and if you add the Data Type Detection advice, a crop of the From Text/CSV preview dialog showing that dropdown.

### Hadley Wickham

The chapter gets the big things right: it treats acquisition as code, it shows the same request three ways (URL, M, httr2) so the parts line up, and every piece of output I could run is genuine, including the Open-Meteo numbers and the readr spec message. The biggest problem is that the first R code the student meets reads files that do not exist (deliveries.csv, prices.tsv, prices_semicolon.txt, deliveries.xlsx), so the sections on delimited files, readxl and skip rows cannot be reproduced from the text, while the one file that does exist and that the student just downloaded, elevator_tickets.csv, is not used until the second section.

- **[must-fix] CSV, TSV and other delimited files (#sec-csv-files)** -- The first R code block reads data/deliveries.csv, data/prices.tsv and data/prices_semicolon.txt. None of these exist in practice/data/. The student has just downloaded elevator_tickets.csv and elevator_tickets_semicolon.csv for the Excel section, and those are not used here. So the opening example cannot be run, and the one that could be run is delayed.
  *Fix:* Open the section with read_csv("data/elevator_tickets.csv") and show the spec message (move the block from #sec-col-types up), then show read_delim("data/elevator_tickets_semicolon.csv", delim = ";", locale = locale(decimal_mark = ",")) and print the first rows so the student sees 24,3 become 24.3. Drop the TSV line or point it at a real file. Every path in the chapter should be a file in practice/data/ or a URL that resolves.
- **[must-fix] Excel files (#sec-readxl) and When the header is not on row 1 (#sec-skip-rows)** -- data/deliveries.xlsx with sheets Notes/2024/2025/Summary does not exist. The skip example is also internally inconsistent: the printed tibble shows the title on row 1, NA on row 2 and the header (Ticket, Date, Crop, Weight) on row 3, which means skip = 2, but the code uses skip = 3 and range = "2025!A4:F66", which put the header on row 4. A student who counts rows from the output gets a different answer from the code. Also the output shows 62 x 6 with only 4 columns while the header row shows 4 names; fine if truncated, but there is no trailing marker to say so.
  *Fix:* Build a small deliveries.xlsx in practice/data/ (Notes sheet, a 2025 sheet with title, blank row, header on row 4, ~60 rows) so excel_sheets() and read_excel() can be run, and regenerate the printed output from it. Make skip and range agree with what is printed. Mention that the first read_excel() guess for a messy sheet turns every column to character, which is itself the tell.
- **[must-fix] Keep a raw copy (#sec-raw-copy)** -- The script as written defeats its own purpose. raw_file is built from Sys.Date(), download.file() runs unconditionally, and read_csv(raw_file) reads the file just fetched. Run it next year and it downloads next year's table and reads that, which is exactly the failure the prose says the raw copy prevents. The paragraph after the code even says so ('the script will still run and fetch whatever the dashboard holds then').
  *Fix:* Either guard the download (if (!file.exists(raw_file)) download.file(...)) with a fixed filename like rm_yields_2026-08-21.csv, or split it: a 01_get_data.R that downloads into a dated file, and a 02_prepare.R that reads a named, pinned file. The README example at the end already implies the second pattern, so make the code match it.
- **[should-fix] Check what R guessed (#sec-col-types)** -- problems() reports row 42, and the text says 'row 42 is NA'. But row 42 in problems() is the line of the file, counting the header; in the data frame the NA is at row 41 (ticket T0039). A student who runs deliveries[42, ] sees 27.6 and concludes problems() is wrong. Also, the tibble print shows permit_book as 4.52e15, not 4.520134e+15 as the prose says; that form only appears if you print the bare vector.
  *Fix:* Say that row in problems() counts file lines including the header, so the data-frame row is one less, and show deliveries |> filter(is.na(weight_tonnes)) as the way to find it. Change '4.520134e+15' to '4.52e15, and the last digits are gone'.
- **[should-fix] Check what R guessed (#sec-col-types)** -- `.default = col_guess()` is already what cols() does when you leave it out, so writing it in every example adds a line the student has to parse for no change in behaviour. The readr message the student sees also points at spec() and show_col_types = FALSE, neither of which is explained until a one-line mention at the end.
  *Fix:* Drop `.default = col_guess()` from the cols() calls and say once that unnamed columns are still guessed. Show the compact alternative for the tickets file, col_types = "cccDcdc", as the thing spec() is leading you toward, and mention show_col_types = FALSE as the way to silence the message once you have stated the types.
- **[should-fix] Calling an API (#sec-api-r)** -- The code calls resp_status() and prints 200, and the prose then says a 400 or 404 'stops the script with an error'. Both true, but together they suggest the status check is doing something. With httr2 defaults, req_perform() has already errored on any non-2xx status before resp_status() runs, so the check is decorative. The motivating failure (leave out longitude and get 400) is described in #sec-apis but never shown in R.
  *Fix:* Show the failure once: build the request without longitude, call req_perform(), and show the httr2 error message (HTTP 400 Bad Request plus Open-Meteo's reason). Then drop resp_status() or say explicitly that req_perform() has already checked it. Also mention req_dry_run(weather_request) as the way to see the URL that was built; it is the cleanest bridge to the URL listing in #sec-apis.
- **[should-fix] Packages that wrap an API (#sec-data-packages)** -- The cansim and rnassqs blocks have no output and are the only R examples in the chapter not verified by running. get_cansim("32-10-0359-01") downloads a table of several hundred thousand rows and takes a minute or more; the filter assumes clean_names() gives geo and harvest_disposition and that the exact level string is 'Average yield (kilograms per hectare)'. If any of those is off, the student gets zero rows with no error. The prose also says 'a 400 or 404 stops the script' in httr2 but here a wrong filter does not stop anything.
  *Fix:* Run both blocks and paste the real glimpse() and the real count(harvest_disposition) so the level strings are on the page. Tell the student the download is large and slow, and that cansim caches it. For the zero-row failure mode, add a one-line check after the filter (nrow(sask_wheat) or stopifnot(nrow(sask_wheat) > 0)). If the nassqs() example cannot be run without a key, say so and label it as not run.
- **[consider] A file on the web (#sec-direct-download)** -- glimpse() output is truncated with a bare '...' that is not something glimpse prints, and the chapter gives no sense of the remaining 13 columns, which are the other crops and matter for the reshape in the next chapter.
  *Fix:* Either print glimpse() of all 18 columns (it is 18 short lines) or use names(rm_yields) and let the student see the full crop list. Remove the '...'.
- **[consider] What an API Does (#sec-apis)** -- The key-handling advice says 'an environment variable or a separate file that you do not share' but never says how to set one in R. Later, rnassqs says 'put the key in the NASSQS_TOKEN environment variable' with the same gap. A student cannot act on this from the text.
  *Fix:* One sentence in #sec-data-packages: the key goes in .Renviron as NASSQS_TOKEN=yourkey; usethis::edit_r_environ() opens that file, and Sys.getenv("NASSQS_TOKEN") shows whether it was picked up after restarting R. Add .Renviron to the list of things not to commit.

### Andrew Gelman

The chapter is unusually honest for an intro text: it hedges Open-Meteo as modelled rather than measured, says the units are not in the yield file, and makes the student write a README with a retrieval date. The biggest problem is that it does not apply its own advice to its own running example: it tells the student to ask who produced a dataset and whether it is measured, surveyed or modelled, and then uses the Saskatchewan RM yield file throughout without ever saying what those numbers are. A secondary problem is that several "outputs" are static listings dressed as real results.

- **[must-fix] Start With the Table You Need / A file on the web (#sec-table-needed, #sec-direct-download)** -- The chapter instructs: "What does one row represent? Who produced it, and is it measured, surveyed, or modelled? ... A file that opens without trouble can still be the wrong file." Then it reads the RM yields file, describes its shape ("One row per RM per year, one column per crop"), and never answers those questions for it. The student is not told where an RM-level canola yield comes from (crop insurance records, covering insured acres only; or survey; or an administrative estimate), whether it is a simple or acreage-weighted average, what an NA means (no crop grown, suppressed, or not insured), or that pre-war rows exist for some crops and not others. The glimpse output shows Canola as NA for the early years with no comment. This is exactly the provenance the chapter says matters, and it is missing for the one dataset the whole module is built on.
  *Fix:* Add a short paragraph after the glimpse in #sec-direct-download (or in #sec-table-needed) stating, from the dashboard page, who produces the RM yields and how (e.g. derived from SCIC crop insurance data, insured acres only), what an NA row means, and any caveat the dashboard itself gives. If the dashboard does not say, say that it does not say and that this is a limitation to carry forward. Mirror the same facts in the README example in #sec-readme, which currently records units and retrieval date but nothing about how the yields were measured.
- **[should-fix] What an API Does / A file on the web / Check what R guessed (#sec-apis, #sec-direct-download, #sec-col-types)** -- Several blocks are presented as actual output but are static listings: the Open-Meteo JSON ("Cut down, the answer to the request above is: ... [7.5, 4.1, 2.8, 3.0, 0.0]"), the `glimpse(rm_yields)` block ("Rows: 26,197 Columns: 18 ... 1938, 1939"), the `problems(deliveries)` tibble ("1 42 4 a double 38.2 t"), and the readr column specification for the tickets file. The book's convention is that console() chunks execute and plain fences do not, so none of these numbers are guaranteed. The tickets and messy-deliveries files are local, so those two can simply be executed; the web ones cannot be executed at render but then should not read as verbatim. Note also the problems() row number depends on readr version (row counted with or without the header), so a static "42" is the kind of detail that silently goes stale.
  *Fix:* Convert the `read_csv("data/elevator_tickets.csv")` specification and the `problems(deliveries)` example to executed console chunks reading from practice/data/. For the Open-Meteo JSON and the rm_yields glimpse, either keep a dated raw copy in the repo and read from it at render, or add one line saying the values shown were retrieved on a stated date and will differ when the reader runs it. That also demonstrates the chapter's own point about revised data.
- **[should-fix] Check what R guessed (#sec-col-types)** -- "Statistics Canada's `..` is 'not available', `x` is suppressed for confidentiality, and `E` after a number flags it as unreliable; they are not all the same kind of missing." The point is right and the `E` gloss is wrong. In Statistics Canada's symbol legend `E` means "use with caution" (the estimate is published, with a high CV), and `F` means "too unreliable to be published" (the value is withheld). Conflating E with unreliable-and-missing is the opposite of the lesson being taught, which is that the codes mean different things and should be read from the documentation.
  *Fix:* Correct the sentence: `..` not available for the reference period, `x` suppressed for confidentiality, `E` published but use with caution, `F` too unreliable to publish. Link the StatCan symbol legend so the student can check rather than take the book's word.
- **[should-fix] What double-clicking a CSV does (#sec-csv-excel)** -- "`3-4` becomes March 4th" and "Dates in day-month order are read as month-day when the day is 12 or less, and left as text when it is not" are stated as universal behaviour. They are what a machine with an English-Canada or US locale does; the paragraph that follows about European locales shows the author knows this is locale-dependent. Stated without the condition, the claim is stronger than what is shown, and a student on a differently-set machine will see something else and conclude the book is wrong.
  *Fix:* Prefix the list with the condition: on a computer with its region set to Canada or the US, Excel does the following. Keep the European-locale bullet as the contrast.
- **[consider] What double-clicking a CSV does (#sec-csv-excel)** -- "None of this matters if you only look. It matters when you save" overreaches. A `lot_code` displayed as 2-Mar is wrong when you look at it, and sorting or filtering on a column that has half become dates and half stayed text gives wrong answers before anything is saved. The sentence is there for rhythm, not accuracy.
  *Fix:* Replace with something plainer: the display is already misleading, and saving makes the loss permanent because Excel writes its guesses over the original.
- **[consider] Check what R guessed (#sec-col-types)** -- "R did better than a double-click ... It still read `permit_book` as a number, and prints it as `4.520134e+15`." Placed right after the Excel bullet "the last digits are rounded away for good", this invites the reader to think R also lost digits. It did not: 4520134343304084 is below 2^53 and is stored exactly; only the printing is abbreviated. The reason to make it text is that it is an identifier, not that R damaged it. The chapter should not let a true claim about Excel bleed into an implied claim about R.
  *Fix:* Add half a sentence: the value is stored exactly, only the print is abbreviated; the reason to declare it text is that it is an identifier and should never be summed or averaged.
- **[consider] Say Where It Came From (#sec-readme)** -- The README example pairs RM-level yields (one row per RM per year) with Open-Meteo precipitation at a single Saskatoon point for one season. The chapter's opening table says rain should be per RM and needs "whatever identifies a weather station". Nothing says that the point-for-RM substitution is a simplification, or that modelled rainfall at one coordinate is being asked to stand in for a municipality. The student is being shown a provenance record that is complete about dates and units and silent about the design choice that matters most for the yield-rain question.
  *Fix:* Add one line to the README example under the weather entry, or one sentence after it: the Saskatoon point is a stand-in for this chapter; matching rain to each RM is done in the merge chapter, and the choice of point vs area is a limitation to record. This is exactly the "what was done and why" a README is for.
- **[consider] Where Agricultural Data Comes From / Say Where It Came From (#sec-data-sources, #sec-readme)** -- Two lines tell the reader what to think rather than showing it: "The starting point for almost any Canadian question" and "A file called `yields_final.csv` with no provenance is close to useless a year later." The second is nearly justified by the clause that follows, but "close to useless" is a verdict; the three concrete failures after the colon are the argument. Also, "Two other things you will meet" introduces three things (paging, rate limits, revision).
  *Fix:* Drop "The starting point for almost any Canadian question" or replace with what StatCan covers that the others do not. Cut "is close to useless a year later" and let the three failures stand. Change "Two other things" to "Three other things" or fold revision into the raw-copy paragraph where it is already discussed.

## module03b -- Merging Data

### Excel guru

The XLOOKUP workbook is sound: the formulas in mod03_merge_excel.xlsx work as written, the composite-key trick is the right Excel 365 idiom, and the deliberately wrong station-only column is a good teaching device. The biggest problem is that the Excel treatment stops where the R treatment gets interesting: R gets anti_join, a relationship check and a row-count trap with real output, while Excel gets a 30-row sheet in which nothing can ever fail, a one-paragraph Power Query mention that a student could not follow, and no Excel equivalent of any of the checks.

- **[should-fix] Merging in Excel (#sec-merge-excel) -- the workbook's checks** -- The prose says "the checks below the data count #N/A results", but the Merge sheet holds three RMs (1, 92, 171) that all exist in the lookup and all sixteen station-years exist in Precip, so D41 and D42 are always 0. A student never sees what an unmatched row looks like or how the count responds. The R section, by contrast, runs anti_join twice and shows the empty result.
  *Fix:* Add two or three rows to the Merge sheet that fail on purpose: one yield row whose RM number is not in 'RM lookup' (e.g. 999) and one whose station-year has no Precip row (e.g. year 2014). Let I and J show #N/A for those rows so the COUNTIF checks return 1 and 1. Then also show the fourth argument, =XLOOKUP(key, lookup, return, "no match"), in one cell so students see if_not_found as the Excel way of naming a failed match.
- **[should-fix] Merging in Excel (#sec-merge-excel) -- no Excel check on the one side of a many-to-one** -- R gets relationship = "many-to-one", which errors if the lookup has a duplicate RM. Excel has nothing: XLOOKUP silently returns the first of any duplicate RM_Number rows, which is exactly the failure the chapter is teaching about, and the chapter never tells the student how to test uniqueness in Excel.
  *Fix:* Add a third check row to the Merge sheet, e.g. "Duplicate RM numbers in lookup" with =ROWS('RM lookup'!A2:A297)-ROWS(UNIQUE('RM lookup'!A2:A297)) or =SUMPRODUCT(--(COUNTIF('RM lookup'!A2:A297,'RM lookup'!A2:A297)>1)), and one sentence in the prose: before a lookup, confirm the key is unique on the one side, because XLOOKUP returns the first match and says nothing about a second.
- **[should-fix] Merging in Excel (#sec-merge-excel) -- Power Query paragraph** -- "Use Data > Get Data to load the tables, then Merge Queries to select the matching columns" is not followable. It omits: that each CSV is imported with Data > Get Data > From Text/CSV > Transform Data (the wording module03c already uses); that Merge Queries lives on the Home tab of the Power Query Editor; that the multi-column key is chosen by Ctrl-clicking (Cmd on Mac) the columns in the same order on both tables; that the Join Kind dropdown defaults to Left Outer; and, most importantly, that the result is a nested Table column that must be expanded with the double-arrow icon in its header before any precipitation value appears. A first-year student following the text ends with a column of the word "Table".
  *Fix:* Either expand this into a short numbered procedure (import both tables with Transform Data, Home > Merge Queries, pick weather_station then Year on the left and Station then Year on the right, Join Kind = Left Outer, OK, click the expand icon on the new column, untick 'Use original column name as prefix', Close & Load), or cut it to a one-sentence pointer and treat Power Query merges properly in module03c where Power Query is introduced. Half-described is worse than either.
- **[should-fix] Merging in Excel (#sec-merge-excel) -- composite key and Excel 365** -- The J-column formula passes Precip!$A$2:$A$161&"|"&Precip!$B$2:$B$161 as the lookup array. This only works because XLOOKUP in Excel 365 evaluates array expressions natively; in Excel 2019 or older the function does not exist at all, and the same trick with INDEX/MATCH needs Ctrl+Shift+Enter. The chapter says nothing about the version dependency. Also, the key is built from a text column joined to a number column; if a student's own CSV import lands Year as text on one side and a number on the other (common after Text to Columns or a locale mismatch), "Kipling|2015" still matches because & coerces both to text, but the RM lookup in column I will return #N/A when RM is text on one side and numeric on the other, and nothing in the chapter warns about this.
  *Fix:* State once that XLOOKUP and the concatenated-array key require Microsoft 365 (which students have through the university) and will not work in older Excel. Add a sentence to the paragraph on unmatched rows: XLOOKUP treats the number 1 and the text "1" as different keys, so if an entire lookup column returns #N/A, check whether one side is stored as text (the green triangle or left alignment) and convert with VALUE() or Data > Text to Columns > Finish.
- **[should-fix] First join: yields and the RM lookup -- "Text keys must match exactly"** -- This paragraph is placed in the R section and is true of R. It is not quite true of Excel, which the chapter does not say: XLOOKUP (and COUNTIF, MATCH, Power Query with the default comparer) are case-insensitive, so "canola" matches "Canola" in Excel but not in left_join. Trailing spaces break both. A student who learns the rule from the Excel sheet and then moves to R will be surprised.
  *Fix:* Move or duplicate the exact-match warning so it appears for both tools, and note the difference in one sentence: Excel lookups ignore case but not spaces; R joins are sensitive to both. Mention TRIM() as the Excel fix for stray spaces since it is the one students will actually need.
- **[consider] Merging in Excel (#sec-merge-excel) -- Power Query join kinds mirror the R section** -- The R section lists left, inner, full and anti joins. The Merge Queries dialog offers exactly these as Join Kind (Left Outer, Inner, Full Outer, Left Anti, Right Anti) and the dialog footer reports "The selection matches N of M rows from the first table", which is the Excel analogue of anti_join and of the row-count comparison. The chapter misses this one-to-one correspondence and leaves the impression that Excel can only do a left lookup.
  *Fix:* In the Power Query paragraph, add that the Join Kind dropdown contains the same four joins as the R section (Left Outer = left_join, Inner = inner_join, Full Outer = full_join, Left Anti = anti_join) and that the match count shown under the dialog is the place to check for unmatched rows before clicking OK.
- **[consider] Merging in Excel (#sec-merge-excel) -- dependence on the embedded workbook** -- The prose refers to "the shaded column" and "the FORMULATEXT column" without naming them, and the iframe is 402 px wide, non-scrolling, 700 px tall, and opens at A1. Columns I to L and the check rows 40-42 are off-screen in the embed, so a reader of the page without downloading sees the yield numbers and none of the formulas the paragraph is describing. The text does not stand on its own.
  *Fix:* Set ActiveCell in the embed URL to something like 'Merge'!H1 so the lookup columns are in view, or widen the iframe, and name the columns in the prose (column I returns the station, J the precipitation using station|year, K the deliberately wrong station-only lookup, L the formula text). Quoting the J5 formula once in the prose in a code span would also let the page stand without the embed.
- **[consider] Merging in Excel (#sec-merge-excel) -- the three tables in the workbook vs the three files** -- The A2 explanation cell in the workbook says the composite key is built "the way the composite key in Module 1 did", and the 'RM lookup' sheet has 296 rows while the yields cover 295 RMs (RM 521 has a lookup row but no yields). Neither the prose nor the sheet says that extra rows on the lookup side are harmless for a left lookup while missing rows are not, which is the asymmetry the one-to-one/many-to-one discussion is about. Also, the Merge sheet's 30 rows are a hand-pasted subset of the CSV, so nothing in the workbook shows a student how they would get the full 2,950 rows into Excel beside a lookup (Data > From Text/CSV, or just opening the CSV and adding the XLOOKUP columns).
  *Fix:* One sentence in the prose after the workbook: the lookup sheet has one more RM than the yields (521), which does no harm because only the yields rows drive the lookup; the reverse, an RM in the yields with no lookup row, is what produces #N/A. And one sentence saying the same two XLOOKUP columns can be added to the full file after opening the CSV in Excel, which is the step a student will actually take.

### Hadley Wickham

The chapter gets the important things right: it starts from what a row represents, derives the key from that, uses anti_join() before left_join(), and passes relationship = "many-to-one" so the join itself enforces the expectation. All the console output is real and matches the prose. The biggest problem is that nothing ever goes wrong on purpose: every anti_join() returns zero rows, no NA ever appears, and inner_join()/full_join() are described but never run, so a student finishes the chapter without having seen what a failed match looks like or how the four joins actually differ on these tables.

- **[must-fix] Four joins / First join: yields and the RM lookup** -- The chapter never shows a join that fails. All three anti_join() calls return 0-row tibbles, left_join() never produces an NA, and inner_join(), full_join() are listed in the bullet points but never executed. The student is told what the four joins do but never sees it, and has no picture of what an unmatched row looks like in the output.
  *Fix:* Add one deliberately broken example before the real join. For instance, `rain_short <- rain |> filter(year != 2021)` then show `with_station |> anti_join(rain_short, by = ...) |> count(weather_station)` (16 station-years listed), `left_join(...) |> filter(is.na(precip_may_aug_mm)) |> nrow()` (295 rows of NA), and `inner_join(...) |> nrow()` (2,655) against `left_join(...) |> nrow()` (2,950). Three short console() chunks make the four-join bullets concrete and give the student a template for diagnosing their own joins.
- **[should-fix] Merging in R (all join chunks)** -- The key is specified with the named-character-vector form `by = c("rm" = "rm_number")` and, in the second join, the mixed form `by = c("weather_station" = "station", "year")`, where one element is named and one is not. Since dplyr 1.1.0 (2023) the documented idiom is `join_by()`, which reads as an equation and has no named/unnamed ambiguity. The mixed form in particular is a thing students copy wrong (they write `"year" = "year"` or forget the quotes).
  *Fix:* Use `join_by(rm == rm_number)` and `join_by(weather_station == station, year)` throughout, and change the sentence at line 100 to: "If the key has different names in the two tables, write `join_by(left_name == right_name)`; a column with the same name on both sides is written once." Mention in one clause that the older `by = c("a" = "b")` form still works, since students will meet it in other people's code.
- **[should-fix] The row-count trap** -- The chapter has just explained that `relationship = "many-to-one"` makes left_join() stop if the expectation is violated, but the trap chunk omits the argument, so the student sees a warning and 29,500 rows rather than the error the argument was introduced to produce. The lesson "state your expectation and let dplyr enforce it" is undercut by not enforcing it in the one place it matters.
  *Fix:* Show both versions. First the chunk as written (warning, 29,500 rows). Then the same join with `relationship = "many-to-one"` added, wrapped in `try()` so the console chunk shows the error ("Each row in `x` must match at most 1 row in `y`") and no table. One sentence: the warning is what you get by default; the error is what you get when you told dplyr what to expect.
- **[should-fix] Keys** -- The key is asserted from what a row represents, which is the right starting point, but the chapter never checks that the key actually is unique in the data before joining. A student working on their own data has no tool for verifying a primary key other than hoping the join errors. The check is one line and it is the same idea `relationship=` enforces later.
  *Fix:* After the three bullets, add a console() chunk: `rms |> count(rm_number) |> filter(n > 1)` and `rain |> count(station, year) |> filter(n > 1)`, both returning 0 rows, with one sentence: a key is only a key if this returns nothing. Then the later `relationship = "many-to-one"` can be introduced as the join-time version of the same check.
- **[should-fix] Merging in R (first chunk)** -- The prose links the files as `practice/data/rm_yields_2015_2024.csv` but the code reads `"data/rm_yields_2015_2024.csv"`. A student reproducing from the text alone will download the three files and then hit a path error, because the chapter never says where to put them or which working directory the code assumes. The same mismatch exists in Module 2, but this is the first chapter with three files and a student is more likely to get one in the wrong place.
  *Fix:* One sentence before the first chunk: "Save the three files in the `data` folder of your project (@sec-project-layout or whichever section in module03a describes the R/ and data/ folders), so the paths below match." Alternatively make the links and the code paths agree.
- **[consider] First join: yields and the RM lookup (prose after chunk)** -- "Both tables have 2,950 rows after the join" is imprecise: `yields` had 2,950 rows before the join and `with_station` has 2,950 after. Only one table is the result of the join. Also, the 0-row `anti_join()` output (`# A tibble: 0 × 1`) is the first time a student sees an empty tibble and the prose does not point at it.
  *Fix:* Rewrite as: "The `anti_join()` returns an empty tibble (`0 × 1`), so every RM has a match. `with_station` has the same 2,950 rows as `yields`, which is what a many-to-one join should give."
- **[consider] What the Merge Gives You** -- `rain_mm = mean(precip_may_aug_mm)` across all RM rows is a mean weighted by how many RMs each station serves (Swift Current has 31 RMs, Nipawin 7). That is defensible as "average rain experienced by an RM" but it is not the average across stations, and the prose calls it annual mean precipitation without saying which. A student who later computes the station mean from `rain` directly will get different numbers and not know why.
  *Fix:* Either add a clause ("averaged over RMs, so stations with more RMs count more") or compute the station-level mean from `rain` with `.by = year` and compare the two in one sentence. This is also a natural place to show `summarise(..., .by = year)` as the current alternative to `group_by()`, though group_by is fine to keep if Module 2 taught it.
- **[consider] Merging in Excel (Power Query paragraph)** -- The Power Query paragraph describes Merge Queries in three sentences with no sheet, no screenshot and no example, while the rest of the chapter is show-don't-tell. A student cannot reproduce it from the text, and it sits between the XLOOKUP worked example and the R section where it interrupts the parallel.
  *Fix:* Either cut it to one sentence pointing forward to wherever Power Query gets a proper treatment (module03c uses it for Unpivot), or add a Merge Queries step to that Power Query example so the student does the two-column key once in a tool that records it.

### Andrew Gelman

The chapter is honest where it counts: it checks anti_join before every join, records the expected relationship, compares row counts, and shows the station-only mistake in both tools. The biggest problem is the last section, which reads a causal story about rain and heat off a table of ten annual means without saying where the precipitation numbers came from, what the yield-rain relationship actually looks like, or that "heat also mattered" is an assertion imported from outside the data.

- **[must-fix] What the Merge Gives You** -- "Canola yield is highest in wet 2016 and among its lowest values in dry 2017. The pattern is not exact. In 2021 rainfall was ordinary but yields fell sharply, because heat also mattered." Ten annual means, and the text picks the two that fit, then explains away the one that does not with a variable that is not in the data. 2017 (33.8 bu/ac) is not even among the lowest: 2021 (21.9), 2024 (31.5) and 2023 (33.9) are lower or equal. And 2024 had above-average rain (229 mm) with the second-lowest yield, which the text omits. "Because heat also mattered" is a claim the merged table cannot support; the student has no temperature column.
  *Fix:* Replace the three sentences with a description that is checkable from the printed table: wetter years tend to have higher canola yields but the relationship is loose (the RM-level correlation is about 0.41, and 2021 and 2024 are both low-yield years with ordinary rain). Say plainly that the merged data cannot say why, and that a temperature variable would be needed to test the heat explanation. Or drop the interpretation entirely and leave the table as evidence the merge worked, which is what this chapter is about.
- **[must-fix] The Three Tables** -- The yields file is traced to the Saskatchewan dashboard in module03a, but `station_precip.csv` and `rm_lookup.csv` appear with no provenance at all. Module03a spends a page on READMEs that record source, units and retrieval date; this chapter then hands the student two files of unknown origin. A reader cannot tell whether the precipitation totals are ECCC gauge observations, Open-Meteo model output, or constructed for the course, and whether `Station_km` and `Population_2021` were computed or typed in.
  *Fix:* Add one or two sentences after the file list stating where each of the three files came from, how the precipitation total was computed (which daily variable, summed over which dates, how missing days were handled), and how the nearest station was assigned. If any file is synthetic or hand-built, say so.
- **[should-fix] What the Merge Gives You** -- "Some station-years are missing several weeks of daily readings, which is why the rain table includes `days_missing`." The column exists but is never used. The annual mean in the table above is computed on `precip_may_aug_mm` as if every station-year were complete, and the 2024 mean (3.8 missing days on average, max 25 in the file) is treated the same as 2017. A summed precipitation total with 25 missing days is biased low, and the chapter says nothing about that.
  *Fix:* Either show `days_missing` in the summary (e.g. a max per year, or a count of station-years with more than some threshold) and note that totals with many missing days understate rainfall, or state that for this illustration the bias was ignored. Don't introduce a quality flag and then compute on the unflagged value without comment.
- **[should-fix] What the Merge Gives You** -- The `filter(!is.na(canola))` drops 46 rows silently. The text does not say how many RM-years have no canola yield, why, or whether they are concentrated in particular years or RMs. A reader who just learned to compare row counts before and after a join is not told that the summary table is built on 2,904 rows, not 2,950.
  *Fix:* Add a line (`count(merged, is.na(canola))` or similar) and one sentence stating that 46 RM-years have no canola figure and that the annual means exclude them. If the missingness is not random (suppression for small acreage, for example) say so, because it bears on whether the annual means are comparable.
- **[should-fix] The Three Tables** -- "A third table says which weather station is closest to each RM" and later "Some RMs are far from the nearest station in this small set. RM 1, Argyle, is matched to Kipling, 145 km away." One anecdote stands in for the distribution. The median RM is 62 km from its station and a quarter are over 85 km; 145 km is not the exception, it is the tail of a generally poor match. Presenting one extreme makes the problem sound like a handful of outliers.
  *Fix:* Replace the single example with the summary: median 62 km, quarter over 85 km, max 161 km. Then state the consequence for the interpretation in the previous section: station rainfall is a coarse proxy for what fell on the RM, which by itself would weaken any yield-rain relationship.
- **[consider] Merging in Excel** -- "It returns the first rain value for the station in every year, without producing an error." This is correct but the chapter does not tell the student which year that is, so the damage is not concrete. A student cannot check the shaded column against the source without knowing that `XLOOKUP` returns the first row in sheet order, i.e. whichever year happens to be listed first for that station.
  *Fix:* Add half a sentence: the value returned is the rain for whichever year appears first for that station in the rain sheet (2015 here), so every year's row gets 2015 rain. Then the check in the next paragraph ("especially rows where the year ... changes") has something specific to look for.
- **[consider] First join: yields and the RM lookup** -- "Both tables have 2,950 rows after the join." Only one table results from the join; `yields` had 2,950 rows before and `with_station` has 2,950 after. The phrasing muddles the before/after comparison the chapter is trying to teach.
  *Fix:* "`yields` has 2,950 rows and so does `with_station`, as a many-to-one join should."
- **[consider] The row-count trap** -- "`dplyr` warns about the unexpected many-to-many relationship." The chunk runs at render time, so the warning is either shown or it is not; the prose asserts it. If `console()` suppresses warnings, the student reads that there is a warning and sees none, which undercuts the contrast with Excel ("gives no warning").
  *Fix:* Confirm the rendered output includes the dplyr warning text. If `console()` swallows warnings, either print it or change the sentence to say what the student will actually see.

## module03c -- Cleaning Data

### Excel guru

The Power Query menu wording is accurate and the formula workbook follows the raw-beside-clean pattern well, with FORMULATEXT columns and summary checks. The biggest problem is that the Excel side is a bullet list where the R side is a worked walkthrough: the Unpivot row count stated for Power Query is wrong because Unpivot drops nulls, and the two problems R treats at length (text in a numeric column, dates stored as text) have no Excel counterpart at all even though Excel is where they bite hardest.

- **[must-fix] Reshaping with Power Query (#sec-unpivot)** -- "The original 2,950 RM-year rows become 17,700 RM-year-crop rows." That is the pivot_longer() count. Power Query's Unpivot Other Columns silently drops every row whose value is null, and rm_yields_2015_2024.csv has blank cells (e.g. 2017 RM 1 has no Durum or Barley), so the Power Query result will have noticeably fewer than 17,700 rows. A student who checks the row count against the R output will think they did something wrong.
  *Fix:* State the actual Power Query row count (load the CSV in PQ and read it off the status bar) and add one sentence: Unpivot removes rows where the value is null, whereas pivot_longer() keeps them as NA; add `values_drop_na = TRUE` to the R call if you want the two to agree, or note the difference explicitly.
- **[should-fix] Cleaning with formulas / workbook mod03_cleaning_excel.xlsx** -- The R section devotes a whole subsection (#sec-text-numbers) to the "38.2 t" weight that turns the column into text, but the workbook omits ticket T0039 entirely, so the Excel treatment never shows the problem. In Excel it is worse than in R: MIN(D5:D23) and AVERAGE skip a text cell without any warning, and the weight_flag formula IF(D5<=0,...) returns "" for text because Excel sorts text above all numbers, so "38.2 t" passes every check in the sheet.
  *Fix:* Add the T0039 row to the workbook, add a check row "Text in the weight column" = SUMPRODUCT(--ISTEXT(D5:D23)) (the sheet already does this for E but not D), and show the fix column, e.g. =IF(ISNUMBER(D5),D5,VALUE(SUBSTITUTE(D5," t",""))). One sentence in the prose: Excel's MIN/MAX/AVERAGE ignore text cells, so a text-in-column check has to be run separately.
- **[should-fix] Tidying and Cleaning in Excel (#sec-cleaning-excel) -- dates** -- The R section has a dates-as-text subsection; the Excel section says nothing about dates, and Excel is the tool that actually damages them. Opening grain_deliveries_messy.csv by double-clicking converts 2025-09-07 to a real date, leaves 15/10/2025 as text, and on a US/English-Canada locale converts 03/11/2025 (T0047) to March 11 with no warning. The workbook stores all dates as text strings, which sidesteps the issue rather than teaching it.
  *Fix:* Add a short paragraph to the Power Query list: set the delivery_date column to Date and note that the two d/m/y rows become Error; for a whole file in another convention use the type dropdown's "Using Locale..." option. Add a sentence warning that opening a CSV directly (rather than via Get Data) lets Excel guess the date format per cell.
- **[should-fix] Cleaning with formulas / workbook mod03_cleaning_excel.xlsx** -- The R treatment of inconsistent categories shows count(crop) before and after cleaning. The Excel prose points to the filter dropdown, which shows distinct values but not counts, and the workbook has no category check at all, so a student cannot see that "Canola " (trailing space) and "CANOLA" are separate groups until PROPER(TRIM()) fixes them.
  *Fix:* Add a small block to the workbook: =UNIQUE(C5:C23) beside =UNIQUE(G5:G23), each with a COUNTIF next to it (or =SORT(UNIQUE(...))). Dynamic arrays are in Excel 365 on both Windows and Mac. Mention in the prose that PROPER does the job of str_to_title and TRIM collapses internal spaces as well, so it matches str_squish.
- **[consider] Cleaning with formulas / workbook mod03_cleaning_excel.xlsx** -- "Tickets entered more than once" is =SUMPRODUCT(--(I5:I23>1))/2, which is only right when each repeated ticket appears exactly twice. A ticket entered three times makes the count fractional. It also counts T0004 (an exact duplicate) the same as a conflicting duplicate, though the prose distinguishes the two.
  *Fix:* Replace with =ROWS(UNIQUE(FILTER(A5:A23,I5:I23>1))) and, if you want the exact/conflicting split, add a Data > Remove Duplicates mention (destructive, so do it on a copy) or a COUNTIFS on ticket and weight together.
- **[consider] Cleaning with Power Query** -- "Remove Top Rows" and "Use First Row as Headers" are on the Home tab, "Unpivot", "Format > Trim", "Fill > Down" and "Split Column" are on Transform, and "Replace Values" and "Remove Duplicates" are on Home. The list gives no tab names, so a first-year student is hunting through ribbons. Also, Power Query Editor on Mac Excel 365 only gained Transform Data for CSV sources in 2024; older Mac builds and Excel for the web do not have it.
  *Fix:* Prefix each item with its tab (Home > Remove Rows > Remove Top Rows; Transform > Format > Trim; etc.) and add one sentence that Power Query Editor needs a current Excel 365 desktop build on Windows or Mac and is not in Excel for the web.
- **[consider] Missing Values (#sec-missing-values)** -- "Many Excel functions skip blank cells automatically, so the missing values need to be counted separately there" is true for blanks and for text like "NA", but a -999 code is averaged in, a literal #N/A error makes AVERAGE return #N/A, and "N/A" in the grade column (T0022) is skipped silently. The workbook's COUNTBLANK only catches blanks.
  *Fix:* Add a check row =COUNTIF(E5:E23,"<0") or more generally note that numeric sentinel codes must be found with MIN, and that in Power Query Replace Values can turn "NA"/"N/A"/-999 into null so they are dropped from Group By averages.
- **[consider] Cleaning with formulas (workbook embed)** -- The iframe is 402 px wide for an 11-column sheet, so the embedded view shows roughly columns A-E; the cleaning formulas in G-J and the FORMULATEXT column K, which are the point of the sheet, are off-screen until the student scrolls inside the frame or opens the file. The prose also says "summary checks appear below the table" without saying where.
  *Fix:* Set ActiveCell to a cell near G4 so the formula columns are visible on load, or widen the iframe to match other chapters' embeds; add "rows 27-33" to the prose.

### Hadley Wickham

The chapter gets the big things right: one small messy file threaded through every section, code that runs at render time so the output is real, and an honest line between what the data can tell you and what needs the source record. The biggest problem is that it stops short of the tidyverse idiom at exactly the points where the idiom is the lesson: the dates are diagnosed but never parsed (lubridate is loaded and unused), the impossible values are found but then silently left in to contaminate the mean computed two sections later, and the synonym example is a no-op on this data, so the student never sees case_when() do anything.

- **[must-fix] Dates Stored as Text (#sec-dates-text)** -- The chapter uses base `as.Date(..., format = "%Y-%m-%d")`, then says the two d/m/y rows "need a second stated format or a date parser that accepts several formats" and leaves them unparsed. lubridate is attached by `library(tidyverse)` and is exactly that parser. As written, the `delivery_date` column is never fixed, so the worked example ends with checklist item 7 ("dates were read as dates") unmet for its own dataset.
  *Fix:* Replace the base call with `ymd(delivery_date)`, which prints `Warning: 2 failed to parse` -- a better diagnostic than a silent NA -- and then show the one-line fix: `mutate(delivery_date = coalesce(ymd(delivery_date, quiet = TRUE), dmy(delivery_date, quiet = TRUE)))`. I verified both rows (T0004 -> 2025-10-15, T0047 -> 2025-11-03) parse correctly. Add one sentence that `03/11/2025` is ambiguous between d/m/y and m/d/y and that the choice came from the file documentation, not the data.
- **[must-fix] Impossible Values (#sec-impossible-values) and Missing Values (#sec-missing-values)** -- The -12.4, 0 and 250 values are found but never acted on. Two sections later `mean(deliveries$moisture_pct, na.rm = TRUE)` is computed and printed with the 250 still in the column (the `summary()` output shows the contaminated mean of 13.22 against a median of 9.5). The prose says "Correct a value only when the source supports the correction" but never gives the student the action available when it does not, which is to set the value to NA and record why.
  *Fix:* After the filter, add `deliveries <- deliveries |> mutate(moisture_pct = if_else(moisture_pct > 100, NA, moisture_pct), weight_tonnes = if_else(weight_tonnes <= 0, NA, weight_tonnes))` with a comment stating the rule, and note that this is itself a recorded decision, not a correction. Then the NA counts in the missing-values section change (moisture 3, weight 2) and the chapter should say so, because it makes the point that cleaning creates missing values.
- **[should-fix] Inconsistent Categories (#sec-categories)** -- The `case_when()` that maps `"Canola/Rapeseed"` and `"Soybean"` does nothing: neither value exists in the file, and after `str_to_title()` the four crops are already clean. The student copies code, runs it, and sees no change. The chunk also prints nothing afterwards, so there is no evidence either way.
  *Fix:* Either add one or two rows with a genuine synonym to `grain_deliveries_messy.csv` (e.g. `Rapeseed`, `Hard red spring`) so the `count()` before and after differ, or cut the chunk and state in one sentence that synonyms need an explicit mapping and show the pattern on the canola trial `variety` column if it has any. Whichever you choose, follow it with `count(crop)` so the effect is visible.
- **[should-fix] Duplicates (#sec-duplicates)** -- `deliveries |> filter(duplicated(deliveries))` names the data frame inside its own pipeline. It works here but it is the only place in the chapter where the pipe refers back to the global object, and it breaks the moment the input is anything other than `deliveries`. janitor is already loaded and `get_dupes()` does both jobs the section describes in one call.
  *Fix:* Use `deliveries |> get_dupes()` for exact duplicates and `deliveries |> get_dupes(ticket_id)` for the conflicting T0030 rows; the second returns both versions side by side with a `dupe_count` column, which replaces the `count() |> filter(n > 1)` then `filter(ticket_id == "T0030")` two-step. Keep `distinct()` for the removal.
- **[should-fix] Opening chunk and Inconsistent Categories** -- The file is read with `trim_ws = FALSE` so that `"Canola "` survives to be fixed by `str_squish()`. The chapter never says that `trim_ws = TRUE` is the readr default, so a student who does the natural thing next time will not see the problem and may think `str_squish()` is always needed, or be confused when the trailing-space category does not appear.
  *Fix:* Add one sentence after the first `glimpse()`: `read_csv()` removes leading and trailing spaces by default; `trim_ws = FALSE` is used here only so the problem is visible, and `str_squish()` still matters for doubled internal spaces and for text that arrives by other routes (Excel, `read_excel()`, pasted values).
- **[should-fix] Missing values in disguise** -- The text says the `na` argument "told R which text codes to treat as missing" but never shows which codes are in the file. Only one of the five listed codes (`N/A`, on T0022 grade) actually occurs, plus one blank and one literal `NA`. A student cannot check the claim from the text, and the more important lesson -- read the file once with the default `na`, see a numeric column become `<chr>`, then re-read -- is skipped.
  *Fix:* Show the diagnostic: read the file with default `na`, `glimpse()` it so `grade` shows up as `<chr>`, then `count(grade)` to reveal `N/A`, then re-read with the expanded `na =`. That is the real-world workflow and it is three lines. Mention `-999` only as a code that would survive this check because it parses as a number.
- **[consider] Working with missing values** -- `summarise(across(everything(), ~ sum(is.na(.))))` uses the formula shorthand with `.`; the current tidyverse idiom is `.x` inside `~` or a native lambda `\(x) sum(is.na(x))`. Minor, but this is the first `across()` the student sees and `.` is the one form that will not generalize to `map()` later. Also `deliveries |> drop_na()` and `drop_na(weight_tonnes, moisture_pct)` print 58- and 59-row tibbles in full; the difference the prose is making is only visible in the header line.
  *Fix:* Write `across(everything(), \(x) sum(is.na(x)))`. Wrap the two `drop_na()` calls in `nrow()` so the output is `58` and `59` and the sentence "dropping every incomplete row can remove far more data" has numbers beside it.
- **[consider] Recognizing untidy data / Tidying and Cleaning in R** -- The untidy-data list names "several variables stored in one column" (`South_InVigor`) and the Power Query section shows Split Column, but the R section has no counterpart. The student is told a problem exists and given the Excel tool for it but not the R one, so the side-by-side structure breaks here.
  *Fix:* Add a short `separate_wider_delim(plot, delim = "_", names = c("region", "variety"))` example on a three-row tibble, in the same style as the `awkward_names` example. Or drop the `South_InVigor` bullet and the Split Column bullet together so the two sides stay parallel.
- **[consider] A Checklist (#sec-cleaning-checklist)** -- Item 8 says to record every correction in the script, but the chapter never writes the cleaned result anywhere, so the relationship between the raw file, the script and the clean file is left implicit. This is the main organizational idea of reproducible cleaning and it costs one line.
  *Fix:* End the R section with `write_csv(deliveries, "data/grain_deliveries_clean.csv")` and one sentence: the raw file is never edited, the script is the record, and the clean file is regenerated by re-running the script, which is the same idea as Refresh in Power Query.

### Andrew Gelman

The chapter is refreshingly plain: it shows the commands, shows the output, and mostly resists telling the student how to feel. Its biggest honesty problem is that it flags bad values (a moisture of 250, a weight of -12.4, a weight of 0) and preaches "correct only when the source supports it," then carries those values silently into later calculations -- the final mean(moisture_pct) is 13.7 against a median of 9.6 because the 250 is still in there, and the text never says so. A cleaning chapter should model exactly the bookkeeping it is asking students to do.

- **[must-fix] Working with missing values / Impossible Values** -- The Impossible Values section displays T0018 (moisture 250), T0006 (weight -12.4) and T0043 (weight 0) and says "Correct a value only when the source supports the correction." Nothing is then done with them, and later `mean(deliveries$moisture_pct, na.rm = TRUE)` prints about 13.7 with no comment. The median is 9.6. The chapter is quietly doing what it tells the student not to do: leaving known-bad values in the data and reporting a summary that depends on them.
  *Fix:* After the impossible-values check, make an explicit decision in code and prose: e.g. set the three values to NA with `if_else()` / `case_when()` and a comment recording why, or state plainly that they are left in and that the later mean is distorted by the 250. Either way, add one sentence at the `mean()` call noting which flagged values are or are not included.
- **[must-fix] Text in a Numeric Column** -- "The file documentation says every weight is in tonnes, so the suffix can be removed." There is no documentation for `grain_deliveries_messy.csv`; it is a synthetic file written for the book. The sentence invents a provenance check that did not happen, in a chapter whose point is that such checks matter.
  *Fix:* Say what is true: the file was constructed for this chapter and every weight is in tonnes by construction; in real work, that fact has to come from a data dictionary or the person who made the file, and the next sentence ("If the suffix indicated a different unit...") is the reason to confirm it before stripping text. Consider stating once near the top that the deliveries file is synthetic, built to contain each problem the chapter discusses.
- **[should-fix] Inconsistent Categories** -- The synonym `case_when()` maps "Canola/Rapeseed" and "Soybean", neither of which occurs in the data (after squish/title-case the crops are Barley, Canola, Peas, Spring Wheat). The code runs and changes nothing, and the student cannot tell from the output that it was a no-op. This is a worked example whose effect is not shown.
  *Fix:* Either add a synonym to the CSV so the mapping visibly merges two counts (show `count(crop)` after the `case_when()`), or say explicitly that these two values are hypothetical and the code is a template. Do not present a transformation with no visible effect as if it cleaned the data.
- **[should-fix] Mixed Units** -- "Three were entered as decimal proportions, such as 0.099 rather than 9.9." This is presented as a fact about how the values were entered, but it is an inference from the values' size; the text then correctly calls it a rule that "depends on subject knowledge." The two framings sit next to each other. Also the rescaling is applied with no record of how many rows changed.
  *Fix:* Phrase the first sentence as the inference it is ("Three values are below 1, which is not a possible moisture percentage; the most likely explanation is that they were entered as proportions"). Add `sum(deliveries$moisture_pct < 1, na.rm = TRUE)` or a comment recording that three rows were rescaled, matching checklist item 8.
- **[should-fix] Dates Stored as Text** -- "two use day/month/year" is stated as known. One of them, `03/11/2025`, is equally readable as March 11. The only evidence that it is 3 November is that every other delivery falls in Sep-Nov 2025 and the other odd row, `15/10/2025`, can only be dd/mm. That is a reasonable inference but it is an inference, and it is exactly the kind of thing the chapter should show rather than assert.
  *Fix:* Add two sentences: one noting that `03/11/2025` is ambiguous on its own, and one saying why dd/mm/yyyy is the defensible reading here (the other row forces it; the season supports it). Then parse it with the second format so the column ends up `<date>` rather than leaving the fix as an exercise the chapter's own checklist demands.
- **[should-fix] When is dropping safe?** -- "If a rain gauge failed at arbitrary times, the observed days may still represent the season" -- this is the right idea (missingness unrelated to the value) but it is stated as a contrast with the farm example without naming the distinction, so the student has no handle on it beyond two anecdotes. The passage also never connects back to the deliveries file's own missing values (2 moisture, 1 grade), which is the data in front of the student.
  *Fix:* Name the distinction in one plain sentence (whether the chance a value is missing depends on the value itself) and apply it to the file at hand: for the two missing moisture readings, say what would make dropping them harmless and what would not (e.g. if wet loads were more often left blank).
- **[consider] Missing Values** -- "Many Excel functions skip blank cells automatically, so the missing values need to be counted separately there." True for blanks, but the more dangerous case the chapter raises two paragraphs later (`-999`) behaves the same in both tools; and text codes like "N/A" are skipped by AVERAGE while "0" is not. The Excel sentence is doing less work than it looks like.
  *Fix:* Either drop the sentence or make it specific: AVERAGE ignores blanks and text, includes any number, so a -999 or a 0 standing in for missing will be averaged in. That is the parallel to the R point that follows.
- **[consider] Opening / provenance** -- The chapter never says where any of the three files came from. `grain_deliveries_messy.csv` and `canola_trial.csv` are synthetic; the RM yields file is a real government download (@sec-direct-download) that was itself trimmed and renamed for the book. A cleaning chapter that tells the student to "record every correction, exclusion and assumption" should state its own.
  *Fix:* One sentence per dataset at first use: synthetic and built to contain these problems, or derived from the Saskatchewan RM yields download with what was changed (years 2015-2024, columns kept).

## module04a -- Graphing Best Practices

### Excel guru

The principles are sound and the R-rendered figures illustrate them cleanly, and nothing in the chapter says anything false about Excel because it says almost nothing about Excel at all. That is the problem: the chapter names a dozen chart forms and several traps (truncated axes, dual axes, patterned fills, legends) without ever telling the student which of these Excel does by default, which it does natively, and which need a workaround, and Module 4b only follows through on bar, line and scatter. A student who reads this chapter and opens Excel will not know that "Bar" is called "Column", that Excel's default axis on the barley data is the truncated one, or that the combo chart's "Secondary Axis" checkbox is exactly the dual-axis chart being warned against.

- **[must-fix] Check the scale and order** -- The text presents the truncated axis (panel A, starting at 100) as a choice someone makes, but for the barley data shown (103 to 114 bu/ac) it is what Excel produces by default. Excel's automatic minimum is not zero when the values are clustered tightly relative to their size (roughly when the smallest value is more than about five-sixths of the largest), and this applies to column charts as well as line charts. So a student who selects those five varieties and inserts a clustered column chart gets panel A without doing anything wrong. The chapter never says this, and Module 4b only says 'check the minimum bound'.
  *Fix:* Add one sentence after the panel A explanation: Excel chooses the axis minimum automatically and will start a column chart above zero when the values are close together, as they are here; the fix is right-click the value axis, Format Axis, and set the Minimum bound to 0 (Module 4b shows this). Consider having the Module 4b workbook include this exact five-variety table so the student can see Excel do it.
- **[must-fix] Types of Charts (intro and Bar charts)** -- The chapter uses 'bar chart' throughout for vertical bars (fig-bar, fig-bar-orientation panel A) and 'horizontal bars' for the turned version. In Excel's Insert > Charts gallery, vertical bars are a Column chart and 'Bar' means horizontal bars. A student told to 'turn the chart' to get horizontal bars will not find a rotate option; they need to change chart type from Clustered Column to Clustered Bar (Chart Design > Change Chart Type). Module 4b's 'uses a bar or column chart' does not resolve this.
  *Fix:* In the bar chart section, add: Excel calls vertical bars a Column chart and horizontal bars a Bar chart; switch between them with Chart Design > Change Chart Type. Also note that Excel draws the first category at the bottom of a horizontal bar chart, so a table sorted largest-first appears smallest-on-top unless the axis is set to 'Categories in reverse order' (Format Axis > Axis Options).
- **[should-fix] Add variables carefully (dual axis)** -- The warning against two vertical scales never names the Excel feature that creates them. In Excel this is Insert > Combo Chart (or Change Chart Type > Combo) with the 'Secondary Axis' box ticked for one series, and Excel's Recommended Charts will often suggest it unprompted when one series is much larger than the other (yield in bu/ac next to acres in millions is the textbook case). The student will meet the temptation by name in Excel but the chapter only describes the outcome.
  *Fix:* Add a sentence naming the combo chart and its Secondary Axis checkbox as the thing to decline, and give the Excel equivalent of the advice: either two separate charts stacked in the sheet, or an index column (value divided by first-year value) so both series share one axis.
- **[should-fix] Ranges, trend lines and reference lines** -- This section introduces fitted lines, shaded ranges, reference lines and waterfalls with R figures but no indication of Excel support, and Module 4b covers none of them. The Excel reality is mixed: the fitted line is native (Chart Elements > Trendline, with an R-squared/equation option) and its 'Forecast Forward/Backward' setting is exactly the extension beyond the data the text warns against; the waterfall is a native chart type since Excel 2016 (Insert > Waterfall) but the start and end bars must be right-clicked and 'Set as Total' or they float; a reference line and a shaded range are not native and need an extra column of constant values added as a line series, or a stacked-area trick for the band. The text treats all four as equally available.
  *Fix:* Add a short paragraph, or a column in the Choosing table, that says which forms are a chart type in Excel (trendline, waterfall with Set as Total, histogram, box and whisker) and which are built from a helper column (reference line as a constant series, shaded range as stacked area with the lower band's fill removed). Either do that here or add the waterfall and trendline to the Module 4b workbook and point to it.
- **[should-fix] Stacked and grouped bars: compare composition** -- Small multiples are recommended as the answer when grouped bars get busy, but Excel has no facet or small-multiples feature. The student has to make one chart per crop by hand (or one PivotChart per crop), size them identically, and set the same axis bounds on each so the panels are comparable, which is precisely the 'shared axes' point the text makes in the abstract. The chapter sets this as a principle without acknowledging the Excel cost, and Module 4b does not pick it up.
  *Fix:* After the facet figure, add that Excel does not build small multiples automatically: make one chart, copy it, change each copy's data series, and fix the Minimum and Maximum bounds on every value axis by hand so the panels share a scale. This is also a fair place to say that this is one of the cases where the R approach (facet_wrap) is genuinely less work.
- **[consider] Histograms and box plots: show distributions** -- The cross-reference to @sec-charts (Module 1c) covers the Excel histogram (Insert > Statistical Chart > Histogram) but box plots in Excel are only a YouTube link there. Since this section explicitly recommends box plots for comparing several groups, the student has no Excel route for that recommendation. Also the Module 1c menu path 'Statistical Chart' is the Windows ribbon tooltip; on Mac the same button is under Insert > Chart > Statistical, and both Histogram and Box and Whisker live there.
  *Fix:* Add a sentence here or in Module 1c: Excel has a Box and Whisker chart in the same Statistical Chart menu as Histogram; it needs the groups laid out as separate columns (one column per group) rather than a long table, which is the opposite of the tidy layout used elsewhere in the book.
- **[consider] Line charts: show change over time** -- 'The connection between points implies that the order and interval have meaning' is true of the R chart but not of an Excel Line chart, which treats the horizontal axis as evenly spaced categories regardless of the values. Years 2015, 2016, 2019, 2020 are plotted at equal spacing, and a missing year simply disappears. Excel only spaces by value if the axis is recognized as dates (Format Axis > Date axis) or if an XY Scatter with straight lines is used. Module 4b's line chart sheet uses consecutive years so the student will not notice until their own data has a gap.
  *Fix:* Add one sentence: in Excel a Line chart spaces the categories evenly whatever they are, so with uneven or missing years use an XY Scatter with lines, or set the axis type to Date axis under Format Axis.
- **[consider] Remove clutter (patterned fills)** -- The text says patterned fills 'were useful when charts had to be printed in black and white' but on screen make bars harder to read. Excel still offers them (Format Data Series > Fill > Pattern fill) and also offers 3-D Column as a top-level chart type, so both of the things being warned against are one click away. Nothing tells the student where these appear so they can recognize them.
  *Fix:* Name them: avoid the 3-D variants in the Insert > Charts gallery and the Pattern fill option under Format Data Series. One sentence is enough.
- **[consider] Excel and R for Charts** -- 'The chart remains easy to change by clicking' undersells the main practical difference a first-year student will hit. An Excel chart is linked to a cell range, so it updates when the cells change but not when rows are added outside the range (Module 4b mentions this), and formatting done by clicking has to be redone for every chart. The section also does not mention that Excel charts can be saved as templates (right-click chart > Save as Template) which is Excel's partial answer to the 'same formatting as a set of other figures' point credited only to R.
  *Fix:* Rewrite the Excel sentence to say what the link to cells means (updates with edits to the range, not with rows added outside it, unless the source is an Excel Table), and mention chart templates as the Excel way to reuse formatting so the comparison with R is even-handed.

### Hadley Wickham

This is a good principles chapter: every figure is drawn from one real dataset, the pairs (truncated/zero, cluttered/reduced, stacked/grouped/faceted, pie/bar) let the reader see the argument rather than be told it, and all 27 figure calls in R/chart_types.R run cleanly against practice/data/sask_variety_yields.csv. The biggest problem is that the figures do not practise what the prose preaches: captions that describe a different chart than the one drawn, in-chart titles duplicating (and sometimes contradicting) the Quarto subcaptions right after a section telling students not to do that, and a pie that uses a legend under a paragraph saying to label slices directly. Students will copy the figures, not the sentences.

- **[must-fix] Bar charts: compare categories (fig-bar)** -- The caption says "Average spring wheat yield by risk zone" but fig_bar() draws average yield by crop (Barley, Canola, Oats, Spring wheat) with in-chart title "Average yield by crop, 2021-2025". The caption describes a chart that does not exist.
  *Fix:* Change the fig-cap to "Average yield by crop, 2021-2025, bu/ac" (or change fig_bar to actually plot spring wheat by risk zone if that was the intent -- the zone version would be a better horizontal-bar example with 21 long labels).
- **[must-fix] Use descriptive labels / all paired figures** -- The prose says "use the document's caption rather than repeating the same title inside the chart", but fig_bar_vertical, fig_bar_horizontal, fig_clutter, fig_declutter, fig_pattern, fig_flat and fig_pie_vs_bar all bake "A. ..."/"B. ..." titles into the plot while the chunk also supplies fig-subcap, so each panel is labelled twice. Worse, the two disagree: the code says "B. Decluttered" while the subcap says "B. Reduced"; the code says "A. As a pie chart" while the subcap says "A. Pie". fig_scatter also hard-codes a sentence-length title ("Zones that grow good wheat also grow good barley (2025)") that duplicates the prose.
  *Fix:* Drop the title = "A. ..." arguments from those functions (or set title = NULL and let the chunk's fig-subcap carry the label) so the figures follow the rule the section states. Same for fig_scatter and fig_bar: remove the in-chart title since the caption carries it.
- **[should-fix] Pie charts: show a simple share (fig-pie)** -- The text ends "If a pie is used, keep the number of slices small, label them directly and leave it two-dimensional", but the pie in fig_pie_vs_bar uses a legend (theme(legend.title = element_blank()...)) and no slice labels. The one pie in the book breaks the one piece of pie advice given.
  *Fix:* Label the slices directly, e.g. add geom_text(aes(label = paste0(Crop, "\n", round(share), "%")), position = position_stack(vjust = 0.5)) and set legend.position = "none". It also makes the "canola is about half" sentence checkable from the figure.
- **[should-fix] Add variables carefully (fig-dual)** -- The prose claims "The apparent crossing point and distance between the lines are controlled by the two scales", but with rng = c(30, 50) and c(20, 62) both panels cross in almost the same place (between 2022 and 2023) and look nearly identical. I rendered both; the demonstration does not show what the sentence says.
  *Fix:* Pick the two right-hand ranges so the panels genuinely differ: one where acres sits above yield throughout (e.g. rng = c(42, 52)) and one where it sits below and never crosses (e.g. rng = c(18, 30)). Then the sentence about the crossing point is true of the figure.
- **[should-fix] Add variables carefully (fig-bubble)** -- The prose and caption say point size represents "seeded acres", but the code merges den only from the Spring wheat side, so size is spring wheat acres per zone (the legend correctly says "Wheat acres (thousands)"). Prose and legend disagree about what the third variable is.
  *Fix:* Say "spring wheat seeded acres" in both the sentence before the chunk and the fig-cap.
- **[should-fix] R/chart_types.R (used by @sec-chart-types and assigned in Module 5)** -- Every helper uses base idioms students are never taught: aggregate(cbind(num = ..., den = ...), by = list(...)), merge() with suffixes, do.call(rbind, lapply(split(...))), read.csv. Modules 3 and 4c teach read_csv, group_by/summarise, left_join and ggplot2 exclusively (zero uses of aggregate or merge in those chapters). Module 5c then tells students to rebuild a figure and "compare the two scripts" against this file. The comparison target is written in a dialect they cannot read. The file header also says "Figures for Module 3".
  *Fix:* Rewrite the data steps in the idiom the book teaches: read_csv(), filter(), mutate(), group_by(Crop, Year) |> summarise(Yield = weighted.mean(Yield, Acres)), pivot_wider(names_from = Crop, values_from = Yield) instead of merge() for the scatter/bubble/trend data, and a single prepared tibble shared by fig_scatter, fig_bubble and fig_trend (the same 8-line block is copy-pasted three times). Fix the header comment to Module 4.
- **[consider] Check the scale and order (fig-truncated)** -- Panel A is drawn with labels = FALSE and panel B with value labels. The prose says "both panels show the same five ... yields" but a reader can reasonably conclude the fix was adding numbers rather than starting the axis at zero. The code comment explains the choice (that is how such charts appear in the wild), but the text does not.
  *Fix:* Either draw both panels without labels so the only difference is the axis, or add one clause to the prose: panel A is shown as such charts usually appear, without the values printed.
- **[consider] Histograms and box plots: show distributions** -- This is the only chart type in the section with no figure; it is one paragraph and a back-reference to Module 1's Excel histograms. Every other type gets a drawn example from the same yields data, and 04c teaches geom_histogram and geom_boxplot, so the omission is conspicuous.
  *Fix:* Add a two-panel chunk from the same dataset: a histogram of 2025 zone-level spring wheat yield and box plots of yield by crop. Twelve lines in chart_types.R, and it makes the "histogram when shape matters, box plot when comparing groups" sentence visible.
- **[consider] Decide whether a chart helps (fig-wheat-fifteen)** -- The prose says "2024" for the two-value sentence while the caption says "2024/25" and the helper comment says approximate USDA WASDE figures. The in-text sentence also does not say the figures are approximate, while the chart caption does.
  *Fix:* Use the same year label in both places and move "approximate figures from USDA" into the sentence too, or cite the source in references.bib so the student can check the 53.7 and 34.9.

### Andrew Gelman

The chapter is admirably plain and mostly says only what its figures show; the Georgia and truncated-axis examples are exactly the kind of honesty-in-display teaching I like. The biggest problem is that a chapter about honest graphics does not say where its own data come from: the "Saskatchewan variety data" used for a dozen figures is never identified, several charts are built from figures the source code itself calls "approximate" or "indicative", and the scatter plot carries a causal-sounding title and a reading ("zones with higher wheat yield also tended to have higher barley yield") that hides a mechanical reason for the near-perfect correlation.

- **[must-fix] Types of Charts (intro) and all figures drawn from load_yields()** -- "The examples below use the same Saskatchewan variety data." That is the only provenance statement for fig-bar, fig-line, fig-scatter, fig-stacked, fig-grouped, fig-facet, fig-pie, fig-extras-1, fig-extras-2, fig-bubble, fig-dual and fig-clutter. The file is practice/data/sask_variety_yields.csv (Risk_Zone, Crop, Variety, Year, Acres, Yield, 2021-2025) but the chapter never names the file, the agency that published it, what a risk zone is, or what "Yield" is (insured yield? reported? bu/ac on which moisture basis?). The section on labels says a caption "should identify the source"; the chapter's own captions do not.
  *Fix:* Add one sentence after "The examples below use the same Saskatchewan variety data" naming the file, the publisher (SCIC variety yield data by risk zone, if that is what it is), the years, and that yields are acre-weighted averages rolled up from zone x variety rows. If the file is synthetic or modified (module04c labels something "synthetic AREC 261 data"), say so here.
- **[must-fix] Scatter plots: compare two numeric variables** -- The rendered chart's title is "Zones that grow good wheat also grow good barley (2025)" and the prose says "The points rise from left to right, so zones with higher wheat yield also tended to have higher barley yield." The correlation across the 21 zones is r = 0.98 (I computed it from the same aggregation the figure uses). A reader will take that as a striking biological fact. It is mostly a single-year weather effect: both crops in a zone got the same 2025 growing season, so a dry zone is low on both. The same plot in a different year could look different, and the 21 points are one year of one province. The title also asserts a conclusion about zones in general from a single cross-section.
  *Fix:* Change the figure title to a descriptive one ("Spring wheat and barley yield by risk zone, 2025"). In the prose, keep "the points rise from left to right" and add the obvious caveat in one sentence: one growing season, both crops share each zone's weather, so the plot says little about whether good wheat zones are good barley zones in general. Drop "also tended to" as a general claim.
- **[should-fix] Decide whether a chart helps (fig-wheat-two, fig-wheat-fifteen)** -- The prose states "In 2024 the United States produced 53.7 million tonnes of wheat and Canada produced 34.9 million" as flat fact, while the figure caption says "Approximate figures from USDA" and the source code says "Approximate USDA WASDE figures -- close enough for the teaching point." Three decimal-place-precise numbers presented in a sentence as exact, then labelled approximate in the caption, is mixed messaging in a chapter about honest presentation. The chart also mixes a 2024/25 marketing year with "In 2024" in the prose.
  *Fix:* Either use actual USDA PS&D figures for 2024/25 and cite the table (and say marketing year 2024/25 in the prose), or round the numbers in the sentence ("about 54 and 35 million tonnes") and say they are approximate there as well as in the caption.
- **[should-fix] Annotate what needs explanation (fig-fertilizer)** -- The series is labelled "Indicative nitrogen fertilizer prices, USD per tonne" and the axis says "Urea price (USD/tonne)". The source code says the numbers were "assembled from published price commentary rather than a single series" and that "the shape ... is what the example needs, not the exact monthly numbers." The annotations then give causal explanations for each movement ("2008: demand and energy costs spike", "long slide as new capacity comes on", "2022: European gas prices and the war in Ukraine push urea to a record"). A hand-assembled series with confident causal labels is the storytelling trap the chapter elsewhere warns against, and "indicative" in the caption does not tell a student what that word means here.
  *Fix:* Say in the prose or caption that the series is an approximation built for the example, not a published price series, and that the annotations are the author's reading. Better: replace it with a real annual series (e.g., World Bank Commodity Price Data, urea, annual average) and cite it; the same teaching point survives with real numbers.
- **[should-fix] Check the scale and order (fig-truncated)** -- "Both panels below show the same five malting barley varieties, with yields from 103 to 114 bushels per acre." The source code attributes these to the Saskatchewan Seed Guide variety trials and records site-years (43 to 105) that the chapter omits. These are trial means from differing numbers of site-years; the chapter treats the 11 bu/ac spread as the thing to display honestly but says nothing about whether the differences between varieties are meaningful at all. For a chapter about not overstating what the data show, the natural follow-up sentence is missing.
  *Fix:* Cite the Seed Guide (year and table) in the caption, and add one sentence noting these are multi-site trial averages with different numbers of site-years, so the spread between varieties is partly noise; the zero baseline shows the sizes honestly, but it does not tell you which differences are real.
- **[should-fix] Ranges, trend lines and reference lines (fig-extras-1 B)** -- Panel B is drawn with geom_smooth(method = "lm", se = TRUE), so the figure shows a grey confidence band, but neither the caption ("a scatter plot with a fitted line") nor the prose mentions the band or what it represents. The same paragraph says of shaded ranges that "Its definition belongs in the caption." The chapter's own figure violates the rule it just stated.
  *Fix:* Either set se = FALSE so the figure shows only what the text describes, or add to the caption "the shaded band is a 95% confidence interval for the line" with a pointer to the inference modules for what that means.
- **[consider] Add variables carefully (fig-dual)** -- "Avoid putting two different vertical scales on the same panel" and "The apparent crossing point and distance between the lines are controlled by the two scales." The point is correct, but the figure as drawn has no title, no legend, and the two series are identified only by axis-title colour (green and clay). The prose says "Both charts below contain the same canola yield and acreage data" and the caption says "the same two series", but a reader cannot tell from the panel which line is acres. This is an example in a section about labelling.
  *Fix:* Label the lines directly ("Yield", "Acres") at their right-hand ends, as the chapter recommends two sections earlier, so the demonstration does not rely on colour matching.
- **[consider] Pie charts: show a simple share (fig-pie)** -- "The pie shows that canola accounts for about half of the total." The four crops are a subset chosen in load_yields() (spring wheat, canola, barley, oats); "the total" is the total of those four, not of Saskatchewan seeded acres. Since the pie's whole point is parts of a whole, the whole should be named.
  *Fix:* Say "about half of the acres seeded to these four crops" in the prose and in the caption.

## module04b -- Graphing in Excel

### Excel guru

The prose is sound and the advice (summarize first, zero baseline for bars, XY scatter not line) is correct Excel practice. But the chapter leans entirely on the embedded workbook, and that workbook is broken: every number on the Data sheet is stored as text, so the AVERAGEIF tables evaluate to #DIV/0!, the bar and line charts are empty, and the scatter chart has no x-range and plots yield against row number rather than acres. A student opening the sheet sees nothing the chapter describes.

- **[must-fix] Start With the Table / workbook callout** -- In textbook_examples/mod04_graphing_excel.xlsx the Data sheet stores acres, yield_bu_acre and year as text (cells are t="s" shared strings, e.g. D2='202', E2='48.3', F2='2025'). AVERAGEIF skips text, so 'Bar chart'!B5:B7 and 'Line chart'!B5:B6 are cached as #DIV/0! and both charts render blank. The chapter's claim that 'Change a value in the Data sheet and the summary formulas and charts update' is false as shipped.
  *Fix:* Rebuild the Data sheet with true numeric cells (in Excel: select D2:F61, Data > Text to Columns > Finish, or re-export from the CSV with numeric types). Re-open in Excel and confirm B5:B7 show numbers before re-uploading to SharePoint, since the embed shows cached values.
- **[must-fix] Create a Chart (scatter bullet)** -- The scatter chart in the workbook (xl/charts/chart3.xml) has an empty x-value literal (<c:xVal><c:numLit><c:ptCount val="0"/>) and only a y reference to Data!$E$2:$E$61. Excel therefore plots yield against point index 1..60, not against acres, even after the text-number problem is fixed. The 'Scatter plot' sheet's own note says x comes from Data!D2:D61, which is not what the chart does.
  *Fix:* In Excel, right-click the chart > Select Data > Edit the series and set Series X values to =Data!$D$2:$D$61 (or rebuild by selecting D1:E61 and Insert > Scatter). Check the x-axis runs roughly 100-500 acres, not 0-60.
- **[should-fix] Create a Chart** -- 'Excel uses the first column as categories' only holds when that column is text. The Line chart sheet has Year as numbers (2024, 2025) in column A, so a student who selects A4:B6 and inserts a line chart gets two series (Year and Mean yield) with 1, 2 on the horizontal axis. The text acknowledges this can happen but gives no remedy ('return to the source selection and check its shape' is not actionable).
  *Fix:* State the fix: either format the year cells as text (or enter them as '2024), or after inserting use Chart Design > Select Data, remove the Year series, and set it as the Horizontal (Category) Axis Labels. One sentence each.
- **[should-fix] Create a Chart (last paragraph)** -- The chapter recommends an Excel Table as a chart source that grows with new rows, and the Data sheet does contain a Table named FieldYieldsTable, but every formula in the workbook uses fixed ranges (Data!$B$2:$B$61, Data!$E$2:$E$61) and the scatter references Data!$E$2:$E$61 directly. Adding a row 62 updates nothing, contradicting the advice.
  *Fix:* Either make the formulas use structured references (=AVERAGEIF(FieldYieldsTable[region],A5,FieldYieldsTable[yield_bu_acre])) and point the scatter at the table columns, or drop the Table claim from the prose. If you keep it, add a line telling the student the Data sheet is a Table and to type a new row under it to see the charts grow.
- **[should-fix] Titles, Axes and Legends** -- The menu path 'Chart Design > Add Chart Element' is correct on Windows and Mac 365, but the more common route for beginners, the '+' button beside a selected chart, is Windows-only; and the right-click > Format Axis pane differs (Mac opens a sidebar with the Axis Options under a bar-chart icon). Neither platform difference is mentioned. Also 'Bound' minimum is under Axis Options > Bounds > Minimum; naming it helps a first-year find it.
  *Fix:* Write the path as: select the value axis, right-click > Format Axis, then under Axis Options set Bounds > Minimum to 0. Note that the '+' chart button exists on Windows only and that Mac users use the Chart Design tab.
- **[consider] Start With the Table (README / bar chart ordering)** -- The workbook README says the bar chart's categories are 'arranged by value', but the Bar chart table is Central, North, South (alphabetical) and Excel plots in table order. Module 04a tells students to sort bars by value. The chapter never says how to reorder bars in Excel (sort the source table, or Format Axis > Categories in reverse order), which is the one step where Excel is fiddlier than ggplot's reorder().
  *Fix:* Sort the summary table by mean yield (once the means compute) so the README is true, and add one sentence: Excel draws bars in the order of the source rows, so sort the summary table to sort the bars; to put the largest at the top of a horizontal bar chart, tick 'Categories in reverse order' in Format Axis.
- **[consider] Moving a Chart Into a Report** -- 'Paste as Picture' is not the literal label in Word's paste options; the icons are 'Use Destination Theme & Embed Workbook', 'Keep Source Formatting & Embed Workbook', 'Use Destination Theme & Link Data', 'Keep Source Formatting & Link Data', and 'Picture'. 'Save as Picture' on the right-click menu exists in Windows 365 and Mac 2019+, but not in Excel for the web, which is what the embedded/Open online link will give some students.
  *Fix:* Name the option as the 'Picture' paste option (or Paste Special > Picture (PNG)), and add that Excel for the web cannot save a chart as an image, so do the export from the desktop app.
- **[consider] Start With the Table (formula column)** -- Column C on the bar and line sheets is a typed string copy of the formula, not =FORMULATEXT(B5). It will silently drift if the formula is edited, and it departs from the mod01 pattern the CLAUDE.md names as the model.
  *Fix:* Replace C5:C7 and C5:C6 with =FORMULATEXT(B5) etc.

### Hadley Wickham

The chapter gets the order right: summarize to one row per thing you want to draw, then chart, and that is exactly the mental model the ggplot2 chapter builds on. The single biggest problem is that the workbook the whole chapter leans on does not work. The Data sheet stores acres, yield and year as text, so every AVERAGEIF evaluates to #DIV/0!, the bar and line charts have nothing to draw, and the scatter chart has no x series at all. A student who opens the file sees three broken charts and a prose description that does not match.

- **[must-fix] Start With the Table (mod04_graphing_excel.xlsx, Data sheet)** -- On the Data sheet, acres (D), yield_bu_acre (E) and year (F) are stored as text in 57 of 60 rows (openpyxl reports data_type 's'; the cached values of Bar chart!B5:B7 and Line chart!B5:B6 are all #DIV/0!). AVERAGEIF ignores text cells, so the summary tables are errors and the bar and line charts are empty. The README's claim that the Data sheet is 'left in analysis-ready form' is false, and the prose's 'Change a value in the Data sheet and the summary formulas and charts update' cannot be demonstrated.
  *Fix:* Regenerate the workbook writing D, E and F as numbers (write floats/ints, not strings, when building the file), re-save from Excel so cached values are populated, and confirm Bar chart!B5:B7 shows 47.6, 47.3, 42.7 and Line chart!B5:B6 shows 45.8, 46.0 (true means from the 57 non-missing yields).
- **[must-fix] Create a Chart (Scatter plot sheet)** -- The scatter chart's series has no x reference (xVal is an empty numLit with ptCount 0) and the y cache is all zeros. The text says 'The scatter plot selects acres and yield'; the actual chart does not reference Data!D2:D61 at all.
  *Fix:* Rebuild the scatter series with xVal = Data!$D$2:$D$61 and yVal = Data!$E$2:$E$61, and after fixing the text-number problem above, verify in Excel that 57 points appear spread over roughly 130 to 470 acres.
- **[should-fix] Start With the Table** -- The chapter's strongest idea, that a chart wants a table with one row per mark, is asserted but never shown. The student is told the Bar chart sheet 'calculates one mean for each region' but never sees what that table looks like or that it has three rows because there are three bars. In the R chapter the same idea is made concrete (summarise, then geom_col). Here there is no picture of the before (60 field rows) and after (3 summary rows).
  *Fix:* Add one sentence making the rule explicit and concrete: 'The bar chart has three bars, so the table feeding it has three rows: one region and one mean per row. The scatter plot has one point per field, so it reads the sixty rows directly.' Then tell the student to look at Bar chart!A4:B7 and compare it with Data!A1:H61.
- **[should-fix] Start With the Table** -- Three fields have a missing yield (e.g. F007). AVERAGEIF silently skips blanks and the scatter silently drops them, which is fine, but the chapter does not say so, and the Module 3 cleaning chapter makes a point of missing values. The Scatter plot sheet mentions it in a note; the prose does not, so a student checking 'Compare one plotted value with its source cell' will count 57 points and not know why.
  *Fix:* Add one sentence in Start With the Table: 'Three fields have no recorded yield. AVERAGEIF leaves them out of the means and the scatter plot draws no point for them, so the charts represent 57 fields, not 60.'
- **[should-fix] Create a Chart** -- 'Excel uses the first column as categories and the other columns as series. If the chart treats years or regions as a series, return to the source selection and check its shape.' This is the one place a student will actually get stuck (years 2024, 2025 being plotted as a second series because they are numbers), and the text names the symptom without the fix. Excel's rule is that a numeric first column is treated as data unless the header cell is blank or the values are text.
  *Fix:* State the cause and a remedy: when the first column is numbers (years), Excel plots it as a series; either use Chart Design > Select Data to set the horizontal axis labels to the year column, or pick the XY scatter type when both columns are measurements. Show the Select Data dialogue as the general fix, since it is what students need whenever the guess is wrong.
- **[should-fix] Create a Chart** -- 'An Excel Table expands when rows are added and is usually a better source for a chart that will receive new data.' This is the right advice but it is dropped in one sentence with no demonstration, and the workbook's Data sheet is a plain range, not a Table, so the student cannot see the behaviour.
  *Fix:* Either convert the Data sheet to an Excel Table in the workbook (Ctrl+T) and change the AVERAGEIF ranges to structured references like Data[yield_bu_acre], so adding a row 62 visibly updates the charts, or cut the sentence. Half-stating a convention without an example adds load without payoff.
- **[consider] Titles, Axes and Legends** -- The Bar chart sheet notes say categories are arranged 'Highest to lowest', but the table is simply Central, North, South in alphabetical order; it happens that the true means (47.6, 47.3, 42.7) are also descending. Nothing in the table sorts by value, so if a student edits the Data sheet the ordering claim becomes false, and the prose never tells the student how to order bars in Excel at all (sort the source table; the chart follows row order).
  *Fix:* Add one sentence to Titles, Axes and Legends: 'Excel draws bars in the order of the source rows. To rank them, sort the summary table by the mean column; the chart follows.' And either sort the table in the workbook by value or change the note to say the order is alphabetical.
- **[consider] Moving a Chart Into a Report** -- 'Excel can also save a chart as an image from its right-click menu' is platform-dependent; on Mac Excel the right-click menu has Save as Picture, on Windows it is also there but many students on the web version do not have it. The chapter does not say which Excel it assumes, and Module 1 students are on a mix.
  *Fix:* Say which Excel the instructions are written for (desktop, Windows/Mac) and give the fallback that works everywhere: copy the chart and paste into Word/PowerPoint as a picture, or use a screenshot tool at full size.

### Andrew Gelman

The prose is plain and mostly says no more than it can back up, which I appreciate; the problem is that the claims it does make are about a workbook I checked, and the workbook does not do what the chapter says. Every number in the Data sheet is stored as text, so the AVERAGEIF tables evaluate to #DIV/0! and the bar and line charts are reading error cells. Beyond that, the worked examples quietly make the mistakes the previous chapter warns about: a two-point "trend" line, a ranking of three means that differ by less than a bushel in a dataset with six-bushel standard deviations, and three dropped observations that the prose never mentions.

- **[must-fix] Start With the Table (#sec-excel-chart-table)** -- The chapter says "Change a value in the Data sheet and the summary formulas and charts update" and "The bars in the workbook begin at zero." In mod04_graphing_excel.xlsx every cell in the Data sheet, including acres, yield_bu_acre and year, is stored as a text string ('202', '48.3', '2025'). AVERAGEIF ignores text, so the cached values in Bar chart!B5:B7 and Line chart!B5:B6 are all #DIV/0!, and the charts those sheets contain are plotting error cells. A student who opens the workbook sees empty charts under a chapter that describes them in detail.
  *Fix:* Rewrite the Data sheet with acres, yield_bu_acre and year as numeric cells (and seeding_date as a date), recalculate, and confirm the three means (Central 47.6, North 47.3, South 42.7; 2024 45.8, 2025 46.0) actually appear before publishing. Add a one-line check to the chapter's final paragraph: if a summary cell shows #DIV/0! or a chart is blank, the source column is text, which connects back to Module 3's cleaning material.
- **[should-fix] Create a Chart (#sec-excel-chart-build)** -- "Excel uses the first column as categories and the other columns as series" is stated as a rule, then hedged with "If the chart treats years or regions as a series, return to the source selection and check its shape." The rule is not true: Excel treats a numeric first column as a series, and the workbook's own Line chart sheet (Year stored as 2024, 2025 in column A) is exactly the case where it fails. The student is told to "check its shape" without being told what shape causes the problem.
  *Fix:* State the actual behaviour: Excel treats a text first column as categories and a numeric first column as another series, so a year column plots as a line unless you use Select Data to move it to the horizontal axis or store the years as text. Drop the vague "check its shape".
- **[should-fix] Start With the Table (#sec-excel-chart-table)** -- The line chart is mean yield for two years, 2024 and 2025, with the sheet title "Average field yield changed little between 2024 and 2025." Module 4a opens with the principle that "Two numbers can usually be written in a sentence" and that a chart helps for a trend. A two-point line is not a trend; it is two numbers (45.8 and 46.0) with a line drawn between them, and the chapter presents it as the model line chart without comment.
  *Fix:* Either extend the synthetic data to five or six years so the line chart has something to show, or keep the two years and say plainly in the prose that with two points the line chart is there to show the mechanics and the honest presentation would be a sentence. Do not leave the example silently contradicting the principle it cites.
- **[should-fix] Titles, Axes and Legends (#sec-excel-chart-elements)** -- The bar chart ranks three regional means "Highest to lowest" (per the sheet's check table), but Central (47.6) and North (47.3) differ by 0.35 bu/ac in groups of 19 fields with standard deviations of 5 to 7 bu/ac. The ordering presents as a ranking a difference that is far inside the noise, and nothing in the chapter or sheet tells the student that a bar of a mean hides the spread of the fields behind it.
  *Fix:* Add one sentence where the summary table is introduced: each bar is the mean of 19 fields whose yields spread about 6 bu/ac either side of it, so the Central/North order is not a finding. Put n and the standard deviation in the summary table (two more AVERAGEIF/STDEV columns) so the reader can see what the bar is summarizing. Consider whether sorting by value is the right default when two values are effectively tied.
- **[should-fix] Start With the Table (#sec-excel-chart-table)** -- Three of the sixty fields have no yield (e.g. F007). The Scatter plot sheet notes that "fields with missing yield are not plotted," but the prose never mentions missing values, and the AVERAGEIF means on the bar and line sheets exclude them without saying so. "The workbook in this chapter uses sixty hypothetical canola fields" implies sixty observations in every chart; the charts use 57.
  *Fix:* State in the prose, once, that three fields lack a recorded yield, that AVERAGEIF and the scatter both drop them, and that the summary table should report the count it averaged (a COUNTIF column next to the mean). This is where the link to Module 3's missing-data material belongs.
- **[consider] Titles, Axes and Legends (#sec-excel-chart-elements)** -- "The line chart uses a narrower vertical range because its values are positions connected through time rather than lengths measured from a baseline" gives a principle as the reason, but the actual choice (axis 40 to 50) is what decides how a 0.2 bu/ac change reads. The sheet's own note, "Range kept close to the observations," is not accurate either: the observations run from roughly 30 to 60; the range is kept close to the two means.
  *Fix:* Say what was done and why: the axis runs 40 to 50 so the 0.2 bu/ac difference is visible as nearly flat; a 45 to 47 axis would make the same two numbers look like growth. Correct the sheet note to "Range kept close to the two annual means" or, better, show the field-level spread.
- **[consider] Start With the Table (#sec-excel-chart-table)** -- The scatter sheet is titled "Field size and yield have little visible relationship" (r = -0.07 across 57 fields), and that is an accurate description of this synthetic dataset. But the chapter prose says nothing about what the scatter shows, and a student may read the sheet title as a fact about canola rather than about sixty invented fields.
  *Fix:* Add a sentence in the prose noting that the scatter shows no clear pattern in these data and that the data are synthetic, so this says nothing about real fields. One clause, not a warning box.

## module04c -- Graphing in R

### Excel guru

The R code is correct and every figure renders, and the chapter reads cleanly. But the R side covers histograms, box plots, fitted lines, transparency, colour-by-group and facets while the Excel chapter (module04b) only covers bar, line and scatter, and this chapter never tells the student which of those extra tools Excel also has (Statistical charts, Trendline, marker transparency) and which it genuinely lacks (facets, colour mapped to a column without restructuring the table). The side-by-side promise of the book is broken here: a student will leave believing you need R for a histogram or a trendline.

- **[must-fix] Common Geoms (geom_histogram, geom_boxplot, geom_smooth)** -- The chapter introduces histograms, box plots and a fitted line with a confidence band as R features, and module04b never mentions them, so the student is left to conclude Excel cannot do them. Excel 365 (Windows and Mac) has Insert > Charts > Statistical (Histogram, Box and Whisker) and Chart Design > Add Chart Element > Trendline > Linear (with an option to display the equation and R-squared). The bin control the text insists on ('state the number of bins or the bin width') is Format Axis > Bin width / Number of bins in Excel's histogram chart.
  *Fix:* Add one short sentence under each of the histogram/boxplot and geom_smooth subsections naming the Excel equivalent (Insert > Statistical chart; Add Chart Element > Trendline > Linear), or add a 'Histograms, box plots and trendlines' section to module04b and cross-reference it from here. Either way, say which parts Excel lacks: a Box and Whisker chart needs the groups side by side as separate columns, and Excel's trendline has no confidence band.
- **[should-fix] Mapping and Setting Aesthetics** -- `colour = region` in `aes()` is the single most-asked 'how do I do this in Excel' question, and neither chapter answers it. In Excel a scatter cannot colour points by a category column; you must split the data into one series per region (one yield column per region, or a PivotTable / FILTER()) and add each as a separate series under Select Data. Without this, students will try 'Vary colors by point' and get nonsense.
  *Fix:* After the map/set example, add two sentences: in Excel, colouring by a group means one series per group (restructure with FILTER() or a PivotTable, then Select Data > Add), which is why the one-line `aes(colour = region)` is the clearest advantage of ggplot for this task.
- **[should-fix] Faceting** -- Facets are presented with no Excel reference. Excel has no faceting; the nearest things are a PivotChart with a slicer/filter (one panel at a time) or manually building one chart per group with matched axes. Since module04a tells students to compare groups, they need to know the Excel path is manual and that the axes will not be shared unless they set Format Axis bounds identically on each chart.
  *Fix:* Add one sentence: Excel has no equivalent; use a PivotChart with a slicer to flip between groups, or build one chart per group and set the same axis bounds on each.
- **[should-fix] geom_line(): change over time** -- `year` is numeric, so the rendered line chart's x axis shows 2024.00, 2024.25, 2024.50, 2024.75, 2025.00 (verified: layer_scales breaks). Excel would treat the same column as a category axis and label just 2024 and 2025, so a student comparing the two outputs will think R made a mistake. The text does not mention the fractional ticks.
  *Fix:* Add `scale_x_continuous(breaks = c(2024, 2025))` to the example (or `aes(x = factor(year), group = 1)`) and one sentence explaining that ggplot treats year as a number, whereas Excel's line chart treats the first column as labels.
- **[should-fix] A First Plot / Common Geoms (na.rm = TRUE)** -- Every geom carries `na.rm = TRUE` but the argument is never explained. The dataset has 3 missing yields; without it R prints 'Removed 3 rows containing missing values or values outside the scale range (geom_point())', which a first-year student will read as an error. Excel charts drop blank cells with no message (and a line chart shows a gap), so the contrast is the point.
  *Fix:* After the first plot, add two sentences: the data has three fields with no recorded yield; ggplot drops them and warns, and `na.rm = TRUE` suppresses the warning once you know why they are missing. Excel drops blanks without telling you.
- **[should-fix] Saving Plots** -- `ggsave("output/region_boxplot.png", ...)` fails if the `output` folder does not exist: 'Cannot find directory ... use `create.dir = TRUE`' (verified with ggplot2 4.0.3). A student following the code in a fresh project gets an error the text does not anticipate. Also, 'PDF or SVG' -- SVG output needs the svglite package, which is not part of the tidyverse install.
  *Fix:* Either add `create.dir = TRUE` to the call or tell the student to create an `output` folder first; drop SVG or note it needs `svglite`. A sentence comparing to Excel's right-click > Save as Picture (which has no dpi control) would close the loop with module04b.
- **[consider] geom_col() and geom_bar(): bars** -- The bar section says geom_bar counts rows for you. In Excel there is no counting chart: you need COUNTIF or a PivotTable first, then a column chart. The Excel chapter's 'Start With the Table' covers means but not counts, so the geom_bar example has no Excel parallel.
  *Fix:* One sentence: Excel has no counting chart; summarise with COUNTIF or a PivotTable and chart the result, which is what geom_bar does internally.
- **[consider] Introduction (read_csv path)** -- The link points to `practice/data/field_yields.csv` but the code reads `data/field_yields.csv`. Students who download from the link and save it in their project will get a file-not-found error unless the project layout from Module 3 (a `data/` folder) has been set up. The chapter assumes that without saying so.
  *Fix:* Add a half-sentence: 'saved in the project's data folder (@sec-reading-data)'.

### Hadley Wickham

The chapter gets the core model right and in the right order: data, mapping, geom, then mapping-versus-setting, then labels, facets and saving, each with one small running dataset. Its biggest weakness is that the examples are written to never produce a message or warning, so students never see the two things ggplot2 will say to them most often (rows removed for missing values, and the `geom_smooth()` formula message), and the line-chart example is built on a two-point numeric year axis that does not make the case for `geom_line()` at all.

- **[must-fix] Common Geoms / geom_line(): change over time** -- The line chart connects two points (2024 mean 45.8, 2025 mean 46.0). `year` is a double, so the x axis will carry fractional breaks (2024.00, 2024.25, ...) for a variable that only takes integer values, and two points do not show a trend. Nothing in the text warns about either. This is the one geom in the chapter where the example actively works against the concept.
  *Fix:* Use a series with enough points to be a line. `rm_yields_2015_2024.csv` is already used in Module 3 and has ten years; summarise mean yield per year from it and plot that. If you keep field_yields, at least add `scale_x_continuous(breaks = 2024:2025)` and say why (a numeric axis does not know that years are integers). Either way, this is a natural place to introduce a scale function, since the Grammar section lists Scales and the chapter never uses one.
- **[must-fix] A First Plot / Common Geoms (all chunks)** -- Every geom carries `na.rm = TRUE` and every chunk also sets `warning: false` and `message: false`. The data has three missing yields. The first time a student runs `geom_point()` on this file they will see `Removed 3 rows containing missing values or values outside the scale range (geom_point())`, and the chapter has arranged never to show it or explain it. `na.rm = TRUE` is also clutter in every listing; it is not idiomatic to type it by default, and here it exists only to silence something the student needs to learn to read.
  *Fix:* Drop `na.rm = TRUE` everywhere. In the first plot, let the warning render (remove `warning: false` on that chunk) and add two sentences: the file has three fields with no recorded yield, ggplot2 drops them and tells you; the warning is information, not an error, and `na.rm = TRUE` suppresses it once you have confirmed the count is what you expect. Keep `warning: false` on later chunks if you want clean figures, but the student has now seen the message once.
- **[should-fix] Mapping and Setting Aesthetics** -- The closing paragraph describes the classic mistake (`aes(colour = "steelblue")`) in words only: 'producing an unnecessary legend'. This is the single most show-don't-tell moment in ggplot2 teaching, and the chapter tells. A student who reads the sentence still will not recognise the salmon-coloured points with a legend titled `colour` when they produce it themselves.
  *Fix:* Add a third small panel (or a separate figure) that actually runs `ggplot(yields, aes(x = acres, y = yield_bu_acre, colour = "steelblue")) + geom_point()` and caption it as the mistake. The picture explains mapping versus setting better than any paragraph; the current two-panel figure plus this one is the whole lesson.
- **[should-fix] Common Geoms / geom_smooth(): fitted patterns** -- Two issues. (1) `geom_smooth()` prints `geom_smooth() using formula = 'y ~ x'` every time; the chunk hides it with `message: false`, so students will meet it unexplained. (2) The correlation between acres and yield in this file is about -0.07, so the fitted line is essentially flat with a wide band. The text says 'The line summarizes the relationship present in these observations' without noting that the relationship shown is roughly none, which is the actual reading of this figure.
  *Fix:* Write `geom_smooth(method = "lm", formula = y ~ x)` so the message does not appear and the listing matches what students get, or show the message once and say it is ggplot2 telling you which model it fit. Then add a sentence reading the figure honestly: a nearly flat line with a band that comfortably includes horizontal means field size tells you little about yield here. That is a good lesson and it is free.
- **[should-fix] Saving Plots** -- `ggsave("output/region_boxplot.png", ...)` fails with `Cannot open file ... No such file or directory` unless an `output/` folder already exists. The render works because the textbook folder has one; a student following along does not. The chunk also uses `labs(x = NULL, ...)` without explaining that NULL removes the axis title, and the lead sentence ('Save a ggplot object before passing it to ggsave()') reads as a rule when it is a convention; `ggsave()` defaults to the last plot drawn.
  *Fix:* Add `dir.create("output", showWarnings = FALSE)` before the `ggsave()` call, or say in one sentence that the folder must exist. Add a clause for `x = NULL` ('NULL removes the axis title; the region names are self-explanatory'). Reword the lead: assigning the plot to a name is the habit to form because it makes the `plot =` argument explicit and lets you reuse the object; without it `ggsave()` saves whatever was drawn last.
- **[should-fix] The Grammar of Graphics** -- The five-part list includes Scales, but no `scale_*()` function appears anywhere in the chapter, and the sentence about geoms that 'calculate a statistic' introduces stats as a sixth part without naming it. A student reading this section has no example attached to 'Scales' and so nothing to hang it on.
  *Fix:* Either attach one concrete use of a scale somewhere (the `scale_x_continuous(breaks = ...)` fix to the line chart does this, or `scale_colour_brewer()` in the Labels and Themes figure), or cut Scales from the list and say that the remaining parts ggplot2 handles by default until you need to change them. Listing a component the chapter never touches adds load without payoff.
- **[consider] Common Geoms / geom_col() and geom_bar(): bars** -- Both bar charts and the box plot present regions in alphabetical order (Central, North, South) because `region` is character. For a comparison chart the first thing a reader wants is bars ordered by height, and students will ask how to do it the moment they make their own.
  *Fix:* In the `geom_col()` example, show `aes(x = fct_reorder(region, mean_yield), y = mean_yield)` with one sentence: character columns are drawn alphabetically; `fct_reorder()` (forcats, loaded with the tidyverse) orders the categories by another variable. Keep it to that one call; the full factor story can wait.
- **[consider] Faceting** -- `facet_grid(region ~ variety)` is named but never shown. With three regions and three varieties it would be a readable 3x3 grid on this data, so the 'use it only when the number of panels remains readable' advice has no picture to make it concrete. Separately, `facet_wrap(~ region)` is the older formula interface; `facet_wrap(vars(region))` is the form the rest of the tidyverse uses and is what students will see in recent documentation.
  *Fix:* Show the `facet_grid(region ~ variety)` (or `facet_grid(vars(region), vars(variety))`) histogram as a second figure; it doubles as a demonstration of why `bins = 8` on 60 rows split nine ways starts to look thin. Mention that `~ region` and `vars(region)` are equivalent so students recognise both.

### Andrew Gelman

The chapter is admirably plain: it teaches the grammar, shows the code, and does not tell the student how to feel. Its main weakness is that it lets the figures make claims the prose never examines -- a two-point "trend" line, a regional bar chart with no sense of spread, a regression line whose slope is indistinguishable from zero -- and says nothing about the three missing yields or the fact that the data are invented, so a student learns the syntax of each plot without learning to ask what the plot is actually evidence of.

- **[must-fix] geom_line(): change over time (fig-r-line)** -- The caption reads "Mean field yield in 2024 and 2025" and the text says "geom_line() then connects those two rows in year order." The two means are 45.8 and 46.0 bu/acre, from 33 and 27 different fields. Drawing a line between two points invites the reader to see a trend; with two years, different fields each year, and a 0.2 bu/acre difference against a within-year SD of about 6, there is no trend to see. The chapter says nothing about this and the figure's y-axis, which ggplot will zoom to the two values, will make the change look large.
  *Fix:* Either pick an example with enough time points to justify a line (e.g. aggregate by seeding month, or use a different dataset), or keep the example and add one sentence saying outright that a line through two annual means of different fields shows almost nothing, and that the apparent slope depends entirely on the y-axis range ggplot chose. Consider adding `scale_y_continuous(limits = c(0, NA))` or `expand_limits(y = 0)` in the example to make the point visibly.
- **[must-fix] geom_smooth(): fitted patterns (fig-r-smooth)** -- "The line summarizes the relationship present in these observations." The fitted slope is -0.0039 bu/acre per acre with SE 0.0072 (t = -0.54). There is no discernible relationship, and the confidence band will be wide and will contain a horizontal line. The sentence asserts a relationship exists; the figure shows it does not. A student will come away thinking the line is the finding.
  *Fix:* Replace with something like: "Here the line is nearly flat and the shaded band is wide -- in these sixty fields there is no clear relationship between field size and yield. The line is drawn whether or not a relationship exists, so look at the band and the scatter, not just the line. Module 7 explains what the line estimates." Also say in one clause what the band is (a 95% confidence band by default) rather than calling it "an interval around it".
- **[should-fix] geom_col() and geom_bar(): bars (fig-r-col)** -- "Mean field yield by region" shows Central 47.6, North 47.3, South 42.7 with no indication of the number of fields per region (19--21) or the spread (SD 5--7). A bar chart of group means with nothing else is the canonical way to make a difference look settled. The text's only guidance is mechanical ("Use geom_col() when the bar heights have already been calculated").
  *Fix:* Add a sentence after the figure: a bar of means hides how many fields are behind each bar and how much they vary; the box plot in the next section shows the same comparison with the spread, and is usually the better choice for this question. That links the two examples and gives the student a reason to prefer one.
- **[should-fix] Introduction / data loading** -- The chapter says the fields are "hypothetical" and the plot caption says "synthetic AREC 261 data", but never says what that means for interpretation: the numbers were generated, so any pattern (or lack of one) in these figures tells you nothing about canola. Three of sixty yields are also NA (F007 and two others), and every chunk carries `na.rm = TRUE` without a word about why.
  *Fix:* One sentence after the glimpse: the file is simulated for teaching, so patterns in it are not facts about canola; and three fields have no recorded yield, which is why `na.rm = TRUE` appears in the geoms -- without it ggplot drops the rows anyway but warns, and the warning is worth seeing at least once. Then drop `#| warning: false` from one chunk so the student sees the warning.
- **[should-fix] geom_histogram() and geom_boxplot(): distributions** -- "State the number of bins or the bin width rather than accepting the default without looking at it." Good advice but unexplained: the student does not learn that the shape of a histogram changes with bin width, which is the actual reason to look. The chapter uses bins = 12 for all sixty fields and bins = 8 per facet (roughly 20 fields per panel) with no comment on what different choices show.
  *Fix:* Add one sentence: with sixty values the histogram looks quite different at 5, 12 and 30 bins, and none is the "true" shape; try two or three and report the one you used. Optionally show the same data at two bin counts side by side using fig-subcap, as the chapter already does for mapping vs setting.
- **[consider] Faceting (fig-r-facet)** -- Three panels of about 20 fields each at 8 bins means roughly 2--3 fields per bar. Differences between panels at that count are mostly noise, and the caption "One yield histogram for each region" leaves the student to read regional differences into them. The earlier box plot shows the regional comparison better with the same data.
  *Fix:* Either note that with ~20 fields per panel the histograms are too coarse to compare shapes, and that facets pay off with more data per panel; or facet something with more observations per panel (e.g. the scatter plot by region, where each panel keeps all its points).
- **[consider] Mapping and Setting Aesthetics (fig-r-map-set)** -- Panel A maps colour to region over the size--yield scatter. The text explains the mechanics only. The figure shows South fields sitting lower, which is a real feature of the dataset (mean 42.7 vs ~47.5), but nothing in the chapter says whether the student should see that or not, and the same plot is reused as the "polished" example with a title "Canola yield by field size" -- a title naming the relationship that the smooth section shows is absent.
  *Fix:* Retitle the polished plot to describe what is shown rather than assert a relationship (e.g. "Field size and yield, by region"), and add a line in one of the two sections that the colours separate the regions more than the horizontal axis does, which is the kind of thing a plot is for noticing and a later module is for testing.
- **[consider] Saving Plots** -- The console chunk calls `ggsave("output/region_boxplot.png", ...)`. The chunk executes at render time, so it either writes into the book directory's output/ folder or errors if the folder does not exist; the student sees no output either way and is not told the folder must exist first. Minor, but the chapter's whole pitch is that console() output is real.
  *Fix:* Either say the `output/` folder must already exist (ggsave does not create it), or switch this chunk to a static listing so the rendered book does not depend on a side-effecting write.

## module05a -- AI Tools

### Excel guru

The silent-failure example is the best thing in the chapter: a join that returns a plausible number instead of an error is exactly the kind of mistake students make, and the row-count/NA-count check is the right lesson. The biggest problem is that a book that teaches Excel and R side by side has written its AI chapter as though Excel did not exist: no mention of Copilot in Excel, no Excel version of any failure mode, and the one worked example happens to be a case where Excel's XLOOKUP would have shouted #N/A where R stayed quiet -- a contrast the chapter could use rather than ignore.

- **[must-fix] Three Ways to Use AI for Code (#sec-ai-generations)** -- The taxonomy (chatbot, in-editor assistant, coding agent) is R/Positron-only. Every student with a Microsoft 365 Copilot licence has an in-editor assistant sitting in Excel's Home ribbon that will write formulas, add formula columns, build PivotTables and charts, and 'Get insights' from a table. It is the AI tool most of these students will meet at work and in other classes, and the chapter never names it. Its failure modes are also different: it only works on data formatted as a Table (Ctrl+T), it writes the formula into the sheet for you, and it will summarise a column visually without a formula you can audit.
  *Fix:* Add a short paragraph to this section on Copilot in Excel as the Excel form of an in-editor assistant: where it lives (Home > Copilot, Microsoft 365 on Windows, Mac and web; requires a Copilot licence, not just Microsoft 365), that it needs the data in a Table, that it offers 'add a formula column' (auditable, the formula is in the cell) and 'show insights' (a chart or PivotTable it builds, which you should check against a SUMPRODUCT or PivotTable of your own). Note that the USask licensing situation may vary and point to the access note in @sec-copilot-signup.
- **[must-fix] A Silent Failure (#sec-ai-silent-failure)** -- The example is presented as a general lesson about silent join failures, but in Excel the same merge (XLOOKUP price by crop, then SUMPRODUCT of Revenue and Acres / SUM of Acres) is not silent at all: 1,051 rows show #N/A and SUMPRODUCT returns #N/A, so the wrong number never appears. Module 3b already teaches this ('An unmatched XLOOKUP returns #N/A, which is easy to count'). The chapter's R-only framing misses that the silent version in Excel is different and just as common: an AI suggesting =VLOOKUP(B2,prices,2) with the approximate-match default TRUE, or wrapping the XLOOKUP in IFERROR(...,0) because 'the #N/A errors look ugly', both of which produce a plausible revenue with no warning.
  *Fix:* After the corrected R result, add two or three sentences contrasting Excel: XLOOKUP makes the unmatched rows visible as #N/A, which is the behaviour you want; the silent Excel failures are VLOOKUP's approximate-match default (on sorted text it returns the nearest preceding crop name, e.g. 'Canola/Rapeseed' picks up 'Canola' by accident and 'Wheat - Hard Red Spring' picks up 'Spring wheat' or 'Soybeans' depending on sort) and an AI-suggested IFERROR or IFNA wrapper that turns #N/A into 0. The check is the same: =COUNTIF(price_col,"#N/A") or =SUMPRODUCT(--ISNA(price_col)) before any revenue number is reported.
- **[should-fix] Common Failure Modes -- Invented functions and packages** -- The section says 'R stops with an error, so this is usually easy to find.' The Excel equivalent is not stated and behaves differently: an invented function returns #NAME? in one cell, which is visible, but a real function that is version-dependent is the more common AI failure. Models regularly suggest TEXTSPLIT, GROUPBY, PIVOTBY, REGEXEXTRACT, LET, LAMBDA or VSTACK, which exist only in Microsoft 365 (GROUPBY/PIVOTBY/REGEX* only in current-channel 365 as of 2025), and return #NAME? on Excel 2019/2021 or an older university lab install. The student cannot tell an invented function from a too-new one.
  *Fix:* Add one sentence: in Excel an invented or unsupported function shows #NAME? in the cell, and the same error appears when an AI suggests a Microsoft 365-only function (GROUPBY, PIVOTBY, TEXTSPLIT, REGEXEXTRACT) to someone on an older version; check the function on Microsoft's support page, which lists the versions that have it.
- **[should-fix] Common Failure Modes -- Assumed column names** -- The fix offered ('Supply glimpse() output') has no Excel counterpart, yet the failure is at least as common in Excel: the model writes =SUMIFS(Yield,Crop,"Canola") against a sheet whose headers are 'Yield (bu/ac)' and 'Crop_Name', or assumes the data is in A:F when it starts in B3, or writes Table structured references (Table1[Yield]) when the data is not a Table.
  *Fix:* Add the Excel version of the remedy: paste the header row and two or three data rows into the prompt, say which sheet and range the data occupies, and say whether it is formatted as a Table. If using Copilot in Excel, formatting as a Table is what lets it see the headers at all.
- **[should-fix] Common Failure Modes -- Numbers produced without a reproducible calculation** -- 'Ask for code, run it in R and keep the calculation in the script' is the only remedy given. The Excel equivalent of this failure is the chatbot returning a number that the student then types into a cell as a constant, or Copilot's 'show insights' producing a figure with no formula behind it. The principle (keep a formula you can audit) is identical and unstated.
  *Fix:* Add one sentence: in Excel, the same rule is that every reported number should come from a formula in a cell, not a typed value; if an AI gives you a number, ask it for the formula instead and put that in the sheet where FORMULATEXT can show it.
- **[should-fix] Data That Should Not Be Sent (#sec-ai-privacy)** -- The paragraph on in-editor assistants only references the Positron/Copilot context setting. Copilot in Excel sends the entire Table (not just selected columns) to the service, and students assume a Microsoft product inside Excel is 'local'. The distinction that matters in practice -- Microsoft 365 Copilot under an organisational tenant stays within the tenant's data boundary, whereas a consumer copilot.microsoft.com or ChatGPT account does not -- is what tells a student whether they can use it on a producer's records.
  *Fix:* Add two sentences: Copilot in Excel sends the whole Table to Microsoft's service; under a university or employer Microsoft 365 account it stays inside that organisation's tenant, which is usually what an 'approved service' means, while pasting the same sheet into a consumer chatbot is not. When in doubt, send headers and a few made-up rows.
- **[consider] Tasks That Are Easy to Check (#sec-ai-good-uses)** -- All five bullets are R tasks (R error message, function documentation, case_when, unfamiliar line of code, plot draft). Excel has equally good easy-to-check uses that the list could name: explaining what a nested IF or SUMPRODUCT does, turning a wordy condition into a SUMIFS, explaining a #VALUE!/#SPILL!/#REF! error, or drafting the M code for a Power Query step you can read in the Advanced Editor.
  *Fix:* Add two Excel bullets, e.g. 'Explain an Excel error value (#SPILL!, #VALUE!, #REF!) and which cell caused it' and 'Draft a SUMIFS or XLOOKUP whose result you can check by filtering the sheet and reading the matching rows.'
- **[consider] The Rest of This Module (#sec-ai-module-plan)** -- The module plan is Positron and ggplot only. A student who came through Modules 1-4 doing half the work in Excel is told nothing about whether AI will be used with Excel in this module or ever. If the deliberate choice is to do the AI module in R only, the chapter should say so in one sentence so the Excel track does not look forgotten.
  *Fix:* Add one sentence stating the scope: e.g. this module practises AI assistance in Positron because the code is visible and checkable in a script; the same checking habits apply to Copilot in Excel, where the formula it writes is the thing to read.

### Hadley Wickham

The silent-join example is the right centrepiece: a plausible number, a real dataset, and a check that catches it, and every number in the chapter reproduces exactly. The biggest problem is that the example does not use the tools Module 3b just taught for precisely this situation (anti_join, relationship =), and it reaches for recode(), which dplyr has superseded, in a chapter whose own failure list includes "out-of-date syntax".

- **[must-fix] A Silent Failure (d25_fixed chunk, line 180)** -- `recode()` has been superseded since dplyr 1.1.0 in favour of `case_match()`. The chapter warns two sections earlier that models emit out-of-date syntax; the chapter then does it itself. A student who asks an assistant "is recode still current?" will be told no.
  *Fix:* Replace with `mutate(Crop = case_match(Crop, "Canola/Rapeseed" ~ "Canola", "Wheat - Hard Red Spring" ~ "Spring wheat", .default = Crop))`. Note `.default = Crop` is required or Barley and Oats become NA, which is itself a nice one-line illustration of a silent failure. Also update `recode()` inside `fig_join_loss()` in R/ai_failures.R.
- **[must-fix] A Silent Failure (first check chunk, lines 146-158)** -- Module 3b teaches `anti_join()` as the tool for finding failed matches before a join and uses it twice. This chapter instead joins first and then counts `is.na(Price_per_bu)`. That works, but it silently abandons the routine students were just taught, and it only works because Price_per_bu happens to have no genuine NAs. The chapter should show the check that generalises.
  *Fix:* Before the join, add `anti_join(d25, p, by = "Crop") |> distinct(Crop)` (returns the two mismatched names directly) and cross-reference @sec-joins or whatever 3b's anchor is. Keep the `count(Crop, price_missing = is.na(Price_per_bu))` afterwards if you like, but lead with anti_join so the routine is: anti_join, join with relationship =, row count.
- **[should-fix] A Silent Failure (closing paragraph, line 200)** -- "The main protection was ... checking the join with a row count and a missing-value count." The row count is never actually compared to anything in the chapter (nrow(j) prints 1416 and the reader has no prior number to compare it against), and the real culprit, `na.rm = TRUE` hiding 1,051 NAs, is left as a one-off observation rather than a rule.
  *Fix:* Either print `nrow(d25)` before the join so the 1416 == 1416 comparison is visible, or drop the row-count claim. Then state the rule plainly: `na.rm = TRUE` should only appear after you have counted the NAs and can say where they came from. Consider showing `stopifnot(!anyNA(j$Price_per_bu))` as a one-line guard that turns this silent failure into a loud one; it is the most transferable thing in the chapter.
- **[should-fix] A Silent Failure (first chunk, line 130)** -- The filter `Acres > 0` is doing hidden work: it drops the 3 rows where Acres is NA (filter drops NA conditions), and nothing in the text says so. In a chapter about code that quietly removes rows, an unexplained row-dropping condition in the "looks reasonable" code is a missed opportunity or, read uncharitably, the same sin.
  *Fix:* Either write `!is.na(Acres), Acres > 0` so the intent is explicit, or add one sentence noting that `filter()` drops rows where the condition is NA, and that this is why the first thing to print after a filter is its row count.
- **[should-fix] A Silent Failure (first chunk, line 114 and rendered transcript)** -- Because the chunk runs `library(tidyverse)` and two unsuppressed `read_csv()` calls, the rendered console transcript opens with ~20 lines of tidyverse attach messages and column specs before the first real output. The student has to scroll past that to find 476.79. Other modules use `show_col_types = FALSE` or suppress the attach block.
  *Fix:* Add `show_col_types = FALSE` to both `read_csv()` calls (which is also the idiom you want students to copy into scripts), and either move `library(tidyverse)` into the hidden setup chunk or accept the attach block once but not the column specs.
- **[should-fix] Common Failure Modes: Out-of-date syntax** -- "Packages change. ... Warnings and package documentation are the checks." is the only failure mode with no example, and it is the one students cannot recognise without one. Show-don't-tell: `gather()` vs `pivot_longer()`, `mutate_at()` vs `across()`, `summarise()` printing a `.groups` message, or `recode()` vs `case_match()` are all things an assistant will produce today.
  *Fix:* Give one concrete pair, e.g. "An assistant may write `gather(d, key, value, -Crop)`; current tidyr uses `pivot_longer()`. The old function still runs, so the only signal is a 'superseded' note in its help page." Also say what 'superseded' means (still works, no longer recommended) versus 'deprecated' (warns) versus 'defunct' (errors), since those three words are what the student will actually see.
- **[consider] Common Failure Modes: Assumed column names and fig-spectrum** -- The figure places "wrong column name (dplyr)" under "R stops you", but the prose never says why the dplyr qualifier matters. In dplyr, `filter(yield > 40)` errors on an unknown column; in base R, `d$yield` returns NULL and `mean(NULL)` returns NA with a warning, and `d[d$yield > 40, ]` returns zero rows silently. That contrast is exactly the chapter's theme.
  *Fix:* Add one sentence to the "Assumed column names" paragraph: dplyr verbs error on an unknown column name; `$` does not, it returns NULL and the error surfaces later or not at all. Then the figure label is explained.
- **[consider] A Silent Failure (d25_fixed chunk, line 191)** -- `relationship = "many-to-one"` appears in the fix with the comment "state the expected relationship" but no explanation of what it guards against here (a duplicated price row). Students saw it in 3b but the chapter does not link to that.
  *Fix:* Add a parenthetical cross-reference to the 3b section on join relationships after the code, and one clause saying what it would catch: if the price table listed Canola twice, the join would stop instead of doubling 674 rows.
- **[consider] Tasks That Are Easy to Check** -- The list asserts that plots and `case_when()` blocks are easy to check but gives no checking routine, so the section reads as a list of permissions rather than a method. Three of the five bullets are checked by the same move: print the object and look at a count.
  *Fix:* Either cut this section to a sentence pointing at the checking routine in 05b, or reframe the bullets as task plus check: "Draft a `case_when()` block, then `count()` the new column against the old one." The join example already models this; the list should use the same shape.

### Andrew Gelman

The join example is the right kind of teaching: the code is shown, the numbers reproduce (I re-ran it: 476.79, 1,416 rows, 1,051 missing, 571.17), and the fix is a count rather than a sermon. The biggest problem is the Course Policy section, which leans on one unpublished working paper and then reads a causal mechanism ("using it to replace the practice") out of a post-adoption subgroup split, which is exactly the storytelling move the rest of the chapter warns students against.

- **[must-fix] Course Policy (#sec-ai-policy), Strömberg paragraph** -- "The largest losses were concentrated among students whose homework became unusually fast while their homework marks rose" and "students who continued spending about as much time on their homework showed much smaller losses" condition on a post-adoption outcome (time spent) and then treat the split as if it were a treatment contrast. Students who keep working at the same pace after getting AI are different students from those who speed up; the comparison cannot separate the tool's use from who chose to use it that way. The next sentence, "Using a tool to understand an error and using it to replace the practice are different activities," presents this as the study's finding when it is the authors' (and the chapter's) interpretation.
  *Fix:* Report the main result and stop. Something like: "After adoption, homework scores rose and completion times fell, while exam scores fell. The paper argues this is because students substituted the tool for practice; that is a plausible reading, but the subgroup comparison it rests on compares students who chose to work differently, so it is not a clean test." Drop the sentence that treats the 'same time on homework' group as evidence for the activity distinction, or label it explicitly as the paper's interpretation.
- **[should-fix] Course Policy (#sec-ai-policy), study description and figure caption** -- "A 2026 study followed 26,811 Chinese secondary students before and after they began using generative AI" gives the reader a sample size but not the design. The caption adds "compared with students who had not," which implies a comparison group, but the text never says whether adoption was self-selected, how adopters were identified, or what the figure's bars measure (units, standardized scores, percentage changes?). The bib entry is a CEPR discussion paper, i.e. not peer-reviewed, and the chapter does not say so. A big N is doing the persuading here.
  *Fix:* One sentence on design and status: who adopted, how adoption was observed, and that the comparison is observational with self-selected adopters. Say it is a working paper. In the caption, state what the y-axis in images/ai_learning_penalty.png actually is and over what window. If those details are not in hand, cut the figure and keep one hedged sentence about the result.
- **[should-fix] A Silent Failure (#sec-ai-silent-failure), "The result, $476.79 per acre, is plausible."** -- Plausible relative to what? The student has no benchmark, so the sentence asks them to take the author's word. The teachable point is stronger if shown: per-crop acre-weighted revenue in the fixed join runs from about $456 (barley) to $659 (canola), so any number in that band would have looked fine. That is why plausibility is not a check.
  *Fix:* After the corrected result, add a one-line per-crop summary (weighted.mean of Revenue by Crop) and say that the wrong answer sat inside the range of the individual crops, which is why looking at the number could not have caught it.
- **[should-fix] A Silent Failure (#sec-ai-silent-failure), "Of 1,416 rows, 1,051 have no price"** -- The statistic being computed is acre-weighted, but the loss is reported in rows. Rows are variety-by-risk-zone records, not acres. In acres the loss is worse: canola and spring wheat are about 13.4 million of 15.3 million acres (roughly 87%). Reporting rows understates what the weighted mean actually lost and mismatches the unit the reader should care about.
  *Fix:* Report both: "1,051 of 1,416 rows, and about 87% of the acres." Consider making fig-join-loss show acres (or both) rather than rows, since the bars currently answer a question the weighted mean does not ask.
- **[should-fix] A Silent Failure (#sec-ai-silent-failure), closing sentence** -- "The main protection was not a better prompt. It was checking the join with a row count and a missing-value count." This is the 'not X, it's Y' closer, and it is also not quite true: pasting the price table's crop names into the prompt would have prevented this specific failure. Both the prompt and the check matter; the sentence sacrifices that for a clean ending.
  *Fix:* Replace with a flat statement: "Supplying the price table's crop names in the prompt would have avoided this case. The check that catches it regardless of the prompt is the row count and the missing-value count after the join."
- **[should-fix] A Silent Failure (#sec-ai-silent-failure), data provenance** -- The chapter says "The yield file and price file supplied with the book" and nothing else. The price file is five round numbers (canola $14.00, wheat $8.00, barley $5.50, oats $4.50, soybeans $13.00) with no year or source; the yield file's origin (SCIC variety yields? synthetic?) is not stated here. A student could reasonably take $571.17 as a real 2025 Saskatchewan revenue figure.
  *Fix:* One sentence: where the yield data come from, and that the prices are illustrative placeholders rather than 2025 market prices, so the $571 figure demonstrates the join and is not an estimate of anything.
- **[consider] Common Failure Modes, fig-spectrum** -- fig_failure_spectrum() plots five hand-chosen x-coordinates (0.06, 0.17, 0.28, 0.72, 0.92) on an axis between "R stops you" and "nothing stops you". It looks like a measurement but it is a diagram of opinions. It also places "wrong column name (dplyr)" on the loud end, while the chapter text says nothing about dplyr; in base R, d$yield for a missing column returns NULL with no error, so the same failure is silent depending on how the code is written.
  *Fix:* Either make it visibly a diagram (no axis, two labelled groups) or drop it; the preceding sentence "An invented function stops the script. A wrong unit or incomplete join may not" already carries the point. If kept, add to the 'Assumed column names' text that dplyr errors on an unknown column but base-R $ indexing does not.
- **[consider] Common Failure Modes, "Assumptions from another place" and fig-units** -- "Answers about agriculture often default to US crops, agencies and geography" is an unsupported frequency claim. And the fig-units caption says "The same 2025 canola yield" when the value is the acre-weighted mean across all canola varieties and risk zones in the file; the reader is not told that.
  *Fix:* Soften the first to "may default to" or give one concrete observed example. In the fig-units caption, say "acre-weighted mean 2025 canola yield in the variety file" so the number has a definition.

## module05b -- GitHub Copilot

### Excel guru

The chapter is a clean, restrained guide to Copilot inside Positron, and the "show the shape of the data" and accept/reject sections transfer well to any tool. The biggest problem from the Excel side is that there is no Excel side: a book that teaches Excel and R in parallel spends a whole chapter on the R editor's AI assistant and says nothing about Copilot in Excel, formula help, or how the same context-and-verify discipline applies to a workbook, so a student who mostly lives in Excel gets nothing they can use.

- **[must-fix] Whole chapter (## GitHub Copilot, #sec-copilot)** -- The chapter is R/Positron-only. Modules 1-4 teach Excel and R side by side, but this chapter never mentions that Copilot also exists in Excel (the Copilot button on the Home tab in Microsoft 365 apps, with formula-writing, 'add a column that...' and data-summary prompts), nor that the Excel version is a different product with a different licence (Microsoft 365 Copilot, not GitHub Copilot) even though both carry the Copilot name. A student who has seen the Copilot icon in Excel will assume the GitHub sign-up in this chapter unlocks it; it does not.
  *Fix:* Add a short section (or a paragraph near the top) stating that 'Copilot' names two separate products: GitHub Copilot (code editor, this chapter) and Microsoft 365 Copilot in Excel (a paid Microsoft add-on, not covered by GitHub Education). Say which one the course uses and whether USask's Microsoft 365 licence includes Copilot in Excel, so students do not chase the wrong subscription.
- **[must-fix] ## Getting Access, line 39 (fig-copilot-pack)** -- The figure references `images/copilot_student_pack.png`, but that file does not exist in `textbook_261/images/` (the folder has no copilot_* file). Quarto render will fail or emit a broken image.
  *Fix:* Either add the screenshot to `images/` or delete the figure line. If kept, caption it with what the student should click on the benefits page, since the prose says 'Education approval and Copilot activation are separate steps' and a screenshot of the activation toggle would resolve that ambiguity.
- **[should-fix] ## Show the Shape of the Data (#sec-copilot-context)** -- The advice (give the assistant column names, category spellings, missing-value behaviour) is excellent but is illustrated only with an R comment block. The Excel equivalent is missing: for a workbook question, a student should paste the header row plus a few rows, name the sheet and the range (e.g. 'data in Sheet1!A1:F1200, header in row 1'), and state whether blanks are empty cells or text such as "NA" or "-" because that changes what AVERAGE and SUMPRODUCT will do. Without that, the AI-written formula guesses at ranges and treats blanks wrongly.
  *Fix:* Add a parallel Excel example: a short prompt that names the sheet, the range, the column letters, the fact that Acres/Yield have blanks, and asks for an acre-weighted mean yield by crop for 2025. Show the expected answer shape (a SUMPRODUCT/SUMIFS pair or a PivotTable with a calculated field) so students can check what comes back.
- **[should-fix] ## Checking the Work (#sec-copilot-verify)** -- The five checks are phrased for scripts ('read every line', 'count rows before and after a filter'). There is no Excel translation, yet AI-suggested Excel formulas fail in Excel-specific ways the checks do not cover: relative references that shift when copied down, absolute references omitted on the lookup table, SUMIFS criteria ranges of unequal length, VLOOKUP with approximate match on by default, and text-that-looks-like-numbers making AVERAGE silently skip cells.
  *Fix:* Add an Excel version of the checklist: (1) press F2 / use Formulas > Evaluate Formula to see which cells the formula really reads; (2) compare the formula result against a PivotTable of the same thing; (3) copy the formula one row down and confirm the references moved the way you expect; (4) check =COUNT() vs =COUNTA() on the column to catch numbers stored as text; (5) for any lookup, confirm the fourth argument / match_mode is exact.
- **[should-fix] ## Accepting and Rejecting (#sec-copilot-accept)** -- The 'stop and investigate' list (unfamiliar functions or packages) has no Excel counterpart, but AI assistants routinely produce Excel formulas that are version-dependent: LET, LAMBDA, FILTER, UNIQUE, XLOOKUP, TEXTSPLIT, GROUPBY/PIVOTBY and VSTACK exist only in Excel 365 (and some only in current-channel builds). A student on an older lab machine or a Mac build that lags Windows will get #NAME? and not know why.
  *Fix:* Add a bullet under 'Stop and investigate' for Excel: 'a function you have not seen that returns #NAME?' with the explanation that newer dynamic-array and LAMBDA-family functions need a current Excel 365 build, and that asking the assistant for a version that works with SUMIFS/INDEX/MATCH is a reasonable fallback.
- **[consider] ## Connecting Copilot to Positron (#sec-copilot-positron)** -- The chapter says 'You do not need to install the GitHub Copilot extension' and names the command 'Authentication: Configure Language Model Providers'. Positron's AI setup has changed across 2025-2026 releases (the Assistant was behind a preview flag, then enabled by default; the provider command wording has shifted). Step 2 also gives only the keyboard shortcut; some lab machines remap Cmd/Ctrl+Shift+P.
  *Fix:* Add the menu route as well (View > Command Palette, or the gear icon) and a one-line note that the exact command name may differ by Positron version, pointing to the Positron AI configuration link already in 'Other resources'. State which Positron version the instructions were checked against.
- **[consider] ## Two Ways to Use It -- Inline completions** -- 'Press Tab to accept, Esc to dismiss' is correct for Positron/VS Code but the chapter does not say that the same grey-text behaviour does not exist in Excel; the closest equivalent is Formula AutoComplete (the dropdown after typing `=SU`) and, with Microsoft 365 Copilot, the 'Explain this formula' / 'Suggest a formula' cards. Students who expect ghost text in the Excel formula bar will conclude Copilot is not working.
  *Fix:* One sentence: in Excel, inline suggestions take the form of Formula AutoComplete and Flash Fill (Data > Flash Fill, Ctrl+E), not grey ghost text; Flash Fill in particular is worth naming because it is the pattern-completion tool Module 3 students will actually reach for when splitting or reformatting text.

### Hadley Wickham

The chapter has the right instincts: Copilot is only as good as the context it gets, and showing the data's shape before asking is exactly the habit to teach. The biggest problem is that it never shows anything. There is no prompt, no suggestion, no run, no output, no check; the student is told to "count rows before and after" and "test on a small case" but never sees one of those done on the dataset the chapter names. A chapter about an interactive tool with zero worked interaction cannot be reproduced from the text alone.

- **[must-fix] Show the Shape of the Data (#sec-copilot-context)** -- The comment prompt asks for "mean 2025 yield by crop, weighted by acres" and then the chapter stops. The student never sees what Copilot suggests, whether it runs, or what the result looks like. This is the one place in the chapter where show-don't-tell matters, and it tells.
  *Fix:* Add a runnable chunk immediately after the comment: `d <- read_csv("practice/data/sask_variety_yields.csv")`; `glimpse(d)` (with real output); then the code a reasonable suggestion would produce, e.g. `d |> filter(Year == 2025, !is.na(Yield), !is.na(Acres)) |> group_by(Crop) |> summarise(mean_yield = weighted.mean(Yield, Acres), n = n())`, executed with console() so the output is real. Then point at one thing in the output that the comment's context made correct (the `!is.na` handling) and one thing a student must still verify (which crop categories ended up in which group).
- **[must-fix] Checking the Work (#sec-copilot-verify)** -- The five checks are stated as a list and none is demonstrated. "Count rows before and after", "test on a small case whose answer is known" and "recalculate one result another way" are each a few lines of R, and a student cannot reproduce them from the list.
  *Fix:* Demonstrate checks 2 and 4 on the weighted-mean example: `nrow(d)` vs `nrow(filter(d, Year == 2025, !is.na(Yield), !is.na(Acres)))`; then recompute one crop by hand, `d |> filter(Year == 2025, Crop == "Barley") |> summarise(sum(Acres * Yield, na.rm = TRUE) / sum(Acres[!is.na(Yield)], na.rm = TRUE))`, and show that it matches. Note that the hand recomputation exposes a real subtlety (rows with Acres but no Yield must be dropped from the denominator too) which is exactly why the check exists.
- **[should-fix] Show the Shape of the Data (#sec-copilot-context)** -- The example comment says Crop includes "Barley", "Canola/Rapeseed", and "Wheat - Hard Red Spring". The actual file also has "IP Canola/Rapeseed", "Wheat", "Wheat - Durum" and "Wheat - Winter". A student who copies this comment hands the assistant an incomplete picture, which is the exact failure the section warns against.
  *Fix:* Replace the hand-written category list with the output of `d |> distinct(Crop)` or `count(d, Crop)` and say: paste this, don't type it from memory. That also turns an abstract rule (give the spelling of categories) into a concrete, repeatable step.
- **[should-fix] Show the Shape of the Data (#sec-copilot-context)** -- Inconsistent naming inside one paragraph: `df$yield` (lowercase) in the warning, then `d` with `Yield` (capitalised) in the comment. Module 3 reads everything through `clean_names()`, so a student following the book would have `yield`, not `Yield`. The inconsistency is itself the sort of thing that makes AI code fail, and the chapter doesn't notice it.
  *Fix:* Pick one convention and use it throughout. Either read with `clean_names()` as in module03b and write the comment with snake_case columns (`risk_zone, crop, variety, year, acres, yield`), or keep raw names and drop the `df$yield` example. Add one sentence: the case of column names is part of the context, and `clean_names()` is why.
- **[should-fix] Show the Shape of the Data (#sec-copilot-context)** -- "Run `glimpse(d)`, `str(d)` or `head(d)`" offers three functions with no guidance. For a beginner each option is a new concept. `glimpse()` is the one the rest of the book uses (modules 2b, 3a-c, 4c).
  *Fix:* Say `glimpse()` and show its output. Mention `head()` only if you want to say why glimpse is better here (column types and NA visibility, fits in a chat box without wrapping).
- **[should-fix] Two Ways to Use It (#sec-copilot-modes)** -- The inline-completion example is a single comment, `# Read the yield data and count rows by crop`, with no indication of what grey text appears. A student has no model of what a completion looks like, how much it writes, or where it tends to go wrong (it will guess a filename and column name here, since none are given).
  *Fix:* Show the comment followed by a plausible completion (`yields <- read_csv("yields.csv")` / `count(yields, crop)`) and point out that both the filename and the column name are invented because the comment gave neither. That makes the inline section motivate the next section instead of just preceding it.
- **[consider] Accepting and Rejecting (#sec-copilot-accept)** -- "Joins, filters, grouped summaries, missing-value handling and any code that changes the number of rows" is listed as read-carefully territory, but there is no example of a suggestion that looks right and is wrong. The distinction between safe boilerplate and dangerous summaries only lands if the student sees one of each.
  *Fix:* One short example suffices: a suggested `summarise(mean_yield = mean(Yield))` that returns NA for every crop because `na.rm` was omitted, or that silently uses an unweighted mean when the comment asked for weighted. Show the output, then the fix.
- **[consider] Connecting Copilot to Positron (#sec-copilot-positron)** -- Specific command-palette names ("Authentication: Configure Language Model Providers", "AI: Create Diagnostic Report") and the April 20, 2026 sign-up pause will go stale within a term or two, and the chapter presents them as fixed facts rather than as a snapshot.
  *Fix:* Date-stamp the instructions ("as of Positron 2026.xx") and say that the palette search term to type is "language model" or "AI" if the exact command name has changed. Move the sign-up-pause note to a single sentence pointing at the GitHub page rather than a full callout, since it will need deleting.

### Andrew Gelman

This is a plain, short chapter that mostly resists the temptation to sell the tool, and the "show the shape of the data" section is the right lesson stated without fuss. The biggest problem is that the chapter's factual claims about GitHub's plans, dates, and Positron's menus are asserted flatly with no date-stamp or source, in a domain where those facts change monthly; one of them (a "temporary pause since April 20, 2026") is the sort of precise-sounding claim that will read as either wrong or stale within a term, and the student has no way to tell which.

- **[must-fix] Getting Access -- Current access note callout** -- "GitHub's documentation says that new sign-ups for student and several paid Copilot plans have been temporarily paused since April 20, 2026." This is a very specific factual claim presented with no indication of when the author checked it. The chapter elsewhere invents a plan name ("Copilot Student") that does not match GitHub's own naming (the student benefit has been Copilot Pro via the Student Developer Pack). A precise date lends false authority; if the pause has ended, or never had that date, the student reads a confident statement that is wrong and cannot tell.
  *Fix:* Either cite the exact GitHub page and add "as of [date checked]" to the callout, or replace the specific claim with the honest version: "Plan names and eligibility change often. Check GitHub's student access page for what is available this term; do not enter payment information." Use GitHub's current plan names or avoid naming plans at all.
- **[must-fix] Getting Access -- figure** -- `![Applying for GitHub Education student benefits.](images/copilot_student_pack.png)` references a file that does not exist in `images/`. The render will show a broken figure, and the caption promises evidence of a process the chapter has not actually shown.
  *Fix:* Add the screenshot, or delete the figure reference until one exists.
- **[should-fix] Introduction (lines 3-5)** -- "The useful part is the context. Copilot can see more of the file than a question pasted into a separate browser window." This is the chapter's central empirical claim about why the tool is better, and it is stated without any description of what context Copilot actually sends (the open file? the selection? other files in the project?). The next sentence then concedes it "does not know what a dataset contains unless the data or its structure is included in that context", which undercuts the first sentence without resolving what is and is not included.
  *Fix:* Say concretely what is sent in each mode, as far as is known: inline completions use the current file around the cursor; chat uses the selection or files you attach. If the author is not sure of the exact rules, say so and point to the Positron AI configuration page rather than leaving "context" as an undefined good.
- **[should-fix] Connecting Copilot to Positron** -- Step-by-step menu names ("Authentication: Configure Language Model Providers", "AI: Create Diagnostic Report", "Posit Assistant") are given as current fact with no version or date. "Recent versions of Positron include AI support directly" is a claim that will become wrong in some direction; a student on a different version has no way to reconcile what they see with the text. The chapter already admits this volatility in passing ("If the account was connected in an older Positron release...").
  *Fix:* State the Positron version these instructions were written against in one line at the top of the section, and tell the student that if the command names differ to search the Command Palette for "language model" or consult the linked Positron page. Consider moving the exact step list into a callout that can be updated without rewriting prose.
- **[should-fix] Show the Shape of the Data -- comment example** -- The comment block says `# Crop includes "Barley", "Canola/Rapeseed", and "Wheat - Hard Red Spring"`. The real `sask_variety_yields.csv` has 25 crop categories, including "IP Canola/Rapeseed", "Wheat", "Wheat - Durum", and "Wheat - Winter". The example is meant to show how to stop the assistant guessing, yet the context it supplies is itself incomplete in exactly the way that causes guesses (a model told there is "Wheat - Hard Red Spring" will not anticipate plain "Wheat" or "IP Canola/Rapeseed" when asked to filter canola). The chapter never says the list is partial.
  *Fix:* Either paste the actual output of `unique(d$Crop)` or `count(d, Crop)` (which is what the text recommends doing) and use a render-time `console()` chunk so the shown output is real, or change the comment to say explicitly that the three are examples from a longer list and that the full list was pasted into the chat. The lesson is stronger if the example is honest about the data.
- **[consider] Show the Shape of the Data -- weighted mean request** -- The example request "Compute mean 2025 yield by crop, weighted by acres" is given with the note that Acres and Yield contain missing values, but the chapter never says what the right answer to the missing-value problem is, so the student cannot apply the chapter's own rule ("accept code when you can explain what it does and can name the check that would reveal an error"). Whether rows with missing Acres are dropped, treated as zero, or cause NA propagation changes the number. This is precisely the forking path the chapter warns about, left unexamined.
  *Fix:* Add two or three sentences: say which treatment of missing values the analyst intends (e.g. drop rows missing either field, and report how many were dropped), and show the row-count check that would catch a suggestion doing something else. That turns the example into a demonstration of the verification section rather than a bare prompt.
- **[consider] Accepting and Rejecting** -- "Rejecting a suggestion costs almost nothing. Accepting code you do not understand leaves you responsible for a calculation you cannot defend or repair." The second sentence is true but is doing the work of a closing line rather than teaching; the three-tier bulleted list above it is the actual content. The paragraph also implies an asymmetry (rejection is free) without acknowledging the real cost of rejection, which is that the student then writes the code themselves and may make the same error.
  *Fix:* Cut the two-sentence closer or fold it into the list introduction. If the asymmetry is kept, state it plainly: a rejected suggestion leaves you where you were; an accepted wrong one leaves you with a result you believe.
- **[consider] Checking the Work** -- "The assistant can help write a check. It cannot replace the independent evidence supplied by that check." A check written by the same assistant that wrote the code is not independent in the sense implied; if the model misreads the column semantics, it will misread them in the check too. The sentence asserts independence where the procedure does not guarantee it.
  *Fix:* Be specific about which of the five checks are independent of the assistant (row counts you compute yourself, a small case whose answer you know by hand, a recalculation in Excel) and say that a check generated by the same tool only catches a subset of errors.

## module05c -- Graphing with AI

### Excel guru

The chapter's central point, that a chart checks the drawing but not the rows behind it, is right and the numbers it quotes (13,625 rows, 4,186 missing yields, 2021 roughly half missing, 2025 three missing) all verify against the CSV. The biggest problem from my side of the desk is that Excel is absent: a book that teaches Excel and R side by side, and that has a full Excel graphing chapter in Module 4, here drops Excel without a word, so a student who does most of their charting in Excel has no idea whether or how any of this applies to them.

- **[must-fix] Graphing with AI (chapter opening) / The Exercises** -- The chapter is R-only and never says so. Module 4 has a full Excel graphing chapter (@sec-graphing-excel) and the course is billed as Excel and R side by side, yet 'Work in Positron with an assistant connected' is the only instruction, and every exercise (direct labels, geom_ribbon band, facets) is framed in ggplot2 terms. Students will reasonably ask whether they can use Copilot in Excel or paste a sheet into ChatGPT, and the chapter gives no answer. Note also that Copilot in Excel (the in-app one that can see the sheet) requires a paid Microsoft 365 Copilot licence that the USask student account does not include, so an unqualified 'use AI in Excel' would mislead.
  *Fix:* Add a short paragraph after the opening stating that the exercises are done in R because ggplot2 code is text an assistant can write and you can read, whereas Excel charts are built by clicking and the free assistants cannot see your workbook. Say explicitly that Copilot inside Excel needs a separate paid licence students do not have, and that a browser assistant can still give Excel steps if you paste the header row plus a few rows, but you then have to carry out and check the clicks yourself. One paragraph is enough; the point is to close the question rather than add an Excel track.
- **[should-fix] A Chart Does Not Check Its Data** -- The pre-plot checklist ('inspect the number of rows, groups, missing values and numerical ranges') is given only abstractly, and the missing-by-year figure is produced by hidden R. An Excel student has no route to reproduce the check, even though it is a two-minute PivotTable: Year in Rows, Count of Variety and Count of Yield in Values, and the gap between the two columns is the missing count (COUNT ignores blanks, COUNTA of a text column does not).
  *Fix:* Add one sentence giving the Excel version of the check: a PivotTable with Year in Rows and both 'Count of Variety' and 'Count of Yield' in Values reproduces @fig-missing as a table; or =COUNTA(F:F)-1 against =COUNT(F:F) on the Yield column for the overall 4,186. This also gives students an independent way to verify the numbers the chapter quotes.
- **[should-fix] The Exercises, 1 and 3; A Useful Prompt Structure** -- 'Spring wheat' is not a value in the Crop column. The data has 'Wheat', 'Wheat - Hard Red Spring', 'Wheat - Durum' and 'Wheat - Winter' (and Module 5b itself quotes the exact string 'Wheat - Hard Red Spring'). An assistant given the prompt in @sec-ai-graph-prompt will either guess 'Wheat' or try a partial match, and the student's check 'Did the calculation average over the intended varieties?' cannot be answered because the intended crop was never named. This is exactly the 'assumed column values' failure the module warns about, but here the ambiguity is in the textbook's own prompt.
  *Fix:* Use the exact string 'Wheat - Hard Red Spring' in Exercise 1, Exercise 3 and the example prompt, and add to the Exercise 1 checks: 'Which Crop value did the code filter on? Compare it with the values in the column (in Excel, Data > Filter on Crop shows the list).' Alternatively keep 'spring wheat' deliberately and make resolving it the first check, but then say so.
- **[should-fix] The Exercises, 4. Build small multiples** -- 'Six crops with the greatest total acreage' has two traps the checks do not mention. Acres is blank in the same 4,186 rows as Yield, so a SUM/sum(na.rm = TRUE) silently drops them and an Excel SUM does the same without comment. And the Crop column contains overlapping categories ('Lentils', 'Lentils - Red', 'Lentils - Large Green'; 'Canola/Rapeseed' and 'IP Canola/Rapeseed'), so the top six may contain what a reader would consider the same crop twice.
  *Fix:* Add a check: 'Are any of the six panels sub-categories of another (Lentils vs Lentils - Red)? Were blank Acres excluded or treated as zero?' A PivotTable with Crop in Rows and Sum of Acres, sorted descending, is the Excel way to get the ranking and shows the overlap at a glance; mention it in the same sentence.
- **[consider] The Exercises, 2 and 3** -- Two of the design changes asked for (direct labels at the right end of each line, a shaded min-max band behind a line) are the ones where the Excel treatment in Module 4 is thinnest: @sec-excel-chart-elements says 'direct labels can be clearer than a legend' but never shows how (select the last point of a series, Add Data Label, Format Data Label > Label Contains: Series Name), and a range band in Excel is the stacked-area-with-invisible-bottom-series trick, which is nowhere in the book. Students who reach for Excel for these exercises will be stuck, and an assistant's description of the Excel clicks is easy to get wrong between Windows and Mac.
  *Fix:* Either state in the chapter opener that these two tasks are R-only (part of the reason the chapter is in R), or add a one-line pointer in each exercise: 'In Excel: last-point data label showing the series name' for Exercise 2 and 'In Excel this needs a stacked area chart with a transparent lower series; ask the assistant for the steps and check that the band is minimum-to-maximum, not zero-to-maximum' for Exercise 3.
- **[consider] A Useful Prompt Structure** -- The four-item prompt structure assumes the assistant can be told 'the data frame d has columns...'. For an Excel user pasting into a browser assistant the equivalent is pasting the header row and five or six data rows, and saying which column is a number and which is text, because a pasted CSV does not carry types and the assistant will happily treat Year as a number to sum or Risk_Zone as a quantity. The chapter's advice on context does not transfer.
  *Fix:* Add one sentence: 'If you are working from a workbook rather than a data frame, paste the header row and a handful of rows, and say which columns are categories (Risk_Zone is a zone number, not a quantity).' This also serves R students who use a browser assistant instead of Copilot in Positron.

### Hadley Wickham

The chapter gets the most important thing right: it insists the student check the data frame that feeds the plot, not just the picture, and the "ask for the summary table before the plot code" advice is exactly how I would teach prompting for ggplot2. The biggest problem is that a chapter about writing ggplot2 with an assistant contains no R code at all -- not the read_csv line, not one worked exercise, not one piece of the syntax (annotate, geom_text at the line end, geom_ribbon, facet_wrap) it says is hard to remember -- so the student has no reference against which to judge what the assistant hands back, and the exercises are underspecified in ways that push the ambiguity onto the AI rather than the student.

- **[must-fix] The Exercises (#sec-ai-graph-exercises)** -- There is no code to load the data or show its structure. The prompt template in the next section refers to a data frame `d`, but nothing in this chapter creates `d`; module05a reads it with read_csv("practice/data/sask_variety_yields.csv") but the student on a fresh session for this chapter has no starting point. A student cannot reproduce the exercises from the text alone.
  *Fix:* Add a short setup block before the exercises: `library(tidyverse); d <- read_csv("practice/data/sask_variety_yields.csv"); glimpse(d)` with the glimpse output shown (run with console so it is real). The glimpse output also gives the student the exact column names and types to paste into the prompt, which is the point of Objective 1.
- **[must-fix] 1. Annotate one point** -- "Spring wheat" is not a value in the Crop column. The data has "Wheat - Hard Red Spring", "Wheat", "Wheat - Durum", "Wheat - Winter" and "Khorasan wheat/Kamut brand". An assistant asked for spring wheat will guess (I would expect it to grep "Spring", or to filter Crop == "Spring wheat" and get zero rows). The check "Did the calculation average over the intended varieties?" hints at this but the student has no way to know which value is intended. The same ambiguity appears in Exercise 3 and in the example prompt ("Keep spring wheat rows").
  *Fix:* Either state the exact value (`Crop == "Wheat - Hard Red Spring"`) in the exercise and the example prompt, or make the ambiguity the explicit teaching point: show `count(d, Crop)` or `distinct(d, Crop)` output and tell the student to pick and name the value themselves before prompting. The latter is better pedagogy, but do it on purpose rather than leaving it as a trap.
- **[should-fix] A Useful Prompt Structure (#sec-ai-graph-prompt)** -- The example prompt asks for one line per Risk_Zone with direct labels at the right end. Risk_Zone takes 21 integer values (1 to 21), so this produces 21 overlapping lines with 21 labels stacked at x = 2025 -- an unreadable chart that also contradicts the direct-labelling exercise (four crops, not twenty-one zones). A student who runs the model prompt gets a bad chart and no guidance on why.
  *Fix:* Change the example to something that works: mean Yield by Year for the four crops in Exercise 2 (name them: Wheat - Hard Red Spring, Canola/Rapeseed, Barley, Oats, which is the CROPS vector in R/ai_failures.R), or keep Risk_Zone but ask for a summary/ribbon rather than 21 lines.
- **[should-fix] 2. Replace a legend with direct labels** -- "four crops" is unspecified. There are 25 values in Crop, several of which (Wheat, Lentils, Mustard variants) overlap with each other. Which four the assistant picks determines whether the chart is sensible, and the student cannot tell if the assistant chose badly.
  *Fix:* Name the four crops (the CROPS vector in R/ai_failures.R is the natural choice, and matches module05a). Also mention that direct labels at the line end in ggplot2 mean a `geom_text()` on a `filter(Year == max(Year))` summary plus `coord_cartesian(clip = "off")` or an expanded x scale -- the clipping check in the exercise only makes sense if the student knows that clipping is the usual failure.
- **[should-fix] 3. Shade a range behind a line** -- The exercise never names the geom. The band across risk zones is a `geom_ribbon(aes(ymin, ymax))` drawn *before* `geom_line()` so the line sits on top; layer order is the thing students and assistants get wrong here, and the check list does not include it. Also "range across risk zones" requires a two-level summary (mean per Year x Risk_Zone, then min/max/mean per Year), which is more involved than the other exercises and is not signalled.
  *Fix:* Add a check: "Is the band drawn behind the line, not over it?" and state in the exercise that the summary has two steps (per zone-year, then per year). Consider showing the 5-row summary table the student should expect, since the chapter's own advice is to look at the table before the plot.
- **[consider] 4. Build small multiples** -- The check "Do the panels share a vertical scale?" is good, but the chapter does not say this is `facet_wrap(~ Crop, scales = "free_y")` versus the default, and the exercise's first step (top six by total acreage) has a data-check of its own: Acres is NA on 4,186 rows, so `sum(Acres)` without `na.rm = TRUE` returns NA for every crop and the top-six selection silently fails or errors. That is exactly the kind of pre-plot data failure the chapter opens with.
  *Fix:* Add to the checks: "Which six crops were selected, and was the total computed with missing acreage removed?" and mention `scales = "free_y"` by name so the student can see whether the assistant's code matches its claim. The expected six (Canola/Rapeseed, Wheat - Hard Red Spring, Wheat - Durum, Lentils - Red, Barley, Field Peas) could be stated so the student has an answer key.
- **[consider] A Chart Does Not Check Its Data (#sec-visual-limits)** -- The figure is produced by `fig_missing_by_year()` in R/ai_failures.R, which uses base `read.csv` and a `group_by() |> summarise() |> pivot_longer()` pipeline that is hidden with echo: false. The numbers in the prose (13,625 rows, 4,186 missing, about half of 2021, almost all of 2025) are correct, but the student is told to 'inspect the number of rows, groups, missing values' without seeing how. This chapter is the one place where showing the six-line summarise is cheaper than describing it.
  *Fix:* Show the summary code and its output inline (a console chunk): `d |> group_by(Year) |> summarise(rows = n(), missing = sum(is.na(Yield)))`. Keep the figure, but let the table be the thing the student copies.
- **[consider] 5. Rebuild a chart from Module 4** -- R/chart_types.R is a theme-and-palette script written for the book's rendering (custom `theme_arec`, hex palette, fixed fig sizes). Comparing a student's assistant-drafted script against it will mostly surface cosmetic differences (theme, colours, fonts) rather than the design decisions the check list asks about. The comparison is also only fair if the student has the same summary data, which the Module 4 figures compute internally.
  *Fix:* Point the comparison at the grammar, not the styling: tell the student to ignore theme/colour differences and compare the aesthetic mapping, the geoms and the pre-plot summary. Or pick one named figure from @sec-chart-types rather than leaving the choice open, so the checks can be specific.

### Andrew Gelman

The chapter's central point is right and rare in a textbook: a chart can show you the drawing is wrong but not that the data are wrong, and the missingness figure makes that concrete with real numbers that check out against the file. The biggest problem is that the chapter never says what the data are or what a missing yield means, so the one empirical claim it builds on ("a chart of 2021 rests on a much thinner set of observations") is offered without the mechanism that would let a student interpret it, and the exercises quietly contain forking paths (which crop is "spring wheat", which average, NA handling in acreage sums) that the assistant will resolve silently and the stated checks will not catch.

- **[must-fix] A Chart Does Not Check Its Data** -- The file is used for five exercises and a figure but its provenance is never stated: where the rows come from, what one row is (a variety in a risk zone in a year), and what a missing Yield means. The pattern in the data suggests structural missingness rather than unreported data: Acres is NA in every row where Yield is NA, the count of missing rows falls monotonically from 1,380 in 2021 to 3 in 2025, and the file looks like a variety-by-zone grid padded to all five years. That is a different thing from 'no recorded yield', and it changes the interpretation of 'A chart of 2021 therefore rests on a much thinner set of observations.' If 2021 rows are missing because those varieties had not been released yet, the 2021 average is not thinner in any worrying sense; it is an average over a different, older set of varieties, which is a composition problem, not a sample-size problem.
  *Fix:* Add two or three sentences naming the source (SCIC variety yield data or whatever it is) and the unit of observation, and state plainly what a missing Yield most likely means in this file (variety not grown or not insured in that zone-year). Then rewrite the 2021 sentence to describe the actual consequence: the set of varieties behind the 2021 point differs from the set behind 2025, so a trend line partly reflects turnover in varieties, not only weather.
- **[must-fix] The Exercises (1, 3) and A Useful Prompt Structure** -- Every exercise says 'spring wheat' and the example prompt says 'Keep spring wheat rows', but no Crop value is 'spring wheat'. The file has 'Wheat', 'Wheat - Hard Red Spring', 'Wheat - Durum', 'Wheat - Winter' and 'Khorasan wheat/Kamut brand'; the 'Wheat' category has varieties prefixed '- Canada Prairie', so it is apparently a different class, not a parent category. The assistant will resolve this ambiguity silently (string-matching on 'Wheat', or picking one class), and the listed checks ('Did the calculation average over the intended varieties?') do not point the student at it. This is exactly the forking path the chapter claims to teach: the chart will look fine whichever way it is resolved.
  *Fix:* Either name the Crop value explicitly in the exercises and the example prompt ('Wheat - Hard Red Spring'), or keep the ambiguity deliberately and add a check: 'Which Crop values did the code keep? Print unique(d$Crop) and decide which ones you meant.' The second option is more in the spirit of the chapter, but it must be explicit.
- **[should-fix] The Exercises (1, 2, 3)** -- 'Average yield by year' is used throughout without saying which average. An unweighted mean of variety-zone rows and an acreage-weighted mean answer different questions, and for spring wheat the row counts per year run from 257 to 377 with zone-year cells as small as 5 rows. The assistant will default to mean(Yield) and the student has no check that asks whether that was the intended estimand.
  *Fix:* Say once, in the exercise preamble, that 'average' means the unweighted mean of the variety-by-zone rows unless stated otherwise, and note that an acreage-weighted mean is a different number. Add 'Is the average weighted or unweighted, and is that what you wanted?' to the checks for exercise 1.
- **[should-fix] 3. Shade a range behind a line** -- The check 'How many risk zones contribute to each year's range?' is vacuous for this file: all 21 zones contribute in every year. The quantity that actually matters for a range across zone means is how many rows sit behind each zone-year mean, which is 5 to 8 for spring wheat. A range of zone means from 14.9 to 48.4 bu/ac in 2021 is partly a range of small-sample means.
  *Fix:* Replace the check with 'How many recorded yields sit behind each zone's annual mean? With five or six rows per zone, the band is partly sampling noise.' This also gives the student a reason to ask for the summary table the prompt section recommends.
- **[should-fix] 4. Build small multiples** -- 'Find the six crops with the greatest total acreage' depends on how NA acres are handled (sum() returns NA without na.rm = TRUE, and the assistant will add na.rm silently), and on whether overlapping categories count separately: 'Canola/Rapeseed' and 'IP Canola/Rapeseed', 'Lentils - Red' and 'Lentils - Large Green', the three wheat classes. Either choice produces a plausible six-panel figure and the checks ('Are there exactly six panels?') only verify the drawing.
  *Fix:* Add a check on the data side: 'Print the acreage ranking the code used. Did it drop or impute missing acres, and are any of the six crops really sub-classes of one another?'
- **[consider] 1. Annotate one point** -- 'identify it as a drought year' asks the student to write a causal label onto the chart from outside knowledge. 2021 was a Prairie drought, but nothing in this file establishes that, and the chapter elsewhere is careful about what a chart can and cannot show. The data also show 2021 average spring wheat yield at about 32 bu/ac versus 45 to 53 in other years, so the label will look self-evidently confirmed by the dip.
  *Fix:* One sentence saying the drought label comes from knowledge outside the data (weather records, the crop report) and that the chart shows a low year, not why it was low.
- **[consider] When the Assistant Gets Stuck** -- 'More repetitions usually reinforce the same approach' is stated as an empirical regularity about assistant behaviour with no basis given; 'usually' is doing the work of evidence. The advice that follows is sensible regardless, so the claim is unnecessary.
  *Fix:* Drop 'usually' and state it as the author's experience or as a description of what the student will see: 'Repeating the same error report tends to get the same fix back.' Or cut the sentence and go straight to the list.
- **[consider] A Chart Does Not Check Its Data** -- 'Nothing in the finished chart reveals that unless the missingness is checked separately.' is a closing beat that restates the section's first sentence ('It cannot show whether the wrong rows were supplied to it'). The figure and the 2021/2025 numbers already made the point.
  *Fix:* Delete the sentence and end on the numbers, or fold its content into the rewritten 2021 sentence from the first finding.

## module06a -- Making a Workbook Someone Else Can Read

### Excel guru

The audience framing and the three contrasting real workbooks are the right way to teach this, and the Excel claims that are made are accurate. The biggest problem is that the chapter is almost entirely prose about Excel habits (README, input shading, frozen panes, protection) with no workbook of its own that actually demonstrates them; the three embedded examples are read-only previews that hide gridlines and headers, and none of them is protected or has a README, so a student never sees the thing the chapter tells them to build.

- **[must-fix] Locking Cells (#sec-locking-cells)** -- The four-step workflow is correct but incomplete for a student following it. Step 2 says 'Open Format Cells > Protection' without saying how to get there (Ctrl+1 / Cmd+1, or right-click > Format Cells, or Home > Format > Format Cells). Step 3 does not mention that the Protect Sheet dialog has a checklist of allowed actions (Select locked cells, Format cells, Sort, Use AutoFilter...) which students will see and not understand; the defaults leave only 'select locked/unlocked cells' ticked, so sorting and filtering a protected data table silently stops working. And the text never says how to undo it (Review > Unprotect Sheet), which is the first thing a student who locked the wrong cells will need.
  *Fix:* Add the keyboard shortcut/right-click path for Format Cells, one sentence on the Protect Sheet checklist (tick Sort and Use AutoFilter if the sheet holds a table the reader will filter), and 'Review > Unprotect Sheet reverses it'. Better still, embed a small protected workbook (see next finding) so the steps can be verified against a real file.
- **[must-fix] Locking Cells / Formatting for Handoff** -- The chapter depends on three embedded workbooks, but none of them illustrates the chapter's own advice. I inspected the files: no sheet in any of the three has sheetProtection, none has a README sheet, the Crop Planning Guide has 'Disclaimer' and 'General Assumptions' but no frozen panes and no protection, and only the RM-yield file has a frozen header row. The Crop Planning Guide styles.xml does contain 38 unlocked (locked="0") cell styles, so the ministry prepared it for protection but never turned it on -- which is actually a nice teaching point, but the text does not mention it. Per the project's own rule ('put Excel explanations in the worksheet, not the prose'), the chapter needs one small workbook of its own with README, Raw, Clean, Analysis, Output tabs, a frozen header, shaded unlocked input cells, a FORMULATEXT column, internal HYPERLINKs, and sheet protection on the Analysis tab.
  *Fix:* Build mod06_handoff.xlsx (e.g. a tiny crop-budget: inputs for acres/yield/price, formulas for revenue) following the mod01_sum.xlsx pattern, embed it, and point the Locking Cells and Hyperlinks sections at it. Add a sentence in 'A model for entering assumptions' noting the Guide's input cells are already marked Unlocked but the sheets are not protected, and invite the student to protect one and test it.
- **[should-fix] Three Workbooks, Three Audiences (#sec-aesthetics)** -- All three iframes set wdHideGridlines=True and wdHideHeaders=True, and the embed is 402px wide and non-interactive. The section asks students to notice 'bold headers, borders and restrained number formats' and that the RM file 'begins in A1, uses one header row' -- but with row/column headers hidden the student cannot see that it starts in A1, cannot see the frozen pane behave, and cannot tell borders from gridlines. The elevator workbook has two sheets (Hist Primary Elevator, Grain Storage Capacity) and the Guide has 30 tabs; the narrow preview shows none of that tab structure.
  *Fix:* For this chapter specifically, drop wdHideHeaders (and probably wdHideGridlines) from the three embeds so the A1 origin, header row and borders-vs-gridlines distinction are visible, widen the iframe, and add one sentence per example telling the student which tab to look at after downloading (e.g. 'Open General Assumptions, then Canola, and compare the layout').
- **[should-fix] Hyperlinks (#sec-hyperlinks)** -- Two accuracy details. (1) In the formula example, `=HYPERLINK("#'Summary'!A1", ...)` is fine, but the text says 'Single quotes are required around a sheet name that contains a space' while the example quotes a name with no space -- the example and the rule disagree, which will confuse. (2) The Insert > Link dialog on Mac is 'Insert > Link' in the ribbon but the dialog is titled 'Insert Hyperlink' with tabs Web Page or File / This Document / Email Address; on Windows the left pane says 'Existing File or Web Page / Place in This Document / E-mail Address'. The chapter's three bullets use neither wording, so a student cannot match them to the dialog.
  *Fix:* Change the example to a sheet with a space, e.g. `=HYPERLINK("#'General Assumptions'!A1", "Assumptions")`, and add a second example without quotes (`"#Summary!A1"`). Name the dialog options as they appear: 'Place in This Document' (Windows) / 'This Document' (Mac) etc. Also note the HYPERLINK formula can use CELL or a text cell for the address, which is the actual reason to prefer the formula form.
- **[should-fix] Formatting for Handoff (#sec-excel-formatting)** -- The section is a bullet list of habits with no Excel mechanics, so it is thinner than anything else in the book that touches Excel. 'Freeze the header row' does not give View > Freeze Panes > Freeze Top Row (it is in module01b, so a cross-reference would do). 'Use one visual system' is the right advice but Excel has a feature for exactly this -- Cell Styles (Home > Styles) ships with Input, Calculation, Output, Note, Warning Text styles that are the conventional input/formula colour scheme, and applying them is faster and more consistent than hand-picking fills. 'Use number formats deliberately' gives no path (Ctrl+1 / Cmd+1, or Home > Number). 'Document assumptions ... in labelled cells' is where Named Ranges belong (Formulas > Define Name, so the formula reads =acres*price rather than =B3*B4), which is a genuine readability feature the chapter is about.
  *Fix:* Add a short parenthetical path to each bullet that has one (Freeze Panes -> @sec reference to module01b; number formats -> Ctrl+1/Cmd+1). Add a bullet on Cell Styles (Input/Calculation/Output) as the built-in visual system, and a bullet on named ranges for assumptions. Demonstrate both in the chapter workbook rather than in prose.
- **[consider] Formatting for Handoff (#sec-excel-formatting)** -- 'Keep raw data unchanged' is stated but the chapter does not connect it to the Excel feature that enforces it: a Power Query import (Module 3) loads the raw file into its own sheet and re-pulls it from source on Refresh, so the raw sheet cannot drift and the cleaning steps are recorded in the query. That is the Excel equivalent of the R script the chapter's R side presumably relies on, and it is missing here.
  *Fix:* One or two sentences under 'Keep raw data unchanged' or 'Give Each Sheet One Job' pointing to the Data > Get Data / Queries & Connections workflow from Module 3 as the way to make the Raw sheet a refreshable import rather than a paste.
- **[consider] Give Each Sheet One Job (#sec-workbook-separation)** -- The five-sheet structure is fine, but Excel has one mechanical trap that bites students who follow it: cross-sheet formulas referencing a sheet whose name has spaces need quotes ('Cleaned data'!B2), and renaming a tab after the fact is safe (Excel updates references) whereas renaming a file that other workbooks link to is not. Students who adopt descriptive sheet names with spaces will hit the quoting immediately. Relatedly, the chapter suggests 'Raw_yields' (underscore) in one place and prose-style names in another without saying why the underscore.
  *Fix:* Add one sentence recommending sheet names without spaces (Raw_yields, Clean_yields) and showing what a cross-sheet reference looks like with and without the quotes.
- **[consider] Sympathy for the Reader (#sec-audience-sympathy)** -- 'Open the file as though it arrived without an explanation' is good advice but there is an Excel-specific check worth naming: the sheet and cell that are active when the file is saved are what the reader sees first. A workbook saved while on the 30th crop tab at cell AZ400 opens there for everyone. Ctrl+Home on each sheet, then select the README tab before saving, is the concrete habit.
  *Fix:* Add one sentence: before saving for handoff, go to A1 on each sheet and leave the README tab active, because Excel opens the file exactly where it was saved.

### Hadley Wickham

The three-audiences framing is the right idea and the examples are real government files rather than toys, which I like. The biggest problem is that the chapter never connects "data for further analysis" to the tidy-data vocabulary the student already has from Module 3, and the RM-yield workbook held up as the model for that audience quietly violates the chapter's own rules (no units, lentils in lb/ac beside everything else in bu/ac, one column per crop). Show-don't-tell cuts both ways: if the examples are going to do the teaching, they need to actually exhibit the principles.

- **[must-fix] Data for further analysis (#sec-aesthetics)** -- The RM-yield workbook is presented as the model for a file someone will 'filter, summarize or import', yet it has no units anywhere in the header and the Lentils column is in lb/acre (median 1609, max 3411) while every other crop is bu/acre (Peas median 41). A student who imports it and does what Module 3 taught (pivot_longer to Year/RM/crop/yield, then summarise) gets nonsense and has no way to know from the file. The chapter says 'Label every input and output. Include units in headers' two sections later.
  *Fix:* Either fix the example file (add units to the header, e.g. 'Lentils (lb/ac)', or convert lentils to bu/ac and say so on a README sheet) or keep the file as-is and use it explicitly as a counter-example: 'Open it, look at Lentils, and ask what unit it is in. The file does not say.' Do not present it as a clean model while it carries a silent unit trap.
- **[should-fix] Data for further analysis (#sec-aesthetics)** -- 'It begins in A1, uses one header row and keeps one observation per row' is a restatement of tidy-data rules without ever using the word or linking back to Module 3. Worse, the file is one row per RM-year with one column per crop, which is exactly the wide layout Module 3 teaches students to pivot_longer before analysis. Calling that 'one observation per row' is at best a convention-dependent claim and the chapter doesn't flag it.
  *Fix:* Add one or two sentences: this layout is the rectangular, machine-readable shape from @sec-(tidy data section in Module 3); it is wide (one column per crop), which is fine for reading and for Excel PivotTables, and a single pivot_longer away from tidy if the next analysis is in R. Be honest that 'one observation per row' depends on whether the observation is an RM-year or an RM-year-crop.
- **[should-fix] A table for reading (#sec-aesthetics)** -- The Manitoba elevator table is praised for 'bold headers, borders and restrained number formats', but the text does not point at the features that actually distinguish it from an importable file: a two-row header with merged cells (C6:D6, E6:F6), units on a separate row, a title block above the data, footnotes in merged rows at the bottom, and no frozen panes. Those are the concrete reasons this file is for reading and not for read_excel(). The student is told it is different without being shown why.
  *Fix:* Name the features: title and source in rows 1-4, merged group headers, a units row, footnotes at row 71-73. Then say plainly that each of those is fine for a reader and is a problem for read_excel(), which is why Module 3 spent time on skip= and header cleanup. One sentence per feature is enough.
- **[should-fix] A model for entering assumptions (#sec-aesthetics)** -- The crop planning guide is the chapter's example of a model 'for entering assumptions', and the Locking Cells section says such a workbook 'should protect its formulas'. None of the sheets in the shipped file are protected (ws.protection.sheet is False on every sheet), and the Guide2026 sheet contains only the placeholder text ' Please do this page'. The file also has a stray 'ddd' in the Disclaimer sheet in the SharePoint copy referenced earlier (cell H1 in the embedded version). A student told to 'test an input cell and a formula cell' will find everything editable.
  *Fix:* Either protect the crop sheets in the distributed copy (unlock the yellow input cells, Review > Protect Sheet, no password) so the Locking Cells walkthrough can point at it, or say explicitly that the government file ships unprotected and make protecting one crop sheet the student's exercise. Remove or fill the Guide2026 placeholder sheet before publishing.
- **[should-fix] Give Each Sheet One Job (#sec-workbook-separation)** -- The README / Raw / Cleaned / Analysis / Output structure is stated as a list with no example, and none of the three embedded workbooks follows it (one has two data sheets, one has 35 crop sheets, one has a single sheet). The student is asked to adopt a structure they never see.
  *Fix:* Build one small workbook from a Module 3 dataset (e.g. rm_yields_2015_2024.csv as Raw, a Cleaned sheet that fixes units, an Analysis sheet with a PivotTable or SUMIFS, an Output chart, and a README) and embed it here, per the project's own 'put Excel explanations in the worksheet' rule. Then the Hyperlinks and Locking sections can refer to cells in that file instead of describing them abstractly.
- **[consider] Hyperlinks (#sec-hyperlinks)** -- The HYPERLINK example says 'Single quotes are required around a sheet name that contains a space' but the example sheet is 'Summary', which has no space, so the quotes shown are optional and the student cannot tell which rule is being illustrated. Also, the second example splits one formula across two lines in a text block; Excel does not accept a line break in the middle of a formula, so it will not paste.
  *Fix:* Use a sheet name with a space in the first example (=HYPERLINK("#'Raw yields'!A1", "Raw yields")) and keep the second on one line, or note that the break is for display only. Better still, put both in a FORMULATEXT column of the README sheet of the example workbook.
- **[consider] Formatting for Handoff (#sec-excel-formatting)** -- 'Keep raw data unchanged' and 'Use conditional formatting for exceptions' are good rules but the chapter treats them as universal. For a file whose next reader is R, conditional formatting, merged title rows and the README sheet are invisible or actively harmful (read_excel ignores colour; the title block shifts the header). The chapter never says which of the nine habits apply to the 'data for further analysis' audience and which do not.
  *Fix:* Add a short note after the list saying which items are for human readers only (visual system, conditional formatting, number formats that change display) and which survive import (one header row, units in the header text, unchanged raw sheet, descriptive sheet names). This is the place to say that a file meant for R should put its documentation in a separate README sheet or a sidecar text file, not above row 1.
- **[consider] Sympathy for the Reader (#sec-audience-sympathy)** -- This section repeats the audience list from #sec-workbook-audience with a second set of questions and no example, and sits between the embedded workbooks and the formatting habits where it interrupts the show-then-tell flow.
  *Fix:* Fold the five questions into the opening 'Start With the Audience' section and delete this heading, or turn it into a concrete exercise: open the RM-yield file cold and write down the first three things you cannot tell from the sheet (unit of Lentils, whether RM 1 is a code or a name, what year range it covers).

### Andrew Gelman

The chapter is admirably plain and mostly teaches rather than sells; the three-audience framing is a good idea and the locking-cells section is concrete. The biggest problem is that the prose describes the three example workbooks in idealized terms that the actual files do not support -- the "table for reading" has no frozen panes, a two-row header starting in row 6 and a cryptic sheet name; the "decision tool" has no sheet protection and a leftover placeholder sheet; the provenance given in the chapter disagrees with the provenance printed in the workbook. A chapter about honesty in handoff should open the files it points at and say what is actually there, including what they get wrong.

- **[must-fix] A table for reading (#sec-aesthetics)** -- "Bold headers, borders and restrained number formats expose the table's structure without changing the data underneath." Opening the file: the header cell A6 is bold but has no border; headers span two rows (row 6 names, row 7 units); the data start in row 8 after an agency name, a URL and a title; no panes are frozen over a 75-row table; the sheet is named "Hist Primary Elevator". Nearly every habit the chapter lists under Formatting for Handoff (descriptive sheet names, freeze the header row, README) is absent from the workbook presented as the model of a readable table.
  *Fix:* Either describe the file as it is -- a typical government release table, readable in print, with specific shortcomings the student can name (two-row headers, no frozen panes, title block above the data) -- or replace it with a workbook that actually exhibits the formatting the sentence claims. Do not claim borders that are not there.
- **[must-fix] A table for reading (#sec-aesthetics)** -- The chapter gives "Source: Manitoba Agriculture, Foresight and Analysis" but row 72 of the worksheet reads "SOURCE: Canadian Grain Commission, Grain Elevators in Canada, August 2025", and row 70 flags "r: revised" values. Manitoba compiled the table; the data are CGC's. In a chapter whose README checklist says "identify sources", the text itself gets the provenance one step short.
  *Fix:* Cite both layers: data from the Canadian Grain Commission, Grain Elevators in Canada (August 2025), as compiled by Manitoba Agriculture. Point the student at the footnotes in rows 70-72 as an example of the source and revision notes a good table carries.
- **[must-fix] A model for entering assumptions (#sec-aesthetics)** -- "It opens with a disclaimer, lists assumptions separately and repeats the same layout across crop sheets." True, but the chapter then teaches Locking Cells as what "a workbook with editable assumptions should" do, and this workbook has no protected sheets (the My Farm cells are unlocked, but protection was never turned on). It also ships a sheet "Guide2026" whose only content is the placeholder " Please do this page". The example is presented as a clean exemplar while quietly contradicting the chapter's own advice.
  *Fix:* Say explicitly that the Ministry's file has the Locked property set on inputs but never applied sheet protection, and has a leftover placeholder sheet -- then use that as the exercise: protect the MBarley sheet and delete or fill Guide2026. A real file with real flaws teaches the habit better than an asserted ideal.
- **[should-fix] Data for further analysis (#sec-aesthetics)** -- "It begins in A1, uses one header row and keeps one observation per row." Correct, but the headers are "Spring Wheat", "Durum", etc. with no units anywhere in the file, no source sheet, and no note on how the extract was made. The chapter's own list says "Include units in headers" and the provenance note "Extract filtered to 2024--2025" appears only in the prose, not in the workbook a downstream user would receive.
  *Fix:* State what the numbers are (bushels per acre, presumably -- check the dashboard) and add that fact to the header row or a notes sheet of the workbook. Note in the prose that a tidy data sheet still needs units and a source line, and that this one lacks them.
- **[should-fix] Start With the Audience (#sec-workbook-audience)** -- "Polish should solve a reading problem. It is not decoration added after the analysis." This is a maxim delivered as a closing beat, not something the section has shown. The three bullets above it are assertions about what each audience wants with no evidence or example attached.
  *Fix:* Drop the two-sentence closer or fold it into the bullet list. Ground the audience bullets by pointing forward to the three workbooks ("the Manitoba table is the first case, the crop planner the second") so the claims are tied to files the student can open.
- **[should-fix] Give Each Sheet One Job (#sec-workbook-separation)** -- "Complex workbooks are easier to inspect when raw data, cleaning, analysis and presentation occupy different sheets." Stated as a general fact; none of the three example workbooks follows this structure (none has a README sheet, raw/clean separation, or an analysis sheet). The student is told a five-sheet structure is "useful" and then shown three files that do not use it.
  *Fix:* Either acknowledge that the government files do not follow this layout and say why a student's analysis workbook should differ (they are consumers of the crop planner; they would be producers of an analysis), or add a small student-built example workbook that has README / Raw / Clean / Analysis / Output sheets so the structure is demonstrated rather than asserted.
- **[consider] Sympathy for the Reader (#sec-audience-sympathy)** -- "Then open the file as though it arrived without an explanation. Start on the first sheet and see whether the next action is obvious." Reasonable advice, but the section's five questions are generic. No check is ever applied to the three examples -- for instance the crop planner's first sheet is a copyright disclaimer, and its second is a blank placeholder, so the "next action" is not obvious at all.
  *Fix:* Run the checklist on one of the three workbooks in two or three sentences. The crop planner is the natural candidate: what a producer sees first, where they would find the input cells, and what mistake (typing over a formula in column C) is most likely.
- **[consider] Locking Cells (#sec-locking-cells)** -- "Protection applies to each sheet separately and is not a security system." Good and honest. But the preceding sentence "For most teaching and workplace files, protection is meant to prevent accidents rather than keep information secret" is an unsupported generalization about practice; the limitation would be stronger as a concrete fact (sheet protection can be removed without the password in seconds; the data are fully readable regardless).
  *Fix:* Replace the "for most ... files" claim with the mechanical fact: protected sheets remain fully readable and the protection is trivially removable, so it guards against mistyping only. One sentence.

## module06b -- Writing a Report

### Excel guru

The writing advice is sound and the structure is usable as a checklist for the course project. The single biggest problem from the Excel side is that the chapter tells students what a reported number or table should look like but never connects that to the Excel mechanics that produce it: rounding via number format versus ROUND, pasting a chart or range into Word, and what the "workbook as the detailed record" should contain. A student working in Excel has no path from this chapter to an actual report file.

- **[must-fix] Presenting Numbers / Round to the measurement** -- "Write 42.4 bu/ac" is given with no indication of how to get there from a cell holding 42.3851. Module 1 already distinguishes number format (display only) from ROUND (changes the value). That distinction matters here: a cell formatted to one decimal still copies into Word as 42.3851 with Paste Special > Text or when pasted as unformatted text, and downstream formulas still use the full value. Students routinely format the display, paste, and get four decimals in the report.
  *Fix:* Add two or three sentences pointing back to @sec for Module 1 number formats: use a number format for display in the workbook; if the rounded value itself is copied into a report or used in later calculations, use ROUND(value, 1). Note that a formatted cell pasted into Word keeps the display format only with the default paste (Keep Source Formatting).
- **[must-fix] Tables and Charts** -- The section says when to use a table versus a chart and how to introduce each, but gives no Excel route for putting either into a report. Module 4b (@sec-excel-chart-export) covers chart paste options; nothing in the book covers moving a table. Pasting a range from Excel into Word gives a live-looking table that carries gridlines, fill colours and General-format numbers, which contradicts the chapter's advice on precision and restraint.
  *Fix:* Cross-reference @sec-excel-chart-export for charts. For tables, add a short paragraph: format and round the summary range in Excel first (number formats, units in the header), then in Word use Paste > Keep Source Formatting or Paste as Picture; state that Paste as Picture is safest for a fixed report and that the pasted table will not update. Mention that the summary table should be a small presentation range, not the raw data sheet.
- **[should-fix] Presenting Numbers / Report fewer numbers** -- "Move supporting detail to an appendix or provide the data file separately" is the right advice but leaves the Excel student guessing what 'the data file' is. Given Module 6a's sheet-per-job structure, the natural answer is the workbook itself, with a Summary sheet the report draws from.
  *Fix:* Tie this to @sec-workbook-separation: the report's numbers come from a Summary or Presentation sheet; the Data and Cleaning sheets are the appendix. One sentence naming that pattern is enough.
- **[should-fix] Describe the Method, Not the Keystrokes** -- "Keep the script or workbook as the detailed record" is asymmetric in practice. An R script is self-documenting; an Excel workbook is only a record if the formulas are visible and the steps are traceable. A workbook with pasted values and no note of where the PivotTable or Power Query came from is not a reproducible record. The example method sentence (weighted average by acres) is also one students will want to reproduce, and the Excel way (SUMPRODUCT(yield, acres)/SUM(acres), or a PivotTable with a calculated field) is never shown anywhere in the book that I can find.
  *Fix:* Add a sentence stating what makes a workbook a usable record: formulas left in place rather than pasted values, a Notes or README sheet listing the source file and any Power Query steps, and the summary cells that feed the report clearly labelled. Consider a one-line pointer to how the acre-weighted average is computed (SUMPRODUCT/SUM) or drop the weighting from the example if the course never teaches it.
- **[should-fix] Presenting Numbers / State the units** -- Units are described as belonging in "the sentence, table header or axis label". In Excel the common failure is a column named Yield with the unit nowhere, and students also try to type "42.4 bu/ac" into a cell, which turns the number into text and breaks averages. A custom number format (0.0 "bu/ac") displays the unit while keeping the value numeric, and this is the idiomatic Excel solution.
  *Fix:* Add one sentence: in a workbook put the unit in the header (e.g. Yield (bu/ac)) or use a custom number format such as 0.0 "bu/ac"; do not type the unit into the cell with the number, since that makes it text.
- **[consider] Presenting Numbers / Supply a comparison** -- "About 15% above the provincial average" is the kind of derived figure students compute in Excel and then misformat: a cell holding 0.15 shown as 0.15, or a cell holding 15 formatted as Percentage showing 1500%. The chapter's own examples invite this.
  *Fix:* A brief note that a ratio cell should hold the fraction (=B2/B3-1) and be formatted as Percentage with 0 decimals, rather than multiplied by 100, would prevent a predictable error in project reports.
- **[consider] The Course Project** -- The section says to use the chapter's structure but does not say what file the Excel-track student submits or in what form the charts and tables should arrive. Module tests take an .xlsx; the project report presumably does not. Students who did the analysis in Excel need to know whether to submit the workbook alongside a Word/PDF report, and whether charts should be pasted as pictures.
  *Fix:* State the deliverable explicitly: a written report (Word or PDF) plus the workbook or script as the reproducible record; charts pasted as pictures; summary tables rounded and unit-labelled in Excel before pasting. If the project details are still TBD per the course notes, at least say the workbook will be submitted with the report.

### Hadley Wickham

The advice is sound and the ordering (answer first, method in words, limits with consequences) is the right thing to teach. But the chapter is entirely tell and no show: there is not a single line of R or Excel, not one real number from a course dataset, and no account of how a number gets from a console into a sentence. The book has `sask_wheat_2025.csv` with `soil_zone`, `acres` and `yield_bu_ac` sitting right there; build the chapter around one real summary from it and the principles stop being assertions.

- **[must-fix] Lead With the Answer (#sec-lead-with-answer)** -- The opening example ("Canola yields in the black soil zone averaged 42 bushels per acre ... about 15% higher than in the brown zone ... including the drought year") is invented. No dataset in practice/data/ has canola by soil zone, so a student cannot reproduce the sentence, and the chapter never shows the calculation that the sentence summarises. The whole chapter is about turning a calculation into a sentence, and the calculation is missing.
  *Fix:* Use practice/data/sask_wheat_2025.csv, which the students already have. `read_csv("practice/data/sask_wheat_2025.csv") |> group_by(soil_zone) |> summarise(n = n(), yield = mean(yield_bu_ac, na.rm = TRUE))` gives Black 53.0, Dark Brown 45.2, Brown 38.6 bu/ac (n = 44/58/48). Show that console output, then show the sentence written from it ("Black-zone wheat fields averaged 53 bu/ac in 2025, about 14 bu/ac (37%) more than brown-zone fields"). Keep the rest of the chapter's examples on the same numbers so the reader can trace every figure.
- **[must-fix] Presenting Numbers / Round to the measurement** -- The section says to report 42.4 not 42.3851 but never says how, and the first thing a student will do is paste `53.04545` from the console. There is also no distinction between rounding for display and rounding in intermediate steps, which is where real errors come from.
  *Fix:* Add the mechanics in both tools, briefly: `round(x, 1)` and `signif()` in R; `mutate(across(where(is.numeric), \(x) round(x, 1)))` for a summary table; `scales::label_percent()` / `label_comma()` for axis labels; in Excel, number formatting changes display while `ROUND()` changes the stored value. One sentence: round at the point of reporting, never before the calculation finishes.
- **[should-fix] Describe the Method, Not the Keystrokes (#sec-report-method)** -- The model method sentence describes an acre-weighted average, but the chapter never shows what that is in code or how it differs from the unweighted mean. On the wheat data the unweighted and weighted means are nearly identical (Black 53.0 vs 53.5), which is itself a useful thing to report. Also, `mean(yield_bu_ac)` on the Black and Brown zones returns `NA` because two fields each have missing yield; this is exactly the kind of decision a method section must disclose, and the chapter's 'Missing data' bullet stays abstract.
  *Fix:* Show the two-line `summarise(mean = mean(yield_bu_ac, na.rm = TRUE), wtd = weighted.mean(yield_bu_ac, acres, na.rm = TRUE))` and then the prose version of it, side by side. Use the NA result as the motivating example for the Missing data caveat: 'Four fields had no reported yield and were dropped from the averages' is the sentence the reader needs.
- **[should-fix] The Course Project (#sec-project-report) and the chapter generally** -- The project is described as 'a reproducible analysis' and the method section says to 'keep the script or workbook as the detailed record', but the chapter never says how numbers and figures get into the report. Left unsaid, students retype values from the console into Word, which is the single largest source of wrong numbers in student reports. The book is written in Quarto and no chapter mentions it.
  *Fix:* Add a short section on the workflow: script produces figures with `ggsave()` (@sec in module04c) and a rounded summary table written with `write_csv()`; the report pulls those in rather than retyping. If you are willing to teach it, a minimal Quarto document with one inline `r` expression is the honest answer to 'reproducible'; if not, say explicitly that the project report may be Word but every number must be traceable to a cell or a line of the script.
- **[should-fix] Tables and Charts (#sec-charts-in-reports)** -- 'Use a table when the reader needs exact values' gives no way to make one. A printed tibble, with `<dbl>` type headers, `yield_bu_ac` column names and five decimals, is not a report table, and nothing in the book so far turns one into something presentable.
  *Fix:* Show the minimal path: `summarise()` -> `round()` -> rename columns to human names with units (`rename("Yield (bu/ac)" = yield)`) -> `knitr::kable()` (or copy to Excel and format, which is fine for this audience). Tie the caption rule (measure, groups, period, units) explicitly to `labs(title =, x =, y =, caption =)` from Module 4 so the chart advice is actionable rather than aspirational.
- **[should-fix] Presenting Numbers / Supply a comparison** -- Every comparison in the chapter is a bare percentage ('about 15% higher') with no statement of the base, and relative versus absolute differences are never distinguished. With the wheat data the difference is 14 bu/ac or 37% of the brown-zone mean; a reader cannot recover one from the other without the base.
  *Fix:* Give both forms once, with the real numbers, and state the rule: report the absolute difference in the data's units and, if you give a percentage, name what it is a percentage of. One sentence on percentage points vs percent if protein_pct is used anywhere (it is in the dataset).
- **[consider] Tables and Charts, example figure introduction** -- The sample introduction says 'compares average yield ... from 2021 to 2025 ... the gap remains after the 2021 drought', while the lead paragraph says the gap holds 'including the drought year'. Small, but the two example passages are meant to describe the same analysis and they describe the data differently; and neither corresponds to any figure the student can make.
  *Fix:* Once the worked example is on the wheat data, rewrite the figure introduction to describe the chart the student actually produces (a bar or boxplot of yield by soil zone from module04), and drop the 'Figure 3' placeholder or number it against a real figure in the chapter.
- **[consider] A Structure That Works (#sec-report-structure)** -- Item 7, 'Sources: provide enough information for another person to locate the data', duplicates the README guidance in module06a without pointing to it, and does not say what 'enough' is for a data file (agency, table name, download date, URL, which rows were excluded).
  *Fix:* Cross-reference @sec-workbook-separation and give one concrete source line for the wheat or Manitoba elevator data as the model.

### Andrew Gelman

The chapter's advice is mostly sound and its list of limitations (coverage, confounding, causation) is better than most report-writing guides. The biggest problem is that the running example it uses to model good writing is itself an invented result, never labelled as such, and its model sentences make inferential leaps ("so it is not explained by one unusual season", "the gap remains after the drought") that a five-year, two-group comparison cannot carry -- the chapter is teaching students to write confidently about numbers nobody computed.

- **[must-fix] Lead With the Answer** -- The model opening paragraph is presented as exemplary but its figures are invented and not marked as such: "Canola yields in the black soil zone averaged 42 bushels per acre over the last five years, about 15% higher than in the brown zone." None of the practice datasets (rm_canola_yields_1990_2025.csv, field_yields.csv, etc.) is cited, and the chapter that tells students to give "enough information for another person to locate the data" does not do so for its own example. A student reading this learns that confident numbers need no provenance.
  *Fix:* Either compute the example from an actual course dataset (rm_canola_yields_1990_2025.csv with rm_lookup.csv would support a soil-zone comparison) and say which file and years, or label the block explicitly as a hypothetical: "(Invented numbers, for illustration.)" The first option is better because the same numbers are reused in the Figure 3 and Method examples.
- **[must-fix] Lead With the Answer** -- The model paragraph's inferential clause overreaches: "The gap appears in every year in the data, including the drought year, so it is not explained by one unusual season." Consistency across five years rules out one explanation (a single bad season) but the sentence is framed as if it settles the interpretation. It also slides toward a causal reading of soil zone while the chapter later, correctly, lists confounding and causation as limitations. The example is teaching the storytelling move the caveats section warns against.
  *Fix:* Rewrite the clause to claim only what five years of two group means support, e.g. "The gap appears in every one of the five years, so it is not an artifact of a single season. Whether it reflects soil or differences in varieties, inputs or weather between the zones cannot be told from these data." Or keep the first sentence and let the second paragraph of the chapter note that the "why" belongs in the limits section.
- **[should-fix] Lead With the Answer** -- "Write this paragraph after completing the analysis, then move it to the beginning." Sensible, but as stated it invites writing the conclusion to fit whatever came out, with no mention that the question should be fixed before the answer is found. Combined with the project advice this is a small garden-of-forking-paths nudge.
  *Fix:* Add one sentence: the question and the comparison to be made should be written down before the analysis, and the opening paragraph should answer that question, including when the answer is "no clear difference."
- **[should-fix] Tables and Charts** -- The model figure introduction says "The black zone is higher in every year, and the gap remains after the 2021 drought." "Remains after" implies a before/after contrast that a 2021-2025 window cannot show (2021 is the first year), and it is a different claim from the opening paragraph, which said the gap appeared "including the drought year." The two model sentences describe the same made-up figure inconsistently.
  *Fix:* Make the two examples agree and restrict the claim to what the figure shows: "The black zone is higher in every year, including the 2021 drought year." If the intent is a drought-recovery point, the window has to start before 2021.
- **[should-fix] Describe the Method, Not the Keystrokes** -- The model method sentence introduces acreage weighting ("weighting each observation by reported acres so that larger plantings contributed more to the provincial comparison") without saying that this choice changes the number. The opening paragraph's "42 bu/ac" and "15%" would differ under unweighted averaging, and the chapter never connects the method choice to the Assumptions bullet in the limits section. A reader is shown that a method statement should sound reasonable, not that it should flag a choice that moves the result.
  *Fix:* Add a sentence after the example: an analyst choice like weighting should be stated in the method and, if the unweighted result differs materially, reported in the limits. Ideally show both numbers from the real dataset.
- **[consider] Saying What You Do Not Know** -- "Do not bury the answer beneath qualifications, and do not omit a limitation that could reverse the decision." This is the right principle but the chapter gives no example of a report where the honest answer is weak or null ("the difference is small relative to year-to-year variation"). Every example in the chapter has a clean, large, consistent effect, which teaches that reports are for findings, not for uncertainty.
  *Fix:* Add a second short model opening for a case where the data do not support a strong claim, e.g. a comparison where the gap is within the year-to-year range, showing how to lead with that as the answer rather than hunting for a result.
- **[consider] Presenting Numbers** -- "Report only the precision the data can support. Yield measured to the nearest tenth of a bushel does not justify four decimal places." Precision is tied only to measurement resolution. For a five-year mean of a noisy quantity the larger issue is sampling and year-to-year variation, which is not mentioned; "about 15%" in the lead is hedged with "about" but the reader is never told what the spread was.
  *Fix:* Add one sentence noting that the precision of a summary also depends on how much the underlying values vary, and that a range or the year-by-year values often says more than an extra decimal. Modules 10-12 can be pointed to for the formal version.
- **[consider] The Course Project** -- "A direct comparison with a clear chart, traceable calculations and honest limits is stronger than an elaborate method the reader cannot follow." Fine as advice, but "honest limits" is the only place honesty appears and it is asserted rather than operationalized; the project section does not say that the question must be stated up front or that a null or messy finding is acceptable.
  *Fix:* State that the project is graded on whether the question, data and limits are stated clearly, and that a finding of "no clear difference" is an acceptable answer when that is what the data show.

## module06c -- Presentations

### Excel guru

The chapter is sound on presentation craft and the advice is correct as far as it goes, but it contains no Excel (or PowerPoint) content at all: not one menu path, paste option, font size or chart-resizing step. A student who has only ever made charts inside a workbook gets told to "enlarge charts" and "test the projected size" with no instruction on how, and the one place the book does cover getting an Excel chart onto a slide (@sec-excel-chart-export in Module 4) is never linked. The biggest problem is that the mechanics of turning workbook output into readable slides are missing entirely.

- **[must-fix] Build Slides for Listening (#sec-slides)** -- "Enlarge charts. Remove fine detail, small annotations and unnecessary series" gives no Excel mechanics. Stretching a pasted Excel chart in PowerPoint scales the picture but not the font sizes if pasted as a chart object, and pasting as a picture then enlarging produces blurry text. Students will do exactly that.
  *Fix:* Add a short paragraph: resize the chart in Excel first (drag the chart frame to roughly 25 cm x 14 cm, or set Format > Size), then set chart text to 18-24 pt via Home > Font with the chart selected, delete gridlines and unneeded series, and only then copy it. Link to @sec-excel-chart-export for the paste options. Better still, add a one-sheet workbook (mod06_slide_chart.xlsx) with the same chart at report size and slide size side by side.
- **[should-fix] Build Slides for Listening (#sec-slides)** -- The chapter never says which paste option to use when moving an Excel chart into PowerPoint, and Module 4 only says Paste as Picture "fixes its appearance". For slides the trade-off is different: a linked or embedded chart keeps editable text (and can be re-fonted in PowerPoint); a picture cannot, but survives the file being emailed to the instructor.
  *Fix:* State the recommendation explicitly: Paste Special > Picture (Enhanced Metafile on Windows, PDF on Mac) for a finished chart, or Use Destination Theme & Embed Workbook if it still needs editing; note that Keep Source Formatting & Link Data breaks when the .xlsx is moved, which matters when the deck is submitted alone.
- **[should-fix] Build Slides for Listening (#sec-slides)** -- "Make every element readable from the back of the room. Test the projected size, not only the laptop view" is untestable in advance for most students and gives no number.
  *Fix:* Give concrete thresholds: nothing on a slide below 18 pt, chart axis labels and tick labels 18-24 pt, and suggest the proxy test of viewing the slide at 50% zoom (or standing 2 m from the laptop). Also mention Excel's default chart font (9-10 pt) is far too small for projection, which is why the chart must be re-fonted.
- **[should-fix] Explain a Chart Out Loud (#sec-presenting-charts)** -- The worked example (yield by soil zone over five years, one line per zone) does not correspond to any dataset in practice/data/ or any workbook in textbook_examples/, so students cannot reproduce the chart being described, and the chapter has no figure at all.
  *Fix:* Either build the example from an existing dataset (rm_canola_yields_1990_2025.csv or field_yields.csv, grouped by region/zone) and show the actual chart as a figure, or change the narrated example to the Module 4 Excel chart (mod04_graphing_excel.xlsx) so the chart the student explains aloud is one they have already built.
- **[should-fix] What Is Being Assessed (#sec-presentation-assessment)** -- "quoted numbers can be traced to the analysis" is an assessment criterion but the chapter never says what the student must hand in alongside the slides. For Excel work this is the workbook, and it is unclear whether the TA will see it.
  *Fix:* State the submission: the slide deck plus the .xlsx (or R script) that produced each chart, with each slide's source note naming the sheet or script. Note that a Paste-as-Picture chart carries no link back to the data, so the workbook must be submitted separately.
- **[consider] Explain a Chart Out Loud (#sec-presenting-charts)** -- "Point to the relevant line, bar or period while describing it" leaves the student waving at a projector. PowerPoint and Excel both have built-in ways to do this that first-years do not know about.
  *Fix:* Mention two options: add a coloured shape or arrow annotation directly in PowerPoint (Insert > Shapes) on a duplicate slide so the highlight appears on the second click, or in Excel format the one series of interest in colour and the others grey before copying (select series > Format Data Series > Line colour). This is the 'directing attention' idea from the Knaflic link, made operational.
- **[consider] Rehearse the Talk (#sec-presentation-rehearsal)** -- "Run the presentation from beginning to end with a timer" ignores that PowerPoint has this built in, and students on the Mac version look for it in a different place.
  *Fix:* Name the feature: Slide Show > Rehearse Timings (Windows and Mac) records time per slide; Slide Show > Rehearse with Coach (Microsoft 365) also flags pace and filler words. Also suggest Presenter View so notes do not end up on the slide, which directly supports the 'slides are not the script' point in #sec-slides.
- **[consider] Introduction / chapter as a whole** -- Module 6 is the only module where the Excel and R treatments are not side by side: Module 4 tells R users how to ggsave() a PNG at a chosen width and height for slides (module04c line 308), while the Excel path (right-click > Save as Picture) is mentioned only in passing in Module 4 and not at all here.
  *Fix:* Add a short 'Getting charts onto slides' subsection with two parallel columns or callouts: Excel (resize, re-font, right-click > Save as Picture > PNG, or copy and Paste Special) and R (ggsave with width, height, dpi), so the chapter matches the book's paired structure.

### Hadley Wickham

The advice is sound and the chapter is admirably short: question, answer, evidence, limits, answer again, and the axes-marks-pattern-meaning order for talking through a chart is exactly what I would teach. But it is all tell and no show. There is not one slide, one chart, one dataset or one line of code in a chapter of a book whose whole premise is teaching Excel and R side by side, so a student finishes it knowing what a good slide should look like and not how to make one from the yields data they already have.

- **[must-fix] Build Slides for Listening (#sec-slides)** -- The section tells the student to 'enlarge charts' and 'make every element readable from the back of the room' but never shows how. Module 4 ends with ggsave("output/region_boxplot.png", width = 6, height = 4, dpi = 300), which produces a chart whose 11-pt axis text is unreadable when projected. Nothing in the book tells the student that the fix is theme_minimal(base_size = 18) or similar plus a wider aspect ratio, or that in Excel the equivalent is resizing the chart before copying and bumping the font size.
  *Fix:* Add one short worked example that takes the region_plot object from @sec-saving-plots and rebuilds it for a slide: theme_minimal(base_size = 18), labs() with a claim as the title, ggsave("output/region_slide.png", width = 10, height = 5.6, dpi = 150). Show the two PNGs side by side (report size vs. slide size) so the difference is visible. Add the Excel twin in one sentence: resize the chart to roughly 25 cm wide, then set Chart Area font to 18 pt before Paste as Picture (link to module04b).
- **[must-fix] Build Slides for Listening (#sec-slides)** -- 'Use a claim as the headline' is the best idea in the chapter and it is illustrated with one sentence about canola in the black soil zone that does not correspond to any dataset in practice/data/. The student cannot reproduce it, and the rest of the chapter's examples (five years of yield by soil zone) also refer to data that does not exist in the book.
  *Fix:* Pick one dataset the student already has (rm_yields_2015_2024.csv or sask_wheat_2025.csv, which has a soil-zone column) and use it for every example in the chapter: the headline claim, the axes/marks/pattern/meaning walkthrough, and the question-and-answer example. Compute the number in the headline from that data so 'quoted numbers can be traced to the analysis' (your own assessment criterion) is modelled rather than only asserted.
- **[should-fix] Explain a Chart Out Loud (#sec-presenting-charts)** -- The four-step script (axes, marks, pattern, meaning) is described in prose with quoted sentences but there is no chart on the page. The reader has to imagine the line chart of yield by soil zone over five years while reading a script for it. Show-don't-tell applies doubly to a section that is about looking at a picture.
  *Fix:* Put the actual chart above the four steps, built with a console('...') chunk from the chosen dataset (geom_line with one line per soil zone, claim in the title). Then the four quoted sentences become a script the student can check against the figure. Consider annotating the chart with numbered markers 1 to 4 matching the steps.
- **[should-fix] Build Slides for Listening (#sec-slides)** -- The chapter never says what tool makes the slides. Students will reach for PowerPoint, which is fine, but the book teaches Quarto rendering in Module 2 and a one-line format: revealjs in the YAML gives a slide deck from the same .qmd they already know how to write, with the chart produced by the code chunk so it can never drift from the analysis. That is the reproducibility argument the report chapter makes, and it is left on the table here.
  *Fix:* Add a short subsection 'Two ways to make the slides'. PowerPoint: export the chart with ggsave or Paste as Picture, one chart per slide. Quarto: a minimal slides.qmd with format: revealjs, '## Claim headline' per slide, and a chunk that draws the chart. Be honest that PowerPoint is the convention in industry and revealjs is the convenient choice when the chart comes from R; neither is a rule.
- **[should-fix] Structure a Short Talk (#sec-talk-structure)** -- The five-part structure is stated but not instantiated. A student cannot tell what 'Evidence: two or three charts' means in slide count or time; 'Six or seven slides' appears two sections later, disconnected from the structure.
  *Fix:* Give one concrete deck outline for a 7-minute talk on the chosen dataset: slide 1 question (30 s), slide 2 answer (30 s), slides 3-5 one chart each (90 s each), slide 6 limits (60 s), slide 7 answer again (30 s). Seven slides, seven minutes. Move the 'six or seven slides' sentence here.
- **[consider] Questions (#sec-talk-questions)** -- The example answer ('I did not test whether the zones differ in seeding date') is good but unconnected to anything the student analysed. It reads as a template rather than as a demonstration of distinguishing what the analysis shows from what remains possible.
  *Fix:* Tie it to the chart used earlier: write the question a lab audience might actually ask about that chart (e.g. 'Is the gap just because the black zone got more rain?') and the answer that names the missing column (station_precip.csv exists and could even be joined, which makes the honest answer 'I could check that, and here is how').
- **[consider] The Audience Gets One Pass (#sec-one-pass)** -- 'Use fewer numbers than a written report' is right, but the chapter never says how many numbers a slide should carry or how they should be formatted. Module 6b covers rounding and units for reports; on a slide the rule is tighter (one or two numbers, rounded to the measurement, units in the headline).
  *Fix:* One sentence cross-referencing @sec-presenting-numbers and stating the slide version: at most two numbers per slide, already rounded, units in the claim headline rather than in a footnote.
- **[consider] What Is Being Assessed (#sec-presentation-assessment)** -- The criterion 'quoted numbers can be traced to the analysis' is the one that most rewards a reproducible workflow, but the chapter gives no mechanism for doing it. A student pasting a screenshot of an Excel chart has no link back; a student whose slide is rendered from a chunk does.
  *Fix:* Add a line under the criterion: keep the script or workbook that produced each chart and name the file on the source note of the slide (matching the 'source notes in the same place' advice in #sec-slides). That makes the criterion checkable by the TA and gives the consistency bullet a reason.

### Andrew Gelman

The chapter is plain, short and mostly says what it means, and the Questions section is the best thing in it: the model answer about seeding dates is exactly how one should decline to overreach. The biggest problem is that the running example does the opposite of what the chapter preaches -- "The black zone remains above the brown zone in all five years" slides straight into "The yield gap therefore appears broader than one unusual season" and a headline of "Canola yields were 15% higher," with no data, no uncertainty and no hint that soil zone is entangled with everything else. The chapter teaches honesty in the abstract while modelling a confident causal-sounding story in the particulars.

- **[must-fix] Explain a Chart Out Loud (sec-presenting-charts)** -- The four-step example moves from pattern to meaning with a 'therefore' that the evidence does not license: "The black zone remains above the brown zone in all five years" then "The yield gap therefore appears broader than one unusual season." Five points on a line with no sense of year-to-year variation, field counts or weighting cannot tell you whether a gap is 'broader' than one season; it can tell you the sign was the same five times. The student is being shown, as the model of good practice, exactly the inference-from-a-small-table move the rest of the module warns against.
  *Fix:* Make step 4 a descriptive claim that matches step 3, e.g. 'The gap is not confined to one year,' and add a fifth step or a sentence that says what the chart does not show (how many fields, how much the gap varies, what else differs between zones). Alternatively keep the 'meaning' step but have it explicitly name the hedge: 'consistent with a persistent difference, though five years is a short series.'
- **[must-fix] Build Slides for Listening (sec-slides)** -- The recommended headline "Canola yields were 15% higher in the black soil zone" is presented as the virtuous alternative to "Yield by soil zone", but it is a bare point estimate with no period, no base, no uncertainty, and it is read by any listener as 'black soil causes higher yield.' The assertion-evidence style is fine; the assertion here is overconfident. Also, this number has no provenance anywhere in the course: the only soil-zone dataset in practice/data is sask_wheat_2025.csv, which is wheat, and module06b invents the 42 bu/ac figure in a box without a source.
  *Fix:* Either attach the example to a real dataset the student can open (and change the crop to wheat if that is what the data are), or flag it as hypothetical in the first use. Then write the headline the way the chapter's own 'Limits' step would demand: 'Black-zone canola yields averaged about 15% above brown-zone yields, 2021-2025' -- a description of the comparison, not a claim about the zone.
- **[should-fix] Structure a Short Talk (sec-talk-structure)** -- Step 4, 'Limits: Identify the uncertainty or missing information most relevant to the conclusion', is the one place the chapter asks for honesty about what the evidence supports, and it is one line with no example, while steps 1-3 and 5 each get illustration elsewhere in the chapter. For this example the obvious limitation (soil zone is correlated with rainfall, varieties, management, seeding date) is never stated until it shows up as a hypothetical audience question.
  *Fix:* Give the Limits step the same treatment as the chart walk-through: one or two model sentences a student could say on a limits slide for the soil-zone example, such as 'Zones differ in rainfall and management as well as soil, so this compares regions, not soil types' and 'Five years of provincial averages; I have no field-level data.'
- **[should-fix] Structure a Short Talk (sec-talk-structure)** -- "State the main finding in one sentence." and "End with the finding and its practical meaning." are good advice for clarity but, stated without qualification, push the student toward a clean story. Many honest analyses of student-sized datasets end with 'the difference is small relative to the year-to-year variation' or 'the data cannot distinguish these two explanations', and the chapter never says that this is an acceptable one-sentence answer.
  *Fix:* Add one sentence after the list: the one-sentence answer can be 'the data do not settle this', and a talk that says so clearly scores better than one that manufactures a result. This also makes the assessment criterion 'limitations are stated honestly' concrete.
- **[consider] Questions (sec-talk-questions)** -- "This is stronger than guessing. It distinguishes what the analysis shows from what remains possible." is the right idea, but the preceding model answer is about a confounder that the speaker 'did not test', which quietly implies the speaker could have tested it and settled the matter. With provincial zone averages you cannot; the issue is that zones differ in many ways at once, not that one covariate was omitted.
  *Fix:* Adjust the model answer so it admits the structural problem rather than a single omitted check: 'Zones differ in seeding dates, rainfall and varieties as well as soil, and this comparison cannot separate those. Field-level data with planting dates would be a start.' Keep the two-sentence closer but drop 'This is stronger than guessing', which tells the reader what to think of it.
- **[consider] The Audience Gets One Pass (sec-one-pass)** -- "Use fewer numbers than a written report." and, in sec-presenting-charts, "Quote the one or two numbers needed to establish the scale of the result." are sensible, but 'fewer numbers' in practice tends to mean 'drop the uncertainty and keep the point estimate.' The chapter never says which numbers survive the cut.
  *Fix:* One added sentence: when you cut numbers, keep the comparison and something about its variability (the range across years, or how often the sign flipped), and drop the decimal places, not the caveat.
- **[consider] What Is Being Assessed (sec-presentation-assessment)** -- "A talk succeeds when the audience can state what was found and why the evidence supports it." This criterion rewards persuasiveness; a talk can meet it perfectly while overclaiming, and the 15%-headline example above would pass. The rubric line 'limitations are stated honestly' is there but is the fourth of five bullets and is not what the closing sentence restates.
  *Fix:* Rewrite the closing criterion to include the limit: the audience should be able to say what was found, why the evidence supports it, and what it does not settle. That is what the chapter actually wants and it costs five words.
